BUILD_DIR := build
NAME      := {{ project.name }}

.PHONY: all build install develop test lint check clean

all: develop

develop:
	pip install -e ".[dev]" --no-build-isolation

build:
	pip build --no-build-isolation

test:
	pytest tests/ -q

lint:
	ruff check src/ tests/

# ---------- the single gate: frob check ----------
check: lint test
	frob check

clean:
	rm -rf $(BUILD_DIR) dist/ *.egg-info _skbuild
