[package]
name = "{{ project.name }}"
version = "0.1.0"
edition = "2021"

[lib]
name = "_{{ project.name }}"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
