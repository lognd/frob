; T-5335: frob's sqlfluff plugin default config -- read by
; frob.sql._sqlfluff_plugin.load_default_config via sqlfluff's own
; load_config_resource. None of Frob_L001-Frob_L004 take a config
; keyword yet, so this section is intentionally empty; it exists because
; sqlfluff's plugin host calls load_default_config unconditionally and
; load_config_resource expects at least a [sqlfluff] header to parse.
[sqlfluff]
