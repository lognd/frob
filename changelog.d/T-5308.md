bump: minor
T-5308: Adds WEBSEC117-122 (header/URL/log injection, WebSocket origin/WSS enforcement), the header/URL/log leaf of the T-5140 web-app epic, reusing T-5307's framework-detection contract and taint-pass shape. Wiring uses a new generic discovery mechanism in taint_gate (pkgutil/importlib over frob.webapp._websec_* modules exposing websec_findings) instead of a hand-edit, since both direct-edit files were leased by concurrent siblings (T-5311, T-5395) at scope time; T-5311 requested this mechanism once its own lease freed. Positive control proven by TestTaintGateDiscovery calling the real taint_gate() against a synthetic Django repo: frob's own checkout is not a detected web framework, so a self-check of this repo cannot show the finding.
