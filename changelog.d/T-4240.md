bump: minor
T-4240: (T-4240) Changed: - src/frob/gates/_ratchet.py::baseline_overrun_violations (new) - src/frob/gates/_ratchet.py module docstring (documents the T-4240 addition and the T-4540 scope-lease blocker on wiring it in) - tests/test_gates_ratchet.py::TestBaselineOverrunViolations (new test class, 4 tests) - docs/modules/gates.md (new "### BASE001: a baseline overrun blocks land, reported by name (T-4240)" section, right after the existing "## Ratchet pools (T-0569)" section) WHY: consumer F-326/H2-3 (second half; TESTRUN001 half is T-3988's, untouched here) found that nothing in the ratchet-pool mechanism (`frob.gates._ratchet`) ever asks, in aggregate, whether a tracked pool's CURRENT live finding count has grown past what got committed as its baseline -- `resolve_ratchet_severity` only judges one finding key at a time, so a red ratchet pool could accumulate indefinitely with no single finding ever naming the fact that the baseline no longer holds. WHAT: `baseline_overrun_violations(root, current_counts: dict[str, int]) -> list[Violation]` loads `frob-ratchet.lock.json` (`load_ratchet_lock`) and, for each rule id present in the caller's `current_counts`, compares the caller-measured live count against `len(pool.entries)` for that rule's committed pool. A rule whose current count exceeds its baseline gets ONE `BASE001` (severity ERROR) `Violation` naming the lock file, the rule id, and both counts, e.g.: BASE001: frob-ratchet.lock.json rule=DEAD001 current 3 exceeds baseline 2 -- this ratchet pool has grown past its committed baseline; snapshot the new findings deliberately (`frob pool snapshot DEAD001`) or fix them before landing Silent for: a rule absent from `current_counts` (not measured this run -- the function never invents a count) and a rule with no committed pool at all (`pool_for` returns `None`, i.e. never snapshotted -- an un-ratcheted rule has no accepted baseline to exceed; that is `ratchet_enabled_rules`/`frob pool snapshot`'s territory, not BASE001's). Failing test first: `tests/test_gates_ratchet.py:: TestBaselineOverrunViolations::test_current_count_exceeding_baseline_fires_base001` was written against a not-yet-existing `baseline_overrun_violations` import; running it (before the implementation) failed at collection with `ImportError: cannot import name 'baseline_overrun_violations' from 'frob.gates._ratchet'`, confirmed live in this session (see commit history: the test file's import list was added, then the implementation, in that order). Acceptance criterion ("a baseline lock whose measured count exceeds accepted_count must yield a BASE001 finding naming the file and the two counts") is proven by all 4 new tests, plus the pre-existing 21 `tests/test_gates_ratchet.py` tests re-run clean (no regression): - TestBaselineOverrunViolations::test_current_count_exceeding_baseline_fires_base001 -- exceeding count fires exactly one BASE001, message contains the lock file name, rule id, and both counts ("baseline 2"/"current 3"). - TestBaselineOverrunViolations::test_current_count_at_or_below_baseline_is_silent -- at or below baseline, silent. - TestBaselineOverrunViolations::test_rule_never_baselined_is_silent -- a rule with no committed pool at all is silent (documented boundary, mirrors resolve_ratchet_severity's own scope). - TestBaselineOverrunViolations::test_names_the_lock_file_and_both_counts_for_multiple_rules -- with two pools tracked, only the one that actually overran fires, and it names itself correctly (no cross-rule leakage). Evidence node ids bound via `frob ticket evidence T-4240 <node-id> --base-ref dev` (4 calls, all succeeded despite a benign xdist-plugin warning on the evidence command's own internal collection probe -- the actual test run used `-p no:xdist` and passed clean, see below): - tests/test_gates_ratchet.py::TestBaselineOverrunViolations::test_current_count_exceeding_baseline_fires_base001 - tests/test_gates_ratchet.py::TestBaselineOverrunViolations::test_current_count_at_or_below_baseline_is_silent - tests/test_gates_ratchet.py::TestBaselineOverrunViolations::test_rule_never_baselined_is_silent - tests/test_gates_ratchet.py::TestBaselineOverrunViolations::test_names_the_lock_file_and_both_counts_for_multiple_rules Direct test run (serial, matching the brief's tooling section): PYTHONPATH=$(pwd)/src .venv/bin/python -m pytest tests/test_gates_ratchet.py -p no:cacheprovider -q -p no:xdist -> SUITE-RESULT: exitstatus=0 collected=25 failed=0 Lint: `ruff check src/frob/gates/_ratchet.py tests/test_gates_ratchet.py` -> All checks passed. `ruff format` found one file needing reformatting (tests/test_gates_ratchet.py); reformatted, re-ran tests, still 25/25 green. Gate check: `frob check --only gates --files src/frob/gates/_ratchet.py --files tests/test_gates_ratchet.py --files docs/modules/gates.md --base dev` was run (took ~8 minutes under fleet load, matching the frob-check-cost-model lesson -- this appears to have run a broader sweep than file-scoped, since the tool summary shows repo-wide gate:ARCH/COV/DOC/... FAIL counts). Grepped the full output for any mention of `_ratchet.py`, `test_gates_ratchet.py`, `gates.md`, or `BASE001`: zero hits outside the WALK001 pre-existing-waiver noise (none of which touch my files) -- my change introduces no new gate findings (no missing frob:tests/frob:doc COV002, no unregistered GATERULE001, no DOC drift). All the FAIL lines in that run's summary are pre-existing repo-wide state unrelated to this change. WIRING NOT DONE (scope refusal, recorded per BRIEF.md): the ticket asked to "wire it into the land pre-sweep the way SELFAUDIT001 is wired". SELFAUDIT001's live-gate registration lives in `frob.gates.__init__`'s `_ProcessJob` pipeline (`sys_gate` is registered there), and a new BASE001 rule id also needs registration in `frob.gates._waive`'s `_KNOWN_GATE_RULES` (GATERULE001's completeness scan). Both attempts were refused by the fleet's own scope-lease mechanism: - `frob ticket scope T-4240 --add src/frob/gates/__init__.py ...` -> ScopeLeaseConflict: held by in-progress T-4540 - `frob ticket scope T-4240 --add src/frob/gates/_waive.py ...` -> ScopeLeaseConflict: held by in-progress (LIVE cross-worktree lease) T-4214 Per BRIEF.md's scope rule ("If you need another file... refused if leased elsewhere -> record in your report, do not touch it"), neither file was touched. `baseline_overrun_violations` is fully implemented, tested, and documented as a self-contained, additive function -- exactly the same posture this module's own T-0569 `resolve_ratchet_ severity` was originally left in ("Deliberately additive and self-contained... owned by a concurrent wave this ticket does not touch"). The module docstring and the new docs/modules/gates.md section both explicitly record that the live-gate wiring (and the GATERULE001 registration) is left for a follow-up ticket once T-4540/ T-4214's leases clear. FOLLOW-UP TICKET FILED (per coordinator instruction, "catalogued is not enforced" -- a detector that is not wired is not a gate, so the gap is tracked explicitly rather than left implicit in a docstring): `T-draft-daef879a` -- "wire BASE001 baseline_overrun_violations into the gate pipeline (_ProcessJob registry in gates/__init__.py, _KNOWN_GATE_RULES in _waive.py) and the land pre-sweep", kind=feature, priority=high, parent=T-4240 (`frob ticket set-parent T-draft-daef879a T-4240`), blocked_by=[T-4540, T-4214] (`frob ticket block T-draft-daef879a --by T-4540` / `--by T-4214`), filed from ROOT. `src/frob/gates/_ratchet.py::baseline_overrun_violations` now also carries `# frob:todo T-draft-daef879a wire into gate pipeline once T-4540/T-4214 release their leases` (commit 0a55bd852) instead of only prose-describing the gap, per TODO001/the drive's own "bare TODO fails frob check" discipline. Re-verification after adding the frob:todo directive: `ruff check` and `ruff format --check` both clean on src/frob/gates/_ratchet.py; `tests/test_gates_ratchet.py` re-run (25/25 passed, exitstatus=0) -- the directive-only change touches no behavior, so evidence node ids are unchanged and still valid (same 4 ids, all still passing); no evidence refresh needed. Filed: T-draft-daef879a (blocked by T-4540, T-4214; parent T-4240). Gates: `frob check --only gates` clean for the touched files (see above); no waivers added.
