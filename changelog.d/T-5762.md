bump: minor
T-5762: frob's LAYOUT review gate (T-5747 story, leaf F-2) needs crunk on PATH whenever a repo actually declares a gallery-eligible org bucket, so a missing crunk install is caught by `frob doctor` instead of silently producing no gallery manifest. This registers crunk as ToolCategory.REQUIRED_FOR_FAMILY the same way sqlfluff/squawk already are for SQL (T-5335's own machinery), with a new relevance predicate that reads crunk.toml directly (components/layouts bucket declared) rather than importing crunk, keeping doctor.py/webapp leaf layers with zero crunk dependency. Tests cover the positive control (buckets declared, crunk missing -> a FAILING finding), the negative control (no buckets -> silent), presence (buckets declared, crunk present -> silent), and the relevance predicate's own edge cases (non-gallery buckets, malformed TOML).
