bump: minor
T-4611: T-3233's land spent 27.3s of forced-synchronous verification because the quarantine-raised ERROR line named only a COUNT of undisposed findings, never which (rule, file) pairs -- triaging required a separate `frob verify dispose`-read round trip mid-land. Fix: _quarantine_undisposed_summary (src/frob/app/ticket_runner/ _land_cmd.py) now enumerates each undisposed finding's deduplicated (rule_id, file) identity inline, capped at 8 with a "+N more" suffix beyond that, so the first log line alone is enough to triage. Added to scope with reason: this is the one source file that emits the log line this ticket exists to enrich.
