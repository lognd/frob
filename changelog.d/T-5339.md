bump: minor
T-5339: A frob:waive with only reason= prose on a T-5335 sqlfluff performance finding cannot prove the flagged query's plan was actually checked -- only that someone asserted it was fine. Reusing INV008's own "declared but unproven" two-part obligation shape (a real bound artifact required, not just a text claim) rather than inventing a new directive keeps this one class of waiver honest without adding a second obligation DSL: frob:waive's already-generic attribute grammar accepts explain="..." with zero frob.graph.dsl changes. Rule-id scope is pkgutil-discovered from frob.sql submodules (T-5308's own convention) so a later SQL rule family opts in without editing this gate. Not wired into frob check's live dispatch yet -- same "gate lands before registration" posture T-5335's own sqlfluff plugin and T-5334's extraction substrate both already carry; a later leaf does the wiring.
