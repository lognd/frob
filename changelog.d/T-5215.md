bump: minor
T-5215: Root cause: `src/frob/policy/__init__.py` imported Severity/Violation/ WaiverRef from `frob.gates._models` -- a submodule of the heavy `frob.gates` package, so importing it always executes `frob/gates/__init__.py`'s entire eager stage roster first (ordinary Python package-import semantics). Fix: import the same three names from `frob.findings` instead -- the leaf module `frob.gates._models` itself already re-exports them from (T-1201's own split), and which imports zero `frob.*` modules. Fixing frob.policy alone was NOT sufficient to make plain `import frob` gates-free, contrary to this ticket's original diagnosis: traced with `python -X importtime` after the policy fix, and found the SAME anti-pattern independently in four `frob.vet` modules (_ecosystem.py/_scan.py/_scan_violations.py/_supplychain.py, all importing Severity/Violation from frob.gates._models) and in `frob.testing._coverage_wait` (a module-level `from frob.gates._coverage import load_stamp`) -- all reached from `frob`'s own top-level `__init__.py` via `frob.doctor` regardless of frob.policy. Fixed all five alongside: the four vet files route through frob.findings the same way policy now does; `_coverage_wait.py`'s `load_stamp` (a function, not a data model -- no frob.gates-independent leaf-module home to move it to) is now a deferred, function-local import inside its one caller instead of a module-level one, matching the file's own pre-existing T-0634 deferred-import precedent for the same reason (avoiding a heavy package-init tax), extended to a second, narrower reason (T-1318/T-5215). Measured: `python -c "import sys; import frob; print('frob.gates' in sys.modules)"` now prints False (was True before this fix). All 5 tests/unit/security/test_redact.py tests pass, including 2 new ones (TestPolicyModuleImportGraph, subprocess-isolated per this file's own established T-1318 idiom) proving both `import frob` and `import frob.policy` leave frob.gates out of sys.modules. Full regression sweep: tests/unit/security/test_redact.py, tests/vet_suite/ (full), tests/unit/test_app_lazy_exports.py, tests/unit/test_app_lazy_dispatch.py, tests/test_coverage_wait_shared.py, tests/test_policy.py, tests/unit/test_policy_weakening_gate.py -- 592 tests total, 0 failed.
