bump: minor
T-5352: WEBSEC209-216 close the ticket's JWT/OAuth-token gap: no lint covered jwt.decode/verify's exp/nbf/audience/issuer arguments, SimpleJWT's refresh-token rotation+TTL config, tokens leaked via URL, or the OAuth authorization-code flow's state/PKCE parameters. Reusing T-5308's pkgutil discovery hook (websec_findings(root, frameworks)) keeps taint_gate's own wiring untouched, matching every sibling WEBSEC leaf. Fixtures live under their own websec2xx/jwt_oauth/ subdir, separate from sibling T-5351's csrf_session/ subdir under the same parent, per the coordinator's split. WEBSEC217 is left unimplemented (secrets- committed is SEC001-003's job per the ticket body, no distinct check fits the 9th reserved id) and filed as T-5496 rather than expanding this ticket's scope.
