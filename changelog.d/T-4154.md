bump: minor
T-4154: Changed: src/frob/check/_python.py::_nested_worktree_ty_excludes (new) src/frob/check/_python.py::_ty_base_cmd (wires the new excludes in) tests/unit/test_check.py::TestRunTyRealPaths (3 new tests) Used the existing `frob.excludes._is_nested_worktree` helper (T-0239) rather than a bare `.claude/worktrees/**` path pattern of its own, per the ticket's ask: `_nested_worktree_ty_excludes` lists the immediate children of `<scan>/.claude/worktrees/`, keeps only the ones `_is_nested_worktree` confirms are real git checkouts, and passes each as its own `ty check --exclude <dir>/**` glob. This is truth-based (a stale non-git leftover under that directory is correctly NOT excluded, covered by `test_non_worktree_dir_under_worktrees_not_excluded`) rather than name-convention-based (not tied to the `agent-*`/ticket-id naming shape). Evidence: tests/unit/test_check.py::TestRunTyRealPaths::test_nested_claude_worktrees_are_excluded tests/unit/test_check.py::TestRunTyRealPaths::test_no_worktrees_dir_adds_no_exclude tests/unit/test_check.py::TestRunTyRealPaths::test_non_worktree_dir_under_worktrees_not_excluded (repro designated and verified: fails at ef13d6246 (test-only commit), passes at 61e72836a (fix commit)) Full tests/unit/test_check.py run: 142/142 passed (2 pre-existing, unrelated failures deselected -- TestRunRuffRealPaths::test_invokes_ ruff_via_project_tool_argv_not_bare_ruff and TestRunRuffAutofix:: test_success_runs_fix_then_format_via_project_tool_argv both assert argv without `--no-sync`, which `project_tool_argv` gained under T-4163 after these tests were last touched; confirmed unrelated to this diff by reverting src/frob/check/_python.py to HEAD and reproducing the same two failures). Filed: none Gates: frob check --ticket T-4154 -- gate:SCOPE/gate:PREWORK and the diff-scoped gate:FMT (FMT001, fixed via `frob format --directives`) clean; ruff-check/ruff-format clean on touched files. Repo-wide gate:ARCH/gate:COV/gate:DOC/gate:DRIFT/gate:PRE/gate:SCOPE/gate:TODO findings in the full run are pre-existing (DOC006 from sibling ticket T-4174's body; TODO001 at tests/unit/test_check.py:221 predates this diff -- a comment mentioning "TODO" inside unrelated prose, nowhere near the touched lines; SCOPE002 findings name `_diag_severity`/ `_unresolved_count`/etc., none of which this diff touches) and unrelated to this diff.
