bump: minor
T-4723: T-4723 -- Source narrative C6: arch/ check/ clean/ deploy/ dup/ narrative/ perf/ -- Done report ## Scope actually swept Ticket scope was narrowed at start time (`frob ticket scope T-4723 --remove src/frob/narrative/_migrate.py`) because that file was leased by in-progress T-4697 -- collision recorded in the ticket body, file NOT touched. Final swept scope (20 files): src/frob/__init__.py src/frob/_cli_parsers/_ticket/__init__.py src/frob/arch/__init__.py src/frob/arch/_abstraction.py src/frob/arch/_mayraise_tables.py src/frob/arch/_models.py src/frob/arch/_normalized.py src/frob/arch/_python.py src/frob/check/__init__.py src/frob/check/_python.py src/frob/clean/_rules.py src/frob/deploy/_audit.py src/frob/deploy/_conform.py (fully exempt, no edit) src/frob/doctor.py src/frob/dup/_legacy.py src/frob/dup/_template.py src/frob/excludes.py (fully exempt, no edit) src/frob/findings.py src/frob/logging/logger.py src/frob/perf/_hotpath_smells.py ## What changed, per file (WHAT / WHY) Same method as T-4719 (see that ticket's Done report for the full mechanism description): for every comment run over 12 consecutive `#` lines that was not already a pure frob:/noqa/type:/ruff:/mypy: directive run, `frob narrative move <file> <line> [--keep-file F] --reason "..."` moved the historical/cross-referenced content into the ticket it cited, leaving a `# see T-#### for the history behind this` pointer. --keep-file was used to retain the frob:doc anchor at check/__init__.py's FROB_CHECK_STOP_BEFORE_ENV and the clean/_rules.py `.frob`-protect tuple. One run (deploy/_audit.py's filesystem-exclusion allowlist) cited no ticket -- appended to T-4723's own body via `frob ticket body T-4723 --append-file`, code condensed to its existing `frob:doc` line plus a `# see T-4723` pointer. Tickets that received moved narrative: T-3350, T-4521, T-1182, T-1144, T-1141, T-1112, T-1181, T-0689, T-0685 (recorded as T-0686 alias in the ticket lookup), T-0687, T-1023 (T-0757's citation resolved there since it was mentioned first in the block), T-1195, T-0610, T-1066, T-0289, T-0627, T-3256, T-3689, T-3220, T-3943, T-3985, T-2970, T-0495, T-4447, T-3276, T-1621, T-2979, T-1649, T-1647, T-3675, plus T-4723 itself (one no-citation block). ## Collateral fixes (found and fixed within scope, not filed separately) A wholesale comment-run move sometimes carried away a directive that happened to sit inside the same contiguous comment run as the narrative being migrated (its digest changed, surfacing pre-existing-but-dormant COV001/COV002 findings, or in two cases actually deleting a live enforcement directive). Fixed in a second commit: - arch/_models.py::ArchSeverity -- restored its `frob:doc docs/modules/arch.md#arch-suggestion` anchor (COV001 regression). - arch/_abstraction.py::_FORWARDER_BODY_TOKEN_LIMIT -- restored the `frob:waive PII012`/`frob:ticket T-1195` pair a wholesale move had swallowed (a real suppression, not just documentation -- losing it would have surfaced a PII012 false positive on this symbol). - arch/_normalized.py (module level) -- restored `frob:invariant INV-042 no_import="tree_sitter"` plus its `invariant spec:` line and the `frob:tests` edge INV005 needs to see the evidence reach this file's own anchor -- a real invariant enforcement directive, not documentation, that a wholesale move had swallowed along with its T-1023 framing. - _cli_parsers/_ticket/__init__.py::_add_ticket_parser and check/_python.py::_run_gates -- their prior ticket citations (T-4521, and T-0095/T-1346 respectively) are all closed, so COV002 fired once the digest moved; added `frob:ticket T-4723` to each. All four were caught by running `frob check --only coverage` against the full 20-file scope AFTER the first narrative-move commit, plus a manual `git diff dev -- src/frob | grep "^-.*frob:(doc|enforces|tests| waive|invariant|ticket)"` sweep that caught the PII012/INV-042 losses COV didn't have a rule for. ## Acceptance criteria, how each is proven [1] Zero comment runs >12 lines remain in the 20 swept files. Verified with a repo-local awk scan for contiguous `#` runs >12 lines over every file in scope; every remaining >12 run is a frob:/noqa/type:/ruff:/mypy: directive-only run (syntax-exempt), confirmed by hand for each hit. [2] Every block that cited a ticket has its narrative readable in that ticket body, moved not deleted. Every `frob narrative move` call reported "moved N line(s) ... into T-####" with the ticket body's char count growing by the reported amount (session transcript). [3] Every block that cited no ticket has its narrative in T-4723's own body. The one no-citation block (deploy/_audit.py's exclusion allowlist) was appended via `frob ticket body T-4723 --append-file`. [4] `git diff -w` over non-comment lines is empty. `git diff -w -- src/frob | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -vE '^[+-]\s*#'` returns 0 lines, checked after both commits. [5] `frob ticket list` exits 0 after each batch of ledger writes. Checked as a final check after all narrative-move/body/evidence batches: exit 0. ## Test node ids (all green, PYTHONPATH=<worktree>/src, serial) tests/unit/perf/test_hotpath_smells.py::TestPerf010YamlCLoader::test_fires_on_pre_fix_shape tests/test_excludes.py::test_is_test_file_by_dir_component tests/unit/test_doctor.py::TestNativeDegradeWarning::test_missing_extensions_named_loudly Broader confirmation run (not bound as evidence, just a wider smoke check): `pytest tests/unit/perf/test_hotpath_smells.py tests/test_excludes.py tests/unit/test_doctor.py tests/test_dup.py` -> 95 collected, 0 failed. All 5 acceptance criteria have evidence bound (3 node ids each), `frob ticket evidence T-4723 <3 node ids> --accepts N --base-ref dev` for N in 1..5, run AFTER the last code commit (f2a6515f5). ## Commits (in order) dee09a0a3 docs(narrative): condense C6 arch/check/clean/deploy/dup/logging/perf comment runs f2a6515f5 docs(narrative): restore directives dropped by the C6 narrative-move sweep 4e5706a32..f7a4a6f1f chore(tickets): record evidence for T-4723 (x5, one per criterion) (plus the earlier self-committing `frob ticket scope`/`start`/`body` calls that ran before any code edit: 25f1fa74c, 49627d043, 072403a58) ## Pre-READY checks `frob check --only sys --files <20 files> --base dev`: gate:DOCARCH pass (0 errors, 552 warnings -- all WARN-tier docstring findings, none attributable to a comment-run change, all pre-existing narrative-in-docstring debt outside this ticket's scope); gate:DRIFT FAIL 6 errors/5 waived (identical baseline count, none in scope files); gate:DSL FAIL 1 error at tests/test_app.py:387 (pre-existing, unrelated file); gate:WAIVE pass; no error attributable to any of the 20 scope files. `frob check --only arch --files <20 files> --base dev`: pass (21 warnings, 36 waived, 546 suggestions repo-wide) -- 0 errors. Every finding touching a scope file is a pre-existing large-file/ high-coupling/abstraction-opportunity/pattern-recommendation/ lock-identity-unresolved WARN or suggestion, structurally unaffected by a comment-only edit (line-count/import-count/signature-sharing facts, none of which a comment touches). Zero ARCH001 (long-and-complex) or LARGE001 findings tied to this change. `frob check --only coverage --files <20 files> --base dev` (run twice, after the fix-up commit): only remaining scope-file hit is gates/_version_coupling.py::version_coupling_gate's pre-existing, already-`[waived: ...]` COV001 (that file is not even in this ticket's scope -- it surfaced because `--only coverage` scans repo-wide). Zero unwaived COV001/COV002 in the 20 scope files after the two directive restorations. `ruff check <20 files>`: All checks passed! `ty check <20 files>`: All checks passed! ## Notes for the coordinator - Scope refusal: src/frob/narrative/_migrate.py removed from T-4723's declared scope at start time due to a live lease collision with T-4697 (the sibling "register frob narrative move as Tier-A --fix" leaf); not swept, deferred. - No waivers added. - No tests skipped; full green run on the modules touched (95 tests). - This ticket surfaced a real pattern worth flagging for future cluster leaves: `frob narrative move`'s default (no --keep-file) removes the WHOLE contiguous comment run, including any frob:doc/frob:enforces/ frob:invariant/frob:tests/frob:waive directive lines that happen to sit inside the same run as the narrative prose. Every cluster leaf should `git diff dev -- <scope> | grep "^-.*frob:(doc|enforces|tests| waive|invariant|ticket)"` after a wholesale move batch and restore anything that was a real directive, not narrative.
