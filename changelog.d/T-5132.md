bump: minor
T-5132: Tickets needed a story-point size so `frob ticket start`/flow/sprint show can enforce and report on sizing, per the owner's 2026-09-20 directive. Added Ticket.points (Fibonacci scale, validated at write time like milestone), a `frob ticket points`/`tokens` setter pair, and a hard refusal at `frob ticket start` for an unsized queued ticket (single `--unsized-ack REASON` override, chosen deliberately over a repo-wide exemption date since the owner's own ticket body argues no backfill of existing queued tickets is needed -- the check only trips when a ticket is actually started). Points-weighted rollups (total/done/ETA) were added to `sprint show`, and points-per-hour / tokens-per-point calibration to `flow`, both mined from the same git-history transition source `sprint_velocity` already trusts (generalized via a new `target_state` parameter). Automatic session-transcript token mining (the amendment's second half) was left out of scope for this pass -- only the manual `frob ticket tokens` path shipped; see Filed below.
