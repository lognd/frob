bump: minor
T-3943: T-3943 (F-173): check/close/done-report hardcode "main" as diff base WHAT changed: - src/frob/check/_python.py: `_run_gates`'s `GateConfig.base` now falls back to `frob.tickets._land._resolve_default_ticket_branch(root, None)` instead of the literal "main" when the caller passes no `base`. This is the exact chokepoint the CI self-gate hits (bare `uv run frob check`, no `--base`), and where COV/PRE/SCOPE/TODO's `git merge-base HEAD main` rc=128 failure on Windows (no `main` ref in that checkout) originated. - src/frob/app/check_runner.py: added one new helper, `_check_default_base(root, cfg)` (`cfg.check_base` if given, else the same resolver), and replaced its own three independent `cfg.check_base or "main"` sites (the skip-unchanged probe, the Tier-C rerun's `GateConfig`, and the check-delta daemon RPC payload) with it. - src/frob/app/ticket_runner/_verify.py: done-report's touched-files diff (`_done_report_touched_files(root, cfg.ticket_id, base or "main")`) now falls back to the resolver instead of "main". - src/frob/app/ticket_runner/_close_cmd.py: `_close_own_obligations_for_ticket` was calling `working_diff(root, "main")` UNCONDITIONALLY -- ignoring its own `base` parameter entirely. Now uses `base or _resolve_default_ticket_branch(root, None)`. - tests/test_check_gate_base.py (new): three tests proving `_check_default_base` prefers an explicit base, falls back to the repo's real current branch (not "main") when one is checked out, and degrades to the historical "main" literal only when no branch is resolvable at all (e.g. not a git checkout). WHY: Consumer-filed F-173 (2026-09-06): a hardcoded "main" diff base buried 8 real findings under 431 false ones on a branch 250+ commits behind main's tip (`frob check --ticket T-0166` -> 439 errors; `--base sub-17-rust` -> 8, the real ones). This repo's own CI hit the same bug class from the other direction: dev run 35223985836's windows job failed with `git merge-base HEAD main` rc=128 ("Not a valid object name main") because that checkout has no `main` ref at all, cascading into COV/PRE/SCOPE/TODO all reporting "cannot be evaluated" rather than a real measurement. `frob ticket work`/evidence/done-report already solved this exact problem for their own call sites via T-4492's `_resolve_default_ticket_branch` (root's own current branch, else `[tool.frob] ticket_land_branch`, else "main" as a final, never-raising fallback) -- T-3943 itself named this as the accessor to check for and reuse rather than deriving a second answer. `frob check`, close, and done-report's own base-resolution sites had NOT been switched over to it; this change routes all four through that one existing helper (check_runner.py's three sites share one new thin wrapper, `_check_default_base`, rather than each repeating the "cfg.check_base or <resolver call>" pattern inline). How the acceptance criterion is proven: [1] bound(['tests/test_check_gate_base.py::TestCheckDefaultBase. test_explicit_check_base_wins', '...test_falls_back_to_current_ branch_not_literal_main', '...test_degrades_to_main_on_ unresolvable_branch']): `test_falls_back_to_current_branch_not_literal_main` is the direct regression proof: a synthetic repo initialized on branch "dev" (via `git init -b dev`, matching this repo's own real default branch) resolves to "dev", not "main" -- the exact fix for the CI failure mode (a `dev`-only checkout with no `main` ref). Verified locally: `PYTHONPATH=$(pwd)/src python -m pytest tests/test_check_gate_base.py -p no:cacheprovider -q` -> SUITE-RESULT: exitstatus=0 collected=3 failed=0. Also re-ran the pre-existing regression suites for every touched function to confirm no behavior change for callers that DO pass an explicit base: `tests/test_check_runner.py`, `tests/unit/test_check_scoped_files.py` (42 passed), `tests/unit/test_ticket_close_own_obligations_t1387.py`, `tests/unit/test_done_report_check_scope.py` (17 passed) -- all green, no regressions. NOT DONE (out of scope for this pass, flagged rather than silently dropped): did not touch `AppConfig.ticket_base_ref`'s own `"main"` field default or the CLI parsers' `default="main"` (T-4105's "differs from its own argparse default" sentinel logic in `_close_cmd.py`/`_verify.py` depends on that literal staying put to detect "flag omitted" vs "explicitly passed main"); did not verify with `~/bin/winrun` (no Windows mirror confirmed available this session) -- `test_falls_back_to_current_branch_not_literal_main`'s synthetic `git init -b dev` repro reproduces the exact "no main ref" condition platform-independently, which is what actually exercises the buggy code path (the bug was in Python's own default-selection, not in git's platform-specific merge-base behavior). Commits (branch t-3943, base dev): - 763f64326 fix(check): stop hardcoding main as the diff base (F-173) - 886269118 chore(tickets): accept T-3943 - 553522d3e chore(tickets): record evidence for T-3943 Tests skipped: none. Scope refusals: none. Waivers added: none.
