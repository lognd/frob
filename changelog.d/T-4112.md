bump: minor
T-4112: T-4112: H3-2 -- inbound-rate obligation (REL303) Worktree: /home/logan/projects/frob/.claude/worktrees/t-4112 HEAD sha: c4ceeeca25078a0700c997ac008a2d5aa14f311d Code commit: 1e22371ec289a397ff88003db21156d94c548827 Evidence commit: d42ad74d82a55ae61225566cee4f786f8b672bd2 WHAT changed, per file - src/frob/strata/_inbound_rate.py (new): REL303 rule. `check_inbound_rate` fires when a flow's `src` node has `trust == "foreign"` (this kernel's "no auth requirement" spelling) and its `dst` node carries a PII/ behavioral tag (`_pii.py::node_carries_pii`, the EXISTING carries-tag, reused per the ticket's own instruction rather than inventing a new one) with a declared `retention=` bound (`_compliance.py:: _retention_limit`) but `Flow.rate is None`. Deliberately its own module, not folded into `_reliability.py`'s REL200/REL201 pair (that file's own docstring scopes its dispatch instruction to rules sharing REL200/201's OUTBOUND direction; this rule reads the opposite way). - tests/unit/strata/test_inbound_rate.py (new): 6 unit tests -- must-fire (unauthenticated + retention + no rate), must-stay-quiet with a declared rate, must-stay-quiet with an authenticated route, must-stay-quiet on a non-carries store, must-not-re-fire on an outbound (export) flow, and a waiver-suppression case. SYNTHETIC FIXTURE, EXPLICITLY FLAGGED: frob's own tree has no HTTP-route/auth-posture shape to dogfood this against (frob has no HTTP routes), so every model is hand-built, not drawn from design/frob.strata. - src/frob/strata/_waive.py: added "REL303" to MULTI_INSTANCE_WAIVER_FAMILIES (a node can originate several inbound flows, so a waiver needs a REL303:<flow-id> sub-target, REL200/REL201's shape). - src/frob/strata/__init__.py: exported REL_MISSING_INBOUND_RATE, InboundRateReport, InboundRateViolation, check_inbound_rate. - src/frob/app/sys_runner.py: wired check_inbound_rate into `frob sys audit`'s `_run_audit`, folding its findings into the SAME combined ReliabilityReport `_print_reliability_report` already prints (its violation shape is field-identical to ReliabilityViolation) -- no new printer/report pair for a single-rule module. - docs/strata/reliability.md: new "REL303: inbound-rate obligation (T-4112, H3-2)" section (surface vocabulary, grammar-ceiling note, waiver channel). - docs/design/registry/check-coverage.yaml: added CHK-GATE-REL303 (handled_by:REL303), matching the frob:enforces directive on check_inbound_rate. How each acceptance criterion is proven - "a rule requiring an inbound rate on an unauthenticated write into a carries-bearing store with retention but no rate": proven by test_unauthenticated_write_with_retention_but_no_rate_fires (must-fire) plus test_declared_rate_stays_quiet, test_authenticated_route_stays_ quiet, test_non_carries_store_stays_quiet (the three negative controls matching the ticket's own fixture spec, including the authenticated- route must-stay-quiet case). - "must not duplicate REL201 or re-fire on an outbound flow": proven by test_outbound_flow_from_store_never_fires (a flow FROM the carries- bearing node, matching REL201's own direction, produces zero findings). - Fixture synthetic, not drawn from frob's own dogfood surface: stated explicitly in both the module and test-file docstrings (frob has no HTTP routes to model this against). Test node ids (evidence-bound, T-4112) tests/unit/strata/test_inbound_rate.py::TestMissingInboundRate::test_unauthenticated_write_with_retention_but_no_rate_fires tests/unit/strata/test_inbound_rate.py::TestMissingInboundRate::test_declared_rate_stays_quiet tests/unit/strata/test_inbound_rate.py::TestMissingInboundRate::test_authenticated_route_stays_quiet tests/unit/strata/test_inbound_rate.py::TestMissingInboundRate::test_non_carries_store_stays_quiet tests/unit/strata/test_inbound_rate.py::TestMissingInboundRate::test_outbound_flow_from_store_never_fires tests/unit/strata/test_inbound_rate.py::TestMissingInboundRate::test_waived_finding_is_suppressed Gates - ruff check + ruff format: clean on every touched file. - ty check: clean on every touched file. - Targeted pytest (test_inbound_rate.py + test_reliability.py + test_pii.py + test_waive.py + test_compliance.py + test_outbound_ destination.py): 144 passed, 0 failed. - `frob check --only sys --files <touched>`: SELFAUDIT001 fires on src/frob/strata/_outbound_destination.py's read_text() call (T-4113's file, same finding, see below) -- nothing attributable to _inbound_rate.py itself (it is purely declarative, no fs/net capability use). - `frob check --only arch --files <touched>`: pass (0 attributable warnings; only whole-tree pattern-recommendations, pre-existing). Filed: none (no out-of-scope work found). KNOWN GAP, explicitly flagged: docs/modules/gates.md's SYS/REL rule-row table was NOT updated for REL303 -- `frob ticket scope T-4112 --add docs/modules/gates.md` was refused (T-4111 holds a live cross-worktree lease on that path). check-coverage.yaml's CHK-GATE-REL303 entry was added instead (in scope), but the human-readable gates.md table still needs a REL303 row once T-4111's lease frees. Gates: frob check --only sys/arch clean for this ticket's own files (coverage/DSL/DRIFT/WAIVE FAILs observed in the same run are pre-existing, unrelated files -- see T-4113's Done report for the one shared finding that IS attributable, on _outbound_destination.py). ## UPDATE 2026-09-19 (T-4221 lease released -- registry work completed) T-4221 landed on dev (f27804db4), releasing its lease on design/frob.strata and docs/design/registry/capability-via-ratchet.lock.json. What changed in this follow-up pass (worktree /home/logan/projects/frob/.claude/worktrees/t-4112, shared with T-4113, code commit df4d54b92): - `git merge dev --no-edit`: clean, no conflicts (dev had moved 137 files since the last merge base; nothing in this ticket's own touched set overlapped). - `frob ticket scope T-4112 --add design/frob.strata --add docs/design/registry/capability-via-ratchet.lock.json --add docs/design/registry/check-coverage.yaml --reason "leases released"`: succeeded. `docs/modules/gates.md` was NOT added -- it is still leased by a DIFFERENT in-progress ticket, T-draft-a62505d4 ("Registry-file class: append-shared, not whole-file leases", worktree t-draft-aefc4511), which T-4111 evidently handed the lease to after landing. Per the standing brief, left untouched; the coordinator needs to sequence a gates.md REL303 row addition after T-draft-a62505d4 releases it, or have that ticket's own worktree add the row. - design/frob.strata: added `frob:ticket T-4113` to both the `stratamod` and `testsuite` node headers (COV002: "node changed with no frob:ticket edge to an open ticket" -- the via-list edits below are the change these bind). - docs/design/registry/capability-via-ratchet.lock.json: bumped `entries."stratamod::fs.read"` 12 -> 13 and `entries."testsuite::net.connect"` 4 -> 6, both with reasons naming T-4113 and the exact new via-list sites. NOTE for whoever next edits this file: it carries duplicate top-level flattened keys (e.g. a top-level `"stratamod::fs.read"` alongside `entries."stratamod::fs.read"`) that are stale/unused by the actual ratchet check (confirmed by reading `capability_via_site_counts` in `_effects.py` -- it only reads `model`-derived grants, and the SELFAUDIT001 finding's printed ceiling matched the `entries.*` value, not the top-level one). Did not touch the stale top-level keys (out of this ticket's scope; flagging so nobody edits the wrong copy). - src/frob/strata/_waive.py: added `frob:ticket T-4112` / `frob:ticket T-4113` directly above `MULTI_INSTANCE_WAIVER_FAMILIES` (COV002: the constant's value changed -- REL303/SYS114/SYS115 joined the set -- with no frob:ticket edge bound). Re-measured (see "Pre-READY checks" below): the 2 SELFAUDIT001 findings from the prior Done report are gone; the 3 COV002 findings (stratamod, testsuite, MULTI_INSTANCE_WAIVER_FAMILIES) are gone. Remaining COV/DRIFT/DSL/TODO findings in the same `--only coverage`/`--only sys` runs are pre-existing and NOT attributable to any file this ticket touches (checked by name against every touched path). Evidence: re-bound (idempotent, same 6 node ids, now current against commit df4d54b92) via `frob ticket evidence T-4112 <node-id>...` with no `--accepts` (T-4112 declares 0 formal acceptance items; the flat evidence list is the only binding this ticket type has). Filed: none new this pass. T-draft-a62505d4's live lease on docs/modules/gates.md is a sequencing note for the coordinator, not a new defect. ## Pre-READY checks `frob check --only sys --files src/frob/strata/_outbound_destination.py --files src/frob/strata/_inbound_rate.py --files tests/unit/strata/fixtures/outbound_destination/hardcoded.py --files tests/unit/strata/fixtures/outbound_destination/configured.py --files tests/unit/strata/fixtures/outbound_destination/config.py --files design/frob.strata --files docs/design/registry/capability-via-ratchet.lock.json --base dev`: pass gate:DOCARCH 0 errors, 549 warnings, 0 unresolved, 0 waived FAIL gate:DRIFT 6 errors, 0 warnings, 0 unresolved, 5 waived (pre-existing, none of the 6 touch this ticket's files) FAIL gate:DSL 1 error, 0 warnings, 0 unresolved, 0 waived (tests/test_app.py:387, pre-existing, unrelated file) pass gate:PROFILE 0 errors, 0 warnings, 0 unresolved, 0 waived pass gate:WAIVE 0 errors, 8 warnings, 0 unresolved, 0 waived -- SELFAUDIT001 on stratamod::fs.read / testsuite::net.connect: GONE (was FAIL 2 errors before the via-grant + ratchet-lock bump; gate:SELFAUDIT does not appear in this run's Tool summary at all now) `frob check --only arch --files design/frob.strata --files docs/design/registry/capability-via-ratchet.lock.json --files docs/design/registry/check-coverage.yaml --base dev`: pass frob-arch 20 warnings (36 waived), 547 suggestions (all whole-tree pattern-recommendations, none naming a file this ticket touches) `frob check --only coverage --files src/frob/strata/_outbound_destination.py --files src/frob/strata/_inbound_rate.py --files src/frob/strata/_waive.py --files design/frob.strata --files docs/design/registry/capability-via-ratchet.lock.json --files docs/design/registry/check-coverage.yaml --base dev`: FAIL gate:COV 10 errors, 123 warnings, 0 unresolved, 278 waived (down from 13 errors; the 3 that dropped were design/frob.strata:570 stratamod, design/frob.strata:1629 testsuite, and src/frob/strata/_waive.py:159 MULTI_INSTANCE_WAIVER_FAMILIES COV002 findings -- all fixed by the frob:ticket directives added this pass. Remaining 10 errors are in unrelated files: src/frob/strata/_effects.py, src/frob/strata/_unity_asmdef.py, src/frob/vet/_source.py, src/frob/vet/_typosquat.py -- none touched by T-4112/T-4113) FAIL gate:DRIFT 6 errors (same pre-existing set as above) FAIL gate:DSL 1 error (same pre-existing finding as above) FAIL gate:TODO 5 errors, 0 warnings, 0 unresolved, 0 waived (pre-existing, none in touched files -- checked by name) pass gate:PLACE 0 errors, 0 warnings, 0 unresolved, 2 waived `ruff check src/frob/strata/_waive.py`: All checks passed! `ruff format --check src/frob/strata/_waive.py`: 1 file already formatted `ty check src/frob/strata/_waive.py`: All checks passed! design/frob.strata parses clean: `parse_module(open('design/frob.strata').read())` -> is_ok Targeted pytest: `tests/unit/strata/test_outbound_destination.py tests/unit/strata/test_inbound_rate.py` -> SUITE-RESULT: exitstatus=0 collected=13 failed=0 Cross-ticket check: `git diff --name-only dev...HEAD` for this ticket's own new commit (df4d54b92) touches only design/frob.strata, docs/design/registry/capability-via-ratchet.lock.json, src/frob/strata/_waive.py -- all three now in T-4112/T-4113's scope (added this pass), none leased by another in-progress ticket (verified via `grep -l` over .git/frob-leases/*.json).
