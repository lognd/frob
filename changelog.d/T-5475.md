bump: minor
T-5475: CI run 35951365410 caught the WEBSEC120 MUST-FIRE positive control firing zero findings. Root cause: _CONTENT_DISPOSITION_RE used [^\n]* between "Content-Disposition" and "filename=", so it only matched when both appeared on the same source line. The fixture's assignment (and any real repo code wrapped by ruff format's line-length limit) puts the f-string on its own line, so the pattern never matched. Widened the gap to a bounded [\s\S]{0,N}? window so the rule still fires across a wrapped assignment; the existing positive/negative fixtures are the regression coverage, no new fixture needed.
