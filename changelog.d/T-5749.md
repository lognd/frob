bump: minor
T-5749: Ledger tiers (A1) needed a milestone tier and a story-flavour concept before the closer/roll-up leaves (A2-A4, B1-B6) can build on top of them. Added TicketTier.MILESTONE and StoryFlavour (user_story|quality_objective), with a real model_validator on both Ticket and TicketSpec enforcing flavour is only legal on tier=story -- strict at both construction and ledger-load time since this is a brand-new field with no historical ledger rows to stay lenient for (unlike milestone/points). Docs anchor updated in the same change so the model stays the single source of truth.
