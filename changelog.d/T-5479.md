bump: minor
T-5479: CI run 35951365410's windows-latest job failed 4 tests in tests/test_hook_frob_suggest.py, all relying on the hook's dedup/ escalation counter across sequential sibling `subprocess.run` calls with no session_id passed. Root-caused directly: `_session_key`'s fallback (`os.environ.get("FROB_AGENT") or f"ppid:{os.getppid()}"`) reads `os.getppid()`, measured on the real Windows mirror to return a DIFFERENT value across two sequential `subprocess.run` calls from the same long-lived parent process -- confirmed with an isolated repro script (two sibling subprocess calls, printed distinct ppids neither matching the real parent's own pid). Real Claude Code hook invocations always supply `session_id` in the payload (per this test file's own docstring); threaded a consistent `session_id` through the 4 affected tests (and added `session_id` support to `_run_edit_hook`, which only had it on `_run_hook` before) to match that production shape instead of exercising the Windows-unstable ppid fallback. Verified on the real Windows mirror (winrun): before the fix, all 4 failed with the exact CI symptom ("BLOCKED ONCE" on a second/third call that should have passed through); after, the whole file (59/59) passes, confirmed on two consecutive Windows runs. Also 59/59 on Linux, no regressions.
