bump: minor
T-5518: Post-merge evidence re-verification runs pytest against the worktree right after _land_merge_stage's merge, but the T-1213 stale-natives auto-rebuild had no call site between that merge and the evidence spawn -- only pre-squash (run_gates) and a best-effort Tier-A gate (_worktree_natives_verifiably_healthy). A land whose own branch adds Rust source (T-3010's milestone_closure_check export) could therefore land its evidence tests failing "individually" against a worktree extension built before the export existed, even though the tests themselves were correct (measured: /tmp/land-T-5464.log ~line 24069, same tests passed after a manual frob natives build with no source change). Fix adds a third T-1213 call site, _rebuild_stale_worktree_natives, invoked from _reverify_evidence_post_merge immediately before it spawns the evidence subprocess, logging resolved interpreter/extension paths at INFO for diagnosability. This pass also canonicalizes stale T-5516 ticket-id references (from an earlier renumber) to the current id T-5518, and re-binds the two evidence ids that did not carry over from the prior draft/renumbered state.
