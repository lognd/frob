bump: minor
T-5351: CSRF and session lifecycle had no static coverage in the WEBSEC family: GET routes could mutate state and bypass CSRF, session validity could be checked client-side only, logins could skip session-id rotation and logouts could leave sessions valid server-side, and cookies could ship with no SameSite/CSRF middleware/idle/absolute timeout configured. This closes that gap with eight WARN-tier rules (WEBSEC201-208), four handler- shape text scans and four built directly on T-5349's normalized SessionConfig reader (no re-parsing). Account-enumeration-via-error-text has no id left in this reservation and is filed as follow-up.
