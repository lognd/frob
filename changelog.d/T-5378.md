bump: minor
T-5378: The real _ticket_dispatch_table() now has 'points'/'set'/'tokens' (added by T-5132/T-4696-era work), each already using the GENERIC_COMMIT_MIRRORED ledger-write strategy, but none of the three had a classification entry in test_dispatch_table_verbs_are_all_accounted_for's tracking buckets. Fix: add all three to _MUTATING_VERB_INVOCATIONS with a minimal real invocation each (points sets ticket_points_value, tokens sets ticket_tokens_in, set exercises the folded milestone field since it needs no --reason) so the "leaves the repo clean" guard actually runs them instead of silently missing new verbs.
