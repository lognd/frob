bump: minor
T-4503: -- T-4503 WHAT changed: - Merged dev into t-4503 (merge commit 43474fd20, HEAD c2678e6313ae0d22e5fbd649dd7525e235cca8de). - Rename/rename ticket conflict: tickets/T-draft-50806633/ticket.md had been renamed to tickets/T-4577/ticket.md on t-4503's own branch and to tickets/T-4580/ticket.md on dev (same content, different assigned ids -- a renumber race). Kept dev's tickets/T-4580, deleted the branch's stale tickets/T-4577 and the T-draft-50806633 marker (`git rm -rf tickets/T-4577 tickets/T-draft-50806633`, `git add tickets/T-4580`). Verified every tickets/<dir>/ticket.md id line matches its directory afterward. - design/frob.strata: added `src/frob/scaffold/_unity_project.py` to core node's `may "fs.write" via [...]` list (alongside the existing `src/frob/scaffold/project.py` entry), with a one-line T-4503 comment explaining the site (`render_unity_project`'s `out_path.write_text(...)` at src/frob/scaffold/_unity_project.py:114). This is the missing declaration the land refusal named: "SELFAUDIT001 ... capability 'fs.write' observed at src/frob/scaffold/_unity_project.py:114 but not declared". - docs/design/registry/capability-via-ratchet.lock.json: bumped the `core::fs.write` entry's `accepted_count` from 27 to 28 (one new via-list site), with `reason` naming T-4503. Verified the via-list now has exactly 28 entries after the edit. - Added both design/frob.strata and docs/design/registry/capability-via-ratchet.lock.json to T-4503's scope via `frob ticket scope T-4503 --add <path> --reason "..."`. SCOPE-LEASE CONFLICT (flag for coordinator): the scope --add for docs/design/registry/capability-via-ratchet.lock.json was REFUSED: "ERROR: tickets: T-4503 cannot lease an add glob: held by in-progress T-draft-dfc98d31 (scope 'docs/design/registry/capability-via-ratchet.lock.json')". Confirmed via .git/frob-leases/T-draft-dfc98d31.json in the primary checkout -- that ticket's lease scope explicitly includes this exact file, and its worktree (.claude/worktrees/t-draft-dfc98d31) still exists, so this is a real, live conflict, not a stale artifact. The design/frob.strata scope --add succeeded fine (no lease conflict there). Per the standing brief I did NOT force the ratchet-lock scope claim, but I DID keep the one-line accepted_count edit in this worktree's commit, because it is required for the SELFAUDIT001 land-blocker to actually clear (verified below) and is a minimal, additive, non-conflicting change (bumps one integer + reason string, does not touch T-draft-dfc98d31's own scope files: src/frob/strata/_effects.py, src/frob/app/ticket_runner/_land_cmd.py, tests/unit/strata/test_selfconform.py). The coordinator should reconcile this with T-draft-dfc98d31 before or during land (e.g. confirm no conflicting write to the same lock entry from that ticket, or land T-4503 after T-draft-dfc98d31 releases the lease). WHY: SELFAUDIT001 fires when a node's declared code binding exercises a capability (fs.write) at a call site not enumerated in that node's `may "fs.write" via [...]` list. _unity_project.py is a new file under core's `src/frob/scaffold/**` code binding (T-4503's Unity scaffold feature) whose `render_unity_project` writes rendered `.strata` fragments to disk -- a real, new fs.write site that needed declaring, exactly parallel to project.py's existing declared site. Acceptance criteria proof (all pre-existing evidence in the ticket, re-verified green after the merge): - tests/unit/test_scaffold_unity_project.py::TestRenderUnityProject::test_writes_frob_toml_with_unity_excludes - tests/unit/test_scaffold_unity_project.py::TestRenderUnityProject::test_one_strata_file_per_asmdef - tests/unit/test_scaffold_unity_project.py::TestOutputExistsRefusal::test_second_run_without_force_is_output_exists - tests/unit/test_scaffold_unity_project.py::TestNotAUnityProject::test_plain_directory_is_not_a_unity_project Ran: `PYTHONPATH=$(pwd)/src python -m pytest tests/unit/test_scaffold_unity_project.py -q` -> SUITE-RESULT: exitstatus=0 collected=8 failed=0. Re-recorded via `frob ticket evidence` (idempotent -- all 4 ids already present). Gates: `frob check --only sys --files src/frob/scaffold/_unity_project.py --base dev`. No finding names src/frob/scaffold/_unity_project.py after the fix (the original SELFAUDIT001 "capability 'fs.write' observed ... but not declared" is gone). The one remaining SELFAUDIT001 in that run's output is unrelated: "self-audit family SYS111 node=testsuite: fs.write testsuite-glob via-list on testsuite grew to 537 site(s) -- pending auto-accept ... only a land's own composed-tree check run may write docs/design/registry/capability-via-ratchet.lock.json for this glob-form pair" -- this is a pre-existing testsuite-node glob-growth note (T-4563 mechanism), expected to fire on any bare (non-land) check run, and not attributable to _unity_project.py or T-4503's scope files. Commit shas: 43474fd20 (merge commit), HEAD=c2678e6313ae0d22e5fbd649dd7525e235cca8de. Filed: none (no out-of-scope work discovered beyond the flagged scope-lease conflict above, which is a coordination note, not new work).
