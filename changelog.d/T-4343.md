bump: minor
T-4343: Changed: src/frob/gates/__init__.py::_repo_state_fingerprint src/frob/gates/__init__.py::_load_graph_queue_lock Investigation, resolving the ticket's contradiction: - Ruled out (already known, re-confirmed by reading): the gate-result cache and the invariant registry are not involved. - Instrumented the actual transaction boundaries (not the docstring): T-4282 already made _ingest_source_files/_ingest_doc_files commit every 200 files, which contradicts frob.graph.cache.store_file_data's own stale docstring claim of a single whole-build commit. So the torn-snapshot MECHANISM is real at the code level, not merely a docstring claim. - Reproduction: 5-way concurrent `frob check --only gates` against one fixed, unchanged commit, run twice (once warm-cache, once forcing a full concurrent rebuild from a cold cache.db), inside one isolated worktree with nothing else touching it -- both runs produced IDENTICAL results across all 5 processes (0 errors, no "drifted from cache" warnings). The pure sqlite-cache race, in isolation, does not reproduce the divergence. - Conclusion: the divergence's actual trigger is a torn REPO WALK, not a torn cache transaction -- a land or a sibling agent's edit landing in the shared primary checkout WHILE build_graph's own walk+parse is in flight, matching the directly-observed evidence ("drifted from cache" warnings naming files other agents were actively touching). Fix: `_load_graph_queue_lock` now brackets `build_graph` with `_repo_state_fingerprint` (git HEAD sha + working-tree status, excluding `.frob/` so the cache's own build artifacts never trip it) and reports the ALREADY-EXISTING `GateError.GraphUnavailable`/GATES001 unmeasured outcome when the repo moved mid-build, instead of trusting a snapshot that may mix pre- and post-change reads. No serialization added anywhere; a stable repo is completely unaffected (regression test `test_stable_head_ok`). Evidence: tests/gates_suite/test_run.py::TestGraphFP::test_head_move_unmeasured tests/gates_suite/test_run.py::TestGraphFP::test_stable_head_ok tests/gates_suite/test_run.py::TestRunGatesQueueFailureThreadsRealTicketError::test_duplicate_id_across_active_and_archive_surfaces_as_ticketerror Filed: none Gates: frob check --ticket T-4343 clean (0 errors); frob test --base main green (4 python tests)
