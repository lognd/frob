bump: minor
T-5396: Checked T-5107 and T-5214 first, per this ticket's own body. T-5214 described exactly 3 of the 8 findings (narrative-node SYS003/SYS100 testsuite/SYS110) -- did the identical fix directly in this ticket (T-5214 remains queued/unclaimed and now describes an already-fixed state; recommend dropping it as superseded, not closed by me). T-5107 (PYTHONPATH leak) does NOT reproduce with a clean shell -- re-verified the scaffold_dx failures happen even with an empty PYTHONPATH, so it is NOT the cause of my 2 scaffold_dx findings; found and filed the REAL, distinct root cause instead (T-draft-4bdecb8f). Fixed 5 of the 8 singles by adding real design/frob.strata declarations (read each finding's exact violation text, verified the underlying code before writing any declaration -- fetch_url traced to doctor.py::_fetch_pypi_latest_version/_fetch_npm_latest_version; html_render traced to literal-substring self-matches on innerHTML/dangerouslySetInnerHTML/v-html inside comments/docstrings/test fixtures describing the T-5141-2 WEBSEC/A11Y rules these walkers feed, never a real render call): - tests/unit/strata/test_sys003_calibration.py::...::test_sys003_zero_against_live_repo_design - tests/unit/strata/test_conform_eval_needle.py::...::test_real_repo_design_selfconform_has_no_eval_gap - tests/unit/strata/test_selfconform.py::TestRealGateGreen::test_repo_design_and_declarations_are_self_conformant - tests/unit/strata/test_selfconform.py::TestCoverageTotality::test_repo_unrestricted_scan_is_clean - tests/unit/strata/test_packs.py::...::test_trusted_component_without_pack_gets_it_injected (was already passing on current dev tip, no code change needed for this one -- just re-verified) The remaining 3 singles are NOT fixed by this ticket, each for a distinct, disclosed reason -- not silently dropped: 1. tests/system/test_frob_self_model.py::...::test_sys_gate_zero_violations -- shares design/frob.strata with a PRE-EXISTING SYS119 templated- assume WARN debt (35 node-instances across 6 CWE groups; 34 of them predate this diff entirely, my own new fetch_url/html_render declarations added a 3rd CWE-79 group and grew CWE-918 by one member). The zero-tolerance assertion (`violations == ()`) cannot go green without also rewriting 35 assume ids with genuinely distinct, module-specific mechanism text (find_templated_assumes tokenizes the PARSED claim id/body/owner/review fields, never the // prose comment -- confirmed empirically: my first attempt at a "distinct" prose comment on identically-shaped assume bodies still collapsed into a template group). That is a large, separate burn-down, out of proportion to this ticket -- filed as T-draft-893a721f with the exact mechanism and a caution about a DOC003 discharge-matching trap I hit (a suffixed claim id can silently stop discharging its THREAT003 obligation -- re-verify the join before assuming a suffix is free). 2 & 3. tests/system/test_scaffold_dx.py's two params (which fail via a nested pytest run inside the scaffolded demo project) and tests/integration/test_logging_integration.py (which does not exist as a standalone test in this repo -- it is src/frob/scaffold/data/shared/python/tests/integration/ test_logging_integration.py.j2, a scaffold TEMPLATE rendered into every scaffolded project, and its failure inside the nested run is exactly what fails the two scaffold_dx params) -- all three collapse to ONE real, newly-diagnosed root cause: the scaffold's logger.py.j2::_init() runs logging.config.dictConfig ONCE per process (module-level cache flag), so the StreamHandler binds to whichever sys.stdout OBJECT is live the first time ANY test in a session calls get_logger() -- pytest's capsys installs a NEW capture object per test, so only the FIRST caller's own capsys sees the handler's writes; every later test's capsys.readouterr() sees nothing even though the log line genuinely printed (visible in pytest's own separate "Captured log call" section). Confirmed this is NOT T-5107's PYTHONPATH-leak (re-verified with an empty PYTHONPATH, the failure still reproduces). Filed as its own ticket (T-draft-4bdecb8f) with the full diagnosis rather than attempted here -- fixing a shared scaffold template correctly needs verifying against a fresh scaffold + multi-test pytest run, more than this pass's remaining scope. Hand-checked the two things the dry-run does not measure (T-draft- c7aa1ed2): this diff is design/frob.strata declarations only (no new file-reading module, no docs touched).
