bump: minor
T-4212: -- T-4212 gates evaluate main only: extend SYS/capability audits to run against a worktree/branch, and treat an unrunnable check as unmeasured, not clean ### WHAT changed, per file - src/frob/gates/_sys_branch.py (NEW): `sys_gate_for_branch(repo_root, ref)` -- the new explicit worktree/branch entry point for the SYS/capability audit family. Resolves `ref` (a branch name, or a worktree directory name under `repo_root`) to its already-checked-out git worktree via `_resolve_ref_worktree` (parses `git worktree list --porcelain`), builds a fresh throwaway `GraphSnapshot` scoped to that worktree, and calls the existing `frob.gates._sys.sys_gate` against it. Reports a single SYS900 `Severity.UNRESOLVED` violation (`_sys900_unmeasured`) -- never an empty tuple -- when `ref` cannot be resolved to a worktree, or its snapshot build fails: an unrunnable check is UNMEASURED, not indistinguishable from "ran clean". Split into its own module (not left in _sys.py) because the addition pushed _sys.py past the 800-line LARGE001 threshold; this follows the same one-family-per-land split _sys_selfaudit.py (T-1420) already established for this family. Net effect on _sys.py itself: zero (added then moved back out, so its own diff is empty). - src/frob/gates/_waive.py: added `"SYS900"` to `_KNOWN_GATE_RULES` (GATERULE001's registry) with a `# frob:ticket T-4212` marker, so the new rule id is registered and waivable like any other. - tests/gates_suite/test_sys.py: new `TestSysGateForBranch` class, 3 tests (below). ### WHY SYS/capability audits (`sys_gate` and the SELFAUDIT001/capability-ratchet surface it composes) previously only ever evaluated whatever root a caller's own `GraphSnapshot` happened to be built from -- historically always the primary checkout's current branch, `main`. Code that exists only on a parked/feature branch was therefore structurally invisible to every SYS/capability gate: a capability grant added or removed there passed with zero signal either way (F-317/M-1, addendum F-039-b/T-0297). `sys_gate_for_branch` makes "run this audit against an explicit worktree/branch" a first-class, accepted entry point rather than a workaround, and makes the failure-to-run case (no such worktree, or a snapshot build failure) loudly UNMEASURED via the new SYS900 rule instead of silently returning `()` (which is indistinguishable from a real clean scan). ### How each acceptance criterion is proven (ticket body: "Fixture-testable: YES -- create a branch/worktree in frob's own tree with a new capability and confirm SYS gates see it only when pointed at it") 1. `TestSysGateForBranch.test_finds_findings_only_visible_on_branch` -- creates a real git repo + a real `git worktree add` checkout of a `feature` branch, adds a design/*.strata file and a dangling `frob:channel` directive ONLY on that branch's worktree (main's own tree has neither). Asserts `sys_gate(repo, ...)` (the existing, unmodified entry point, root = the primary checkout) sees NOTHING (`SYS001` findings == []) -- reproducing the exact blind spot this ticket names -- then asserts `sys_gate_for_branch(repo, "feature")` DOES see the SYS001 violation. This is the positive-control proof that an explicit worktree/branch audit now sees code invisible to the existing main-only path. 2. `TestSysGateForBranch.test_missing_worktree_reports_unmeasured` -- a bare repo with no worktree for the given ref: asserts exactly one SYS900 `Severity.UNRESOLVED` violation naming UNMEASURED, not an empty tuple. 3. `TestSysGateForBranch.test_snapshot_build_failure_reports_unmeasured` -- monkeypatches `_resolve_ref_worktree` to succeed but `build_graph` to return `Err(...)`: asserts the same SYS900 UNRESOLVED verdict, so a snapshot-build failure is distinguished from a clean pass too. ### Test node ids (evidence) - tests/gates_suite/test_sys.py::TestSysGateForBranch::test_finds_findings_only_visible_on_branch - tests/gates_suite/test_sys.py::TestSysGateForBranch::test_missing_worktree_reports_unmeasured - tests/gates_suite/test_sys.py::TestSysGateForBranch::test_snapshot_build_failure_reports_unmeasured All 78 tests in tests/gates_suite/test_sys.py pass (verified locally, PYTHONPATH=<worktree>/src pytest, serial). ruff check/format and `ty check` clean on all touched files. ### Pre-ready checklist results 1. `frob check --only sys --files src/frob/gates/_sys.py --files src/frob/gates/_sys_branch.py --files src/frob/gates/_waive.py --files tests/gates_suite/test_sys.py --base dev`: 0 SELFAUDIT001/SYS findings attributable to my files (5 repo-wide errors present are pre-existing drift owned by other in-flight tickets: T-3233's _cli_parsers drift, T-4335's rapid_sweep drift, T-4563's SYS111 auto-accept pending state, tests/test_app.py's own pre-existing malformed directive). 2. `frob check --only arch --files ...`: 0 errors, 19 warnings (repo-wide baseline, none new). `_sys.py` large-file warning that appeared after the first draft (885 > 800 lines) is now gone after the _sys_branch.py split (791 lines); `_sys.py` high-coupling (11 imports) is a PRE-EXISTING repo-wide warning (was already 12 imports on dev, confirmed against the pre-split check run), not introduced here. 3. `frob check --only coverage --files ...`: 0 COV002/COV006/COV007 errors attributable to my files (fixed COV002 on `_sys900_unmeasured` and `_KNOWN_GATE_RULES` by adding `# frob:ticket T-4212`; fixed DOCARCH001 change-narrative warnings on `sys_gate_for_branch` and the `TestSysGateForBranch` docstrings by rewording to utility-first prose). Remaining COV/DOCARCH findings in the log are pre-existing, attributed to files/symbols this ticket never touched (docptr_findings_touching, TestGateOrderSetEquality, test_waive.py, test_dsl_markdown_waive.py). 4. `ty check` + `ruff check`/`ruff format --check`: clean on all 4 touched files. 5. Cross-ticket: `git diff --name-only dev...HEAD` = _sys_branch.py (new), _waive.py, tests/gates_suite/test_sys.py, tickets/T-4212/ticket.md (_sys.py's own net diff is empty). None of the 3 code files are leased by any other in-progress ticket (`grep -l` over .git/frob-leases/*.json came back empty for all three). 6. kind=feature, not bug -- BUG002 repro-designation N/A. 7. tickets/T-4212/ticket.md's `id:` == T-4212; no stray draft dirs. 8. Evidence bound AFTER the last commit (ab0303750, the _sys_branch.py split) -- no commit followed evidence binding. ### Commits (this worktree, dev..HEAD) - 95580540f chore(tickets): record T-4212 start transition - db24b0ec8 chore(tickets): scope T-4212 (+_waive.py) - ab22f4f88 feat(gates): add sys_gate_for_branch, SYS900 UNMEASURED verdict (T-4212) - c5e86ecbb chore(tickets): scope T-4212 (+_sys_branch.py) - ab0303750 refactor(gates): split sys_gate_for_branch into _sys_branch.py - c78c7c338 chore(tickets): record evidence for T-4212 ### Scope notes for the coordinator - Added `src/frob/gates/_waive.py` and `src/frob/gates/_sys_branch.py` to scope beyond the ticket's original `src/frob/gates/_sys.py` (both via `frob ticket scope T-4212 --add`, reasons recorded in the ticket's own `scope_changes`). Neither was leased by another in-progress ticket. - Wanted to add `docs/modules/gates.md` to scope (every sibling symbol in this family points its `frob:doc` there) but it is LIVE-leased by T-4111 -- refused by the scope-lease guard. Worked around this by pointing the new symbols' `frob:doc` at the FAMILY'S EXISTING anchor (`docs/modules/gates.md#self-audit-at-land-selfaudit001-t-0756`, already used by 4 other symbols in this same family with no per-symbol doc-file edit) rather than editing the doc file. No new prose was added there; flag this to whoever lands T-4111 in case a doc mention of sys_gate_for_branch would help future readers, but it was not required by any gate. - No tests skipped. No waivers added.
