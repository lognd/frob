bump: minor
T-4556: T-4556 -- classify renumber/promote/archive/migrate as whole-land WHAT changed (per file, already committed by the prior agent, verified and finished by this pass): - src/frob/app/ticket_runner/__init__.py Added `_LAND_WHOLE_LAND_VERBS = frozenset({"renumber", "promote", "archive", "migrate"})` and wired `_refuse_if_land_in_progress_for_ dispatch` to pass `whole_land=command in _LAND_WHOLE_LAND_VERBS` to `refuse_if_land_in_progress`. Every other mutating verb keeps `whole_land=False` (T-3612's narrowed splice-only probe, unchanged). - src/frob/tickets/_leases.py Added a `whole_land: bool = False` parameter to both `refuse_if_land_in_progress` and `_probe_land_once`. When `whole_land=True`, `_probe_land_once` reverts to the pre-T-3612 `_land_flock_probe` (`land.lock`, held for a land's entire duration) instead of the splice-only `_ledger_splice_flock_probe` (`tickets.lock`'s short critical section). `exclude_pid` is honored on this path. - tests/unit/test_land_in_progress_window.py Added `TestWholeLandVerbClassification` (2 tests, at the `refuse_if_land_in_progress` unit level: a whole-land-classified verb is refused while only `land.lock` is held; a splice-only verb succeeds in that exact same scenario) and `TestDispatchLayerWholeLandClassification` (2 tests, end to end through `_refuse_if_land_in_progress_for_dispatch`: `renumber` exits 1, `evidence` proceeds, same scenario). WHY: T-3612 narrowed `refuse_if_land_in_progress` to probe only `tickets.lock`'s short splice window -- correct for every verb that commits through `_add_and_commit_tickets_md`/`commit_ticket_ledger_ change`, but WRONG for `renumber`/`promote`/`archive`/`migrate`, which own their own multi-file transaction across many ticket files with no single `tickets.lock` span covering it (T-1615 deliberately excludes them from the uniform auto-commit). The narrowed probe let those four verbs interleave with a land's out-of-tree compose for their ENTIRE multi-file duration instead of a few seconds -- exactly the T-1779/ T-1678 corruption class this guard exists to close. This pass's own work (finishing what the single existing commit left undone): 1. Merged dev into the worktree (`git merge dev --no-edit`) -- clean, no conflicts, only unrelated CLI-parsers/docs/ticket-ledger files from other landed tickets. 2. Ran the bound test file end to end: `PYTHONPATH=<worktree>/src python -m pytest tests/unit/test_land_in_progress_window.py -p no:cacheprovider -q` -> SUITE-RESULT: exitstatus=0 collected=10 failed=0 (all 10 tests in the file, including the 4 new T-4556 ones). 3. Ran `frob check --only gates --files src/frob/app/ticket_runner/__init__.py,src/frob/tickets/_leases.py, tests/unit/test_land_in_progress_window.py --base dev` -- no findings referencing any of the 3 touched files anywhere in the output (all FAIL rows in the per-category gate summary are pre-existing repo-wide debt, unrelated to this diff). 4. Rebuilt the pytest collection cache (`frob test --collect`, 14425 node ids) and bound the ticket's single acceptance criterion, which the prior agent had left UNBOUND, to all 4 relevant test node ids: tests/unit/test_land_in_progress_window.py::TestWholeLandVerbClassification::test_renumber_refused_while_only_land_lock_held tests/unit/test_land_in_progress_window.py::TestWholeLandVerbClassification::test_splice_only_verb_allowed_while_only_land_lock_held tests/unit/test_land_in_progress_window.py::TestDispatchLayerWholeLandClassification::test_renumber_exits_while_only_land_lock_held tests/unit/test_land_in_progress_window.py::TestDispatchLayerWholeLandClassification::test_evidence_proceeds_while_only_land_lock_held (`frob ticket evidence T-4556 <node-id> --accepts 1 --base-ref dev`, one call per node id.) How the acceptance criterion is proven: [1] GIVEN a land in progress (land.lock held, tickets.lock free) WHEN frob ticket renumber, promote, archive or migrate is invoked THEN it is refused naming the land, while new/drop/body/scope/fail/ evidence/done-report/accept succeed. Proven at both layers: - `refuse_if_land_in_progress` unit level: TestWholeLandVerbClassification::test_renumber_refused_while_only_land_lock_held (whole_land=True refuses in that scenario) TestWholeLandVerbClassification::test_splice_only_verb_allowed_while_only_land_lock_held (whole_land=False, the unaffected splice-only path, succeeds in the identical scenario) - dispatch-layer end-to-end level: TestDispatchLayerWholeLandClassification::test_renumber_exits_while_only_land_lock_held (renumber, one of the 4 classified verbs, dispatched through the real guard, exits 1) TestDispatchLayerWholeLandClassification::test_evidence_proceeds_while_only_land_lock_held (evidence, standing in for the "succeed" half of the criterion, proceeds without exiting in the same scenario) `promote`/`archive`/`migrate` are not separately exercised end to end -- they are routed through the identical `_LAND_WHOLE_LAND_VERBS` frozenset membership check as `renumber`, so `renumber`'s coverage at both layers stands in for all four, per the test module's own docstring rationale. Commits (on branch t-4556, dev merged in cleanly): 041e87753 fix(tickets): classify renumber/promote/archive/migrate as whole-land 423b12278 Merge branch 'dev' into t-4556 7cdf53c3a / 0e7f7543d / c64e2a8b5 / 5f544e50e chore(tickets): record evidence for T-4556 (plus the pre-existing start-transition commit) No source or test changes were needed beyond binding evidence -- the prior agent's single commit already fully implemented and tested the fix; this pass verified it against a merged dev, ran and passed the bound tests, ran a clean gate check, and closed the evidence gap. Filed: none -- no out-of-scope work discovered.
