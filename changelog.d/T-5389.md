bump: minor
T-5389: T-5151's `frob ticket attach --remove PATH`/`--remove-all` parsed correctly through argparse and had matching AppConfig fields, but their dests (`ticket_attach_remove_path`, `ticket_attach_remove_all`) were never added to `_config_external`'s `_STRING_FIELDS`/`_BOOL_FLAGS` forwarding tuples, so `AppConfig.from_external` silently dropped both flags and the fields stayed at their defaults regardless of the CLI (FLAGCOV001). Added both dests to the correct tuples and added a regression test class following the existing T-2320/T-0749 idiom (real parser + real `AppConfig.from_external`, not a direct construction) so this class of gap cannot regress silently again.
