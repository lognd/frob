bump: minor
T-5333: Adds the squawk migration-safety adapter so the SQL family covers migration-level anti-patterns (NOT-NULL without default, index-without-CONCURRENTLY, lock-taking rewrites) alongside sqlfluff's statement-level rules. Registers squawk into T-5335's own REQUIRED_FOR_FAMILY machinery, reusing sql_relevance verbatim as its relevance predicate rather than inventing a narrower migrations-only predicate, per the owner's stated posture that SQL tools are required-for-the-family with no never-fail flag. Parsing is kept pure and separate from the subprocess spawn so it is fully testable via canned squawk JSON fixtures without the real binary installed.
