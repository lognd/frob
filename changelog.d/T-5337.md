bump: minor
T-5337: Adds the ORM N+1/pooling/transaction/index rule family so the SQL family has real lint coverage beyond raw injection, and so two sibling epics (T-5147-6 WEBPERF pool config, T-5145-3 COMPLY GDPR migration-TTL scan) stop being blocked on a helper that did not exist. Reuses _extract.py's tree-sitter-via-raw_tree and tracked-file-scan shapes rather than standing up a second walker. SQL103/SQL107 and the TS/Prisma N+1 walk are left for a follow-up ticket rather than widened in scope. Also grants core's fs.read via-list on src/frob/sql/*.py (T-5334 left this for whichever leaf landed real reads first) in its own commit.
