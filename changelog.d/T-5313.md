bump: minor
T-5313: Every A11Y rule the web-app epic still owes (missing alt, missing aria-label, skipped heading levels, missing <html lang>) needs the same html/vue/jsx tree-sitter query shapes; this ticket lands the one shared query library so no individual rule module hand-rolls its own node walk. Kept as a leaf-layer Node-in/Result-out module (no frob.lang import) because [arch.layering] still declares webapp = [] -- callers already allowed to import both lang and webapp do the raw_tree() call and hand this module the parsed tree. Positive and negative fixture controls exist per grammar family (html, jsx, vue) and per query shape, plus an unsupported-language negative case.
