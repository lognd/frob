bump: minor
T-3907: Replaced the flat 20-row verb table with the CLI's own taxonomy (frob --help's explore/quality/design/ops/ticket/vet/serve groups). Each group gets a one-or-two-sentence concept paragraph answering what question it answers and when to reach for it, its member verbs one line each, and a pointer into docs/ for depth (docs/modules/cli.md, docs/modules/gates.md, docs/modules/tickets.md, docs/modules/vet.md, docs/modules/serve.md, docs/design/, docs/guides/release.md, docs/modules/fleet.md, docs/modules/deploy.md). Kept the first screen unchanged: the two-sentence intro, the single install line, the verified doctor-command output, and the annotate -> check -> fix-or-waive loop, all before the taxonomy so a reader still reaches a working command without scrolling past it. DOC005 requires a command-table row for every real top-level subcommand, so the old giant "Enforcement/Analysis/Setup" reference tables were not simply deleted -- they were consolidated into one "Full command reference" table after the grouped sections, with group rows pointing back up to their concept paragraph ("see 'quality' above") instead of re-explaining. Nothing that was in the old README was dropped without a home; the per-command depth those tables used to half-carry already lives in docs/modules/cli.md's generated command reference and per-command docs/commands/*.md pages, which the README now points to instead of duplicating. Did not touch the format/fmt split: T-3906 (Series FF, concurrent) is consolidating that split and will update its own row on land, per the ticket's coordination note. Every command referenced in the README (all group members, all full-reference-table rows, install, doctor, and the full quickstart sequence) was run with --help or executed directly in the worktree and confirmed to resolve against the live subcommand registry; frob check's gates-fast/-native/-security/lint/static stages were run chunked (T-0627 forbids an unchunked run under FROB_AGENT) and the only remaining errors are pre-existing DOC006/REF002 findings in tickets/T-3886, tickets/T-3902, tickets/T-3906, and docs/guides/quickstart.md -- none of them touch README.md and none were introduced by this change.
