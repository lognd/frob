bump: minor
T-4037: Mechanism (acceptance item 1): reused the EXISTING `Ticket.findings` field (`tuple[(rule_id, file), ...]`, T-2760) as the orthogonal flag -- a ticket is rule-shaped exactly when `findings` is non-empty. No new TicketKind: a rule-shaped remediation can be a bug/security/invariant- kind ticket just as easily as any other, so kind was the wrong axis. Done-condition (machine-verified): `rule_shaped_findings_unresolved` (src/frob/tickets/_models.py) reads `frob.gates.known_gate_rule_ids()` -- a REGISTRY read, not a `frob check --only <stage>` re-run -- so a rule that loads clean at zero live violations still counts as shipped. Returns the sorted, de-duplicated set of `findings` rule ids NOT in that registry. Close-time refusal (acceptance item 2): `_refuse_if_rule_shaped_ findings_unresolved` (src/frob/app/ticket_runner/_close_cmd.py), called from `_close` before the six existing close-time guards -- HARD sys.exit(1), same posture as `_open_blockers_at_close`'s caller just above it, never merely a warning. Runs at close only, not land (land re-verifies evidence, not ticket-shape guards of this kind, per the existing precedent this mirrors). Retroactive scan (acceptance item 3): walked all 35 children across T-3919/T-3920/T-3928/T-3942/T-3984/T-4025 (35 tickets found via `parent:` grep). Rule-shaped (RULE-ID-named remediation, no `findings` field populated on any of them today -- this check applies going forward, retroactively populating `findings` on 35 existing tickets is itself out of this ticket's declared scope, src/frob/tickets/_models.py and src/frob/app/ticket_runner/_close_cmd.py only): T-3952 (ASSERT001), T-3953 (RACE001), T-3960 (dangerous-comparison- idiom rule), T-3963 (TAINT-IDENT001), T-3966 (ENVVAR003), T-3967 (RESULT001), T-3970 (PROTO001), T-3971 (ENVVAR002), T-3972 (LOOP001), T-3985 (zero-subjects enforcing-gate finding), T-3986 (POL000), T-3988 (TESTRUN001), T-3989 (INV000), T-3991 (GEN001), T-3992 (CI001), T-3994 (SEV001), T-3997 (TESTMOCK001), T-4030 (policy.pattern rule). Not rule-shaped (process/tooling/data-model, no new rule id to ship): T-3954, T-3955, T-3959, T-3961, T-3964, T-3965, T-3969, T-3973, T-3974, T-3975, T-3976, T-3977, T-3987, T-3990, T-3995, T-3996, T-4033, T-4034, T-4035. Whoever closes any of the 18 rule-shaped tickets above should first record its rule id via `frob ticket new --finding RULE-ID FILE` (or equivalent) so this ticket's refusal actually engages for it. Evidence: - tests/test_tickets_rule_shaped.py::TestRuleShapedFindingsUnresolved (empty/loaded/unloaded/mixed cases) - manual smoke test of `_refuse_if_rule_shaped_findings_unresolved` (SystemExit(1) on an unresolved rule id, no exit on a clean ticket) Filed: none Gates: `frob check --ticket T-4037` clean (0 errors).
