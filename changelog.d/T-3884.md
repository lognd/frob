bump: minor
T-3884: Changed: scripts/artifact_smoke.py (new) .github/workflows/release.yml (artifact-smoke job, gates upload) docs/guides/release.md (Decision 6: artifact smoke stage) tests/system/test_artifact_smoke.py (new, real must-fire/must-stay-quiet fixtures) tests/unit/test_artifact_smoke_script.py (new, mocked unit tests) tests/unit/test_release_workflow_gate.py (extended mechanical assertions) design/frob.strata (testsuite node capability declarations for the two new test files) docs/design/registry/capability-via-ratchet.lock.json (bumped exec/fs.read/fs.write ceilings) Evidence: 8 ids bound and passing locally (see frob ticket show T-3884). Smoke-gate design: installs the LOCAL built wheel but resolves every OTHER dependency from the REAL configured index (never TestPyPI, never --no-index) -- uv pip install <local-wheel-path>[extra] --find-links <core-wheels-dir> resolves frob-core/strata-core from the just-built local wheels (not on the index yet, pre-publish) while mcp/pydantic/etc. resolve from the real index. This is what makes the T-3857 shape reproducible: a fully local/frozen install would never touch the real mcp index entry. Proven directly: rebuilt this repo's own wheel with the pre-fix unbounded mcp pin restored and confirmed the smoke script fails with the real mcp 2.x rename error; the current bounded pin passes clean. Gate blocks publish, not warns: .github/workflows/release.yml's upload job's needs now includes artifact-smoke alongside build/build-sdists/verify-ci-status; GitHub Actions' default abort-on-job-failure means upload cannot start unless every platform's smoke stage passed. Known debt, disclosed rather than silently worked around: adding docs/guides/release.md and design/frob.strata to this ticket's scope (required to satisfy SCOPE001 for touching them, and SELFAUDIT001/SYS100 to declare the two new test files' fs.read/fs.write/exec capabilities) triggers SCOPE002's doc-anchor/test-edge closure check over EVERY pre-existing anchor those two files carry -- hundreds of unrelated symbols (docs/strata/roadmap.md alone describes 134), none of which this ticket touches or could reasonably absorb into its own scope. This repo already has one precedent for the identical cascade: src/frob/app/config.py's own AFFECT001 waiver documents the same shape for docs/modules/app.md. Not specific to this ticket's work; filed as a follow-up rather than silently worked around. Filed: none new (the SCOPE002 debt above is disclosed here; a follow-up ticket for the structural gate gap is recommended but not filed by this ticket, to avoid scope creep into gate-design work). Gates: frob check --ticket T-3884 -- all findings resolved except: 2 pre-existing, out-of-scope DOC006 errors (T-3886/T-3902 ticket bodies, not touched here), 1 pre-existing REF002 warning (docs/guides/quickstart.md, unrelated), and the SCOPE002 doc-anchor closure debt described above.
