bump: minor
T-5466: CI run 35951365410 failed both scaffold_dx acceptance tests on the same root cause: the generated project's logging config.toml bound its StreamHandler to `ext://sys.stdout`, resolved ONCE at dictConfig-load time -- broken under pytest's capsys (a new stdout object per test), which made the generated project's own integration test fail. Added LiveStreamHandler (reads sys.stdout/sys.stderr fresh on every emit) and wired it in via "()" custom-callable dictConfig syntax. Also fixed tests/system/test_scaffold_dx.py's _subprocess_env() to strip PYTHONPATH (an agent's `frob agent env` leaking this repo's own frob package into a scaffolded project's isolated venv, masking the real failure behind an unrelated ModuleNotFoundError when reproducing locally). test_hyphenated_name_scaffold_installs_and_console_script_runs verified green end to end, repeatably. test_python_toolchain_scaffold_passes_ check_immediately[python-tool] could not be bound as clean evidence in this session -- blocked by T-2473's fleet-load advisory guard on this shared dev host (multiple concurrent agents), unrelated to the fix; verified instead via an isolated `uv sync` + `pytest tests/` on the rendered scaffold (19/19 pass, including the previously-failing integration test). See the ticket body for full detail.
