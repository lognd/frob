bump: minor
T-4713: Why: T-4703 measured 1,712 runs of 3+ consecutive directive lines (largest 73) before leaf 1 removed the reverse frob:tests copy. This leaf is the stack lint (name the remaining stacks) plus the Tier-A fix that collapses them using leaf 2's multi-target grammar, sequenced after leaves 1-3 so it measures post-cleanup stacks, not ones about to be deleted or reoriented. What: frob.gates._directive_stack.stack_lint_violations groups edges by (file, src) and flags a symbol carrying threshold-or-more DISTINCT origins (a multi-target line counts once, matching how a human reads physical lines). fix_dstack001_merge merges same-kind frob:tests/frob:doc runs into one multi-target header per kind, in FIRST-SEEN order (interleaved doc/tests/doc collapses to one doc header then one tests header). Both are conservative: any other kind, a non-default per-target attribute (frob:tests' own kind="unit" default is recognized and collapsed safely), or a non-contiguous stack is left untouched rather than guessed at. Idempotent by construction -- the merged form drops below threshold, so a second run finds nothing. DSTACK001 registered in _KNOWN_GATE_RULES and docs/modules/gates.md. Left open: TIER_A_HANDLERS registration (_fix_engine.py) and the violation-collection wiring (gates/__init__.py) are outside this leaf's declared scope; frob.toml threshold wiring was blocked by a lease collision with the concurrently in-progress T-4663. Filed as T-draft-ced04135.
