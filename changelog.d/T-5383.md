bump: minor
T-5383: Confirmed root cause is T-5133 fallout (BRIEF item 12): TestSetSprintRecordsTriageChange's three tests called set_sprint(tmp_path, ticket_id, "v0.531.0"), a semver-shaped label, which T-5133 now refuses since a sprint must be goal-named and a version belongs in --milestone (SprintIsSemverShaped). Fix: switch the fixtures to a goal-named label ("burn-down") so the tests exercise set_sprint's real contract again, unrelated to the milestone test in the same file which already used a bare version correctly via --milestone's own setter.
