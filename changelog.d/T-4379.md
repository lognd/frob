bump: minor
T-4379: T-4379: T-2104's IN_PROGRESS scope-narrowing self-heal releases blocked_by on ANY empty-scope ticket, not just narrowed scope WHAT changed ------------ src/frob/tickets/_doable.py - Imported `ScopeChangeOp` from `frob.tickets._models`. - Added `_scope_removed(blocker: Ticket) -> bool`: whether `blocker`'s append-only `scope_changes` audit (T-0455) records at least one real `REMOVE` entry -- proof scope was actually taken away, not merely never declared. - `_open_blockers`: the T-2104 self-heal previously dropped an IN_PROGRESS blocker's edge whenever `scope_overlap_globs(ticket.scope, lease_scope) is None`, with no further check. Now also requires `narrowed`, computed as `bool(lease_scope) or _scope_removed(blocker)` -- i.e. the release only fires when the lease scope is genuinely nonempty-but-disjoint (the ordinary T-2104 case), or when it is empty AND the blocker's own audit trail proves a real narrowing occurred. - Extracted `_blocker_scope_narrowed_away(ticket, blocker, lease_scope) -> bool` (disjoint AND (nonempty OR `_scope_removed`)) so `_open_blockers`'s own body is UNCHANGED line-for-line from `dev` except the one condition line -- the first two drafts (a nested `if`, then an inlined multi-line boolean expression) each pushed `_open_blockers` over ARCH001's 60-line-AND-complex threshold (LANDPARITY002, diff-scoped). tests/test_tickets.py - Imported `ScopeChangeEntry`, `ScopeChangeOp`. - Added two tests to `TestDoableStaleBlockedBySelfHeals`: - test_never_scoped_container_blocker_does_not_self_heal (MUST FAIL FIRST on main): a container/rollup blocker with `no_scope_declared=True`, `scope=()`, and no `scope_changes` entries must NOT self-heal the instant it goes IN_PROGRESS. Confirmed failing pre-fix (see below). - test_genuinely_narrowed_to_empty_blocker_still_self_heals (MUST-STILL-PASS control): a blocker with a real `scope_changes` `REMOVE` entry narrowing it down to empty still self-heals -- the fix distinguishes "never had scope" from "had it and gave it up," it does not disable the empty-scope self-heal outright. WHY --- `scope_overlap_globs` returns `None` (no overlap) whenever EITHER side's glob list is empty, unconditionally -- its inner loop never executes over an empty list. A container/rollup ticket (`no_scope_declared=True`, `scope=()`) therefore has an empty LIVE lease scope from the moment it starts, so the pre-fix check read that as "narrowed away from every possible dependent" the instant it went IN_PROGRESS, regardless of whether its actual rollup work was done -- it never had scope to narrow FROM in the first place. This is a different bug from the genuine T-2104 self-heal (a blocker whose scope shrank mid-work to no longer collide with a dependent): T-2104 was designed for real narrowing, not for a blocker that never declared scope to begin with. Evidence (test node ids, bound via `frob ticket evidence`) ------------------------------------------------------------ tests/test_tickets.py::TestDoableStaleBlockedBySelfHeals::test_never_scoped_container_blocker_does_not_self_heal tests/test_tickets.py::TestDoableStaleBlockedBySelfHeals::test_genuinely_narrowed_to_empty_blocker_still_self_heals Full relevant classes: `PYTHONPATH=$(pwd)/src .../python -m pytest tests/test_tickets.py::TestDoableStaleBlockedBySelfHeals tests/test_tickets.py::TestStartHonorsSelfHealedBlockedBy tests/unit/test_close_blocked_by_guard.py` -> 14 passed, 0 failed. Lint: `ruff check`/`ruff format --diff` clean on both touched files. Confirmed regression coverage: applied the fix's diff as a patch, reverted it, re-ran the two new tests -- 1/2 failed (test_never_scoped_container_blocker_does_not_self_heal, exactly reproducing the reported bug: T-0001 became doable the instant the never-scoped container T-0002 went IN_PROGRESS); reapplied the fix, both pass. Commits ------- worktree: /home/logan/projects/frob/.claude/worktrees/t-4379 (branch t-4379) HEAD: d3965afe54a489965f93bc06809623f9d9a5e535 "fix(tickets): extract narrowing check to keep _open_blockers under ARCH001" (on top of 4a8095842 "fix(tickets): flatten narrowing check to stay under ARCH001 nesting", on top of 565d737bc "fix(tickets): only self-heal blocked_by for a blocker that actually narrowed") Gates ----- `frob check --only gates --ticket T-4379 --files "src/frob/tickets/_doable.py,tests/test_tickets.py" --base dev` run three times as the fix's shape settled; final run clean of diff-local findings. Confirmed pre-existing/repo-wide and left untouched (per --ticket's own scope-note: only gate:SCOPE/PREWORK and the diff-driven parts of gate:COV/FMT/AFFECT are scoped to this ticket, every other gate family's count is repo-wide): - gate:LANG/TICK/REF/TODO/MILE/WIRE findings unrelated to any touched file or symbol. - PERF008 on `_all_leases(...)` -- predates this diff, same lazy-memoized call under the same `if all_leases is None` guard as on `dev`. Diff-local finding, now resolved: - LANDPARITY002 (`_open_blockers` pushed past ARCH001's 60-line threshold): two earlier drafts (a nested `if`, then an inlined multi-line boolean) each pushed it over; final shape extracts `_blocker_scope_narrowed_away` so `_open_blockers`'s own body is byte-for-byte unchanged from `dev` except the one condition line. Filed: none (no out-of-scope work found while working this ticket).
