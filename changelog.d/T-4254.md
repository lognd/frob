bump: minor
T-4254: T-4254 -- DRIFT/COV: symbols sharing one frob:doc anchor are one contract -- a ticket touching one should reopen review of all WHAT changed, per file: - src/frob/gates/_docblocks_refs.py - Added imports: `Severity` from `frob.findings`, `Diff`/`working_diff` from `frob.gitio`, `EdgeKind` from `frob.graph._models`. - Added `_shared_doc_anchor_groups(snapshot)`: groups every `EdgeKind.DOC` edge's source symbol (`edge.src`, a `path::qualname` symref) by the anchor it targets (`edge.target`); returns only anchors with 2+ distinct participant symbols -- a lone symbol citing its own doc anchor is not a "group" anything else needs to review. - Added `_symbol_touched_by_diff(symref, span, diff)`: whether any `Hunk` in `diff` overlaps `span` (a `SymbolRecord.span`) in the file `symref` lives in. - Added `_cov009_violation(...)` and `cov009_violations(snapshot, diff)`: for every shared-anchor group, if `diff` touches at least one participant's own line span but not another's, emit one WARN COV009 per UNTOUCHED participant, naming which touched sibling(s) triggered the review. A group with zero touched participants (this diff has nothing to do with it) stays completely silent. - Added `cov009_gate(root, snapshot)`: the `run_gates`-ready entrypoint, computing its own `working_diff(root, "main")` exactly the way `frob.gates._land_format.land_format_gate` does (T-4298 precedent) -- fails OPEN (`()`) when the diff cannot be computed, matching every other diff-scoped land-time check's posture in this package. - Added `frob:ticket`/`frob:doc`/`frob:tests` directives on all four new public/private symbols that need them. - tests/gates_suite/test_docblocks_refs_cov009.py (new file) - `TestSharedDocAnchorGrouping`: 2 tests for `_shared_doc_anchor_groups` in isolation (two symbols sharing an anchor form a group; a lone participant does not). - `TestCov009SharedAnchorReview`: 4 tests for `cov009_violations` end-to-end (touching one sibling flags the other; touching both flags neither; an untouched group stays silent; a lone-participant anchor never fires even when touched directly). - Diff fixtures are built directly (`Diff(base=..., hunks=(Hunk(...),))`) rather than through a real git working tree -- `cov009_violations` is a pure function over `(snapshot, diff)`, so no git plumbing is needed to exercise it, matching the ticket's own "Fixture-testable: YES" framing. WHY: Consumer F-386 item 4 (T-4182, shell round-5) found a shared contract's fix and its miss landing in two different tickets' scopes because frob had no notion that multiple call sites bound to the same `frob:doc` anchor implement ONE contract. The `frob:doc` edges already carry the right grouping key (the anchor); COV009 makes that grouping mechanical instead of relying on a human noticing the anchor was shared. Acceptance criteria (added via `frob ticket accept`, none existed on the ticket originally): 1. "frob:doc edges sharing the same anchor are grouped into one contract (_shared_doc_anchor_groups), ignoring anchors with only one participant symbol" -- proven by TestSharedDocAnchorGrouping::test_two_symbols_same_anchor_form_a_group and ::test_lone_anchor_participant_is_not_a_group. 2. "cov009_violations warns on every untouched sibling in a group once the diff touches at least one other sibling's own line span, and stays silent when the diff touches none or all of them" -- proven by TestCov009SharedAnchorReview::test_touching_one_sibling_flags_the_other, ::test_touching_both_siblings_flags_neither, and ::test_untouched_group_is_silent. Test node ids (all passing, `pytest tests/gates_suite/test_docblocks_refs_cov009.py` -> "6 passed in 0.52s"): - tests/gates_suite/test_docblocks_refs_cov009.py::TestSharedDocAnchorGrouping::test_two_symbols_same_anchor_form_a_group (evidence) - tests/gates_suite/test_docblocks_refs_cov009.py::TestSharedDocAnchorGrouping::test_lone_anchor_participant_is_not_a_group (evidence) - tests/gates_suite/test_docblocks_refs_cov009.py::TestCov009SharedAnchorReview::test_touching_one_sibling_flags_the_other (evidence) - tests/gates_suite/test_docblocks_refs_cov009.py::TestCov009SharedAnchorReview::test_touching_both_siblings_flags_neither (evidence) - tests/gates_suite/test_docblocks_refs_cov009.py::TestCov009SharedAnchorReview::test_untouched_group_is_silent (evidence) - tests/gates_suite/test_docblocks_refs_cov009.py::TestCov009SharedAnchorReview::test_lone_anchor_participant_is_not_a_group (regression, not separately bound -- covered by criterion 1's grouping tests) Also ran the existing DOC004 suite for regression (module-level import changes only, no behavior change to existing checkers): `pytest tests/gates_suite/test_doc.py` -> "58 passed in 5.15s". `ruff check`/`ruff format` clean on both touched files. `frob check --only gates --files src/frob/gates/_docblocks_refs.py --files tests/gates_suite/test_docblocks_refs_cov009.py --base dev`: ran ~250s (did not finish inside the foreground timeout, a large unscoped multi-family run per the ticket playbook's own sec 6c note -- `--files` does not scope most gate families). Grepped the partial output for the touched paths and for COV009/malformed: zero hits against either touched file. No new finding attributable to this change was observed. DELIBERATELY NOT DONE (disclosed): COV009 is fully implemented and tested as a standalone, fixture-tested function (`cov009_violations`/`cov009_gate`), but is NOT yet wired into `frob check`'s actual dispatch: - `src/frob/gates/__init__.py`'s `_assemble_gate_report` dispatch table does not call `cov009_gate` yet. - `src/frob/gates/_waive.py`'s `_KNOWN_GATE_RULES` registry does not list "COV009" yet. - `docs/modules/gates.md`'s rule-catalog table has no COV009 row, and its `frob:enumerates members=` list does not include COV009. None of these three files are in T-4254's declared scope (`src/frob/gates/_docblocks_refs.py` only), and all three were leased by other in-progress tickets at scoping time (`src/frob/gates/__init__.py` and `src/frob/gates/_waive.py` by T-4540; `docs/modules/gates.md` by this agent's own concurrent T-4214). Per the dispatch playbook, work outside declared scope is filed as a new ticket rather than folded in silently. Filed: T-5171 "Wire COV009 (shared frob:doc anchor review) into frob check dispatch + registry" -- scope src/frob/gates/__init__.py, src/frob/gates/_waive.py, docs/modules/gates.md; body lists the three concrete steps (registry entry, dispatch-table wiring, docs row) plus a follow-up end-to-end fixture once the wiring lands. Commit: 276007778 "feat(gates): add COV009 shared frob:doc anchor review obligation" (worktree branch t-4254, base dev).
