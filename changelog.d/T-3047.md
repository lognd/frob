bump: minor
T-3047: Adds T-3004 section 8's typed review/decision records to the V-model graph kernel: a new `review` node kind (requires commit+reason, provenance as data) and a required `reason` attr on `decision` nodes (owner review superseding the prior schema-doc note that decision carried no required attr). `review` is now also a legal `decides` edge source. Verified with 4 new Rust unit tests (construction refusal, a reasoned decision constructs, review requires both attrs, and a supersedes round-trip through the graph via forward_closure) plus 7 new Python-level tests at the same PyO3 boundary the other node/edge kinds are exercised through. Two pre-existing tests in T-3010's tests/unit/strata/test_vmodel_check.py break as a direct, measured consequence of the decision-reason requirement; that file is held by T-3010's live scope lease and cannot be claimed here -- the exact fix is saved as a patch (scratchpad/t3047-fix-for-t3010-test_vmodel_check.patch) for whoever owns that file once the lease frees. docs/strata/vmodel.md update filed as T-draft-b28f4789 for the same reason.
