bump: minor
T-5392: Post-land sweep flagged two findings on this ticket: DUP002 on `_walk_css.py::_css_rule_symbol` and the same FLAGCOV001:frob.toml finding T-5389 already fixes (T-5389 landed first, so that half is already resolved by the time this lands). The DUP002 finding is the T-5303 walker-shape precedent: every per-language walker (bash, zig, kotlin, css, javascript, ...) builds a `RawSymbol` via the same field-list pattern deliberately, not by copy-paste of real shared behavior -- `_walk_javascript.py::_const_symbol` already carries a waiver citing this exact pairing (T-5300). Added the symmetric `frob:waive DUP002` comment to `_css_rule_symbol` rather than extracting a shared helper, since extracting one would blur each walker's own grammar-specific field derivation (T-5300's own rationale, already accepted for the javascript side of this same pair).
