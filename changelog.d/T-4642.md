bump: minor
T-4642: Done report -- T-4642 land Tier-A directive canonicalizer emits lines over the ruff limit that the land's own ruff gate then refuses (E501) WHAT changed ------------ src/frob/gates/_fix_engine.py - New _noqa_e501_guard(line_text, path, root): if a rewritten directive line now exceeds the resolved line-length limit (frob.gates._fmt_ directives.resolve_line_length) and does not already carry a suppression (_NOQA_SUFFIX_RE), appends " # noqa: E501" -- only for a "#"-comment-leader language (marker_for), mirroring _canonical_ lines's own existing posture in frob.gates._fmt_directives. - _rewrite_line_substring (shared by fix_doc007_dotted_form and fix_doc002_unique_slug) now takes a `root: Path` parameter and routes its rewritten line through _noqa_e501_guard before writing. Both call sites updated to pass `root`. tests/gates_suite/test_fix_engine.py - New TestFixEngineTierA.test_doc002_rewrite_that_exceeds_ruff_limit_ gets_noqa: POSITIVE CONTROL. A `# frob:doc docs/m.md#widget-alpha- beta-gamma-delta-epsil` directive (58 chars, well under the limit) fuzzy-matches (ratio ~0.67, above DOC002's 0.6 cutoff) a real heading whose full slug is 70 chars -- after DOC002's Tier-A rewrite the line is 91 chars. Asserts: the rewrite happened, the pre-noqa portion is >88 chars, the line ends with " # noqa: E501", AND (the load-bearing half) a REAL `ruff check` subprocess run against the rewritten file exits 0. WHY (root cause, measured on T-4230's land, twice) --------------------------------------------------- _rewrite_line_substring was a bare `str.replace` with zero length awareness. DOC002's fuzzy-matched candidate slug is a REAL heading slug in the target doc, unbounded by the original (short) slug's width -- so a directive line that fit under ruff's line-length limit before a Tier-A rewrite could stop fitting after one, and the land's own `ruff check` gate then refused the brand-new E501 finding Tier-A itself had just caused. FMT001/`frob fmt` was ruled out by direct reproduction: `format_paths(Path("src/frob/gates/_coverage.py"), check_only=True)` against the exact T-4230 worktree file reports 0 changes, matching the coordinator's own observation that `frob fmt` in the worktree changes nothing further. Manual workaround relayed to the coordinator for landing T-4230 today (before this fix lands): append " # noqa: E501" to the line by hand in the worktree now -- DOC002's rewrite only substring-replaces the "docs/modules/gates.md#<slug>" portion, so a hand-added trailing noqa survives its rewrite untouched. Acceptance criterion and evidence ----------------------------------- "A directive rewrite (DOC007 dotted-form / DOC002 fuzzy-slug) that pushes a line past the resolved line-length limit gets a trailing noqa: E501 appended in the same write, and the rewritten file is clean under a real ruff check." -> tests/gates_suite/test_fix_engine.py::TestFixEngineTierA:: test_doc002_rewrite_that_exceeds_ruff_limit_gets_noqa Bound via `frob ticket evidence T-4642 <node> --accepts 1 --base-ref dev`. BUG002 repro: kind=bug, designated_repro_test = the same node id above. Since the test was authored in the same commit as the fix, used the T-2021-style technique: commit 25199f3b1 temporarily disabled the noqa guard (reverting _rewrite_line_substring's call to a bare `.replace(old, new, 1)`) while keeping the new test, confirmed FAILED_AT_PARENT via `--designate-repro --base-ref 25199f3b1`, then commit 46c8aebda restored the real fix. `--check-repro` not separately re-run (matches T-4638's own precedent this session); --designate-repro's own verdict (FAILED_AT_PARENT) is the binding one. Commit shas (branch t-draft-ede38ca6) -------------------------------------- 22d5a9314 fix(gates): guard Tier-A directive rewrites against a new E501 25199f3b1 test(gates): BUG002 repro commit -- disable noqa guard 46c8aebda fix(gates): restore noqa guard after BUG002 repro (plus frob ticket ledger auto-commits for scope/start/accept/evidence) HEAD = b87e5cc3c Gates ----- - `frob check --only sys --only coverage --only arch --files src/frob/ gates/_fix_engine.py --files tests/gates_suite/test_fix_engine.py --base dev`: 0 findings on either touched file (arch: pass, 19 warnings all pre-existing elsewhere; coverage: 0 findings on either file; sys/SELFAUDIT: 0 findings). DRIFT (5, all waived)/DSL (1)/TODO (4) are pre-existing, on files this ticket never touched. - `ruff check`/`ruff format --check` on both touched files: clean. - `ty check src/frob/gates/_fix_engine.py`: clean. - tests/gates_suite/test_fix_engine.py::TestFixEngineTierA -k "doc002 or doc007" (6 tests, including the new one): all pass at final HEAD. Cross-ticket ------------ git diff --name-only dev...HEAD = src/frob/gates/_fix_engine.py, tests/gates_suite/test_fix_engine.py -- both in this ticket's own declared scope (docs/modules/gates.md was left untouched: leased by T-3259/T-4116 for this ticket's whole duration; no doc fold-in filed since this is a private-helper-only, no-new-public-symbol change with no frob:doc obligation). Filed: none.
