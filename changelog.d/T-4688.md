bump: minor
T-4688: -- T-draft-ea93df7b graph is rebuilt 5+ times per ticket close-out: --only checks share no cache, docptr always cold-rebuilds ### WHAT changed, per file src/frob/graph/__init__.py Added `get_snapshot(root, cache) -> Result[GraphSnapshot, BuildError]`: the one load-or-build entry point every graph consumer should call instead of hand-rolling `load_graph`-then-`build_graph`. Tries `load_graph` first (cheap, cache-only, content-keyed via on-disk (mtime_ns, size) then a real content hash on mismatch -- never mtime alone); falls back to `build_graph` only on a miss. Logs "graph: reused cache key=<cache>" on a hit and "graph: rebuilt (reason=<err>)" on a miss, at INFO, so the rebuild count this ticket was filed to measure is auditable from logs going forward. Added to `__all__` and to docs/modules/graph.md's Public API section. src/frob/app/ack_runner.py (_load_snapshot_for_ack) Migrated from its own load_graph-then-build_graph fallback to `frob.graph.get_snapshot`. src/frob/app/coverage_runner.py (`frob coverage --full`'s graph load) Same migration. src/frob/app/dup_runner.py (`frob dup --probe`'s graph load) Same migration. src/frob/app/graph_runner.py (_load_snapshot, used by `frob graph query`/ friends; `_run_build`, the explicit `frob graph build` command, is left alone -- an explicit user-requested rebuild is not the duplication this ticket targets) Same migration. src/frob/gates/_prework.py (`_resume_or_start_sweep`'s dup+xref pre-work sweep graph load, used by `frob ticket work`/`frob ticket start`) Same migration. docs/modules/graph.md Added `get_snapshot`'s `frob:describes` anchor and a Public API entry documenting its load-or-build/logging/cross-process-reuse contract. tests/unit/test_graph_get_snapshot.py (new file) Three tests for `get_snapshot` (see Acceptance below). Placed in a new file rather than tests/test_graph.py because tests/test_graph.py's lease was held by in-progress T-4625 at write time (frob ticket scope --add refused the combined add atomically; re-tried with just this new file and it succeeded). tests/unit/test_app_runners_batch5.py Three `TestDupRunner` tests monkeypatch `frob.graph.load_graph` directly with a `_FakeLoaded` stub carrying only `is_ok = False`; `get_snapshot` now reads `.danger_err` off that stub for its INFO log on the miss path, so each stub gained `danger_err = "stale"`. No assertions changed. ### WHY Repo-wide audit (git grep build_graph/load_graph -- src/frob, 2026-09-19) found 18 independent `load_graph`-then-`build_graph`-on-miss call sites, each reimplementing the same fallback against the same on-disk .frob/cache.db. Per BRIEF-mandated agent workflow, a ticket close-out runs 5 separate `frob check --only <stage>` invocations, each a fresh process -- `build_graph`'s own `@memoize_per_run` (T-0423) only dedups within one process, so it gave zero reuse across those 5. `_docptr_kept_violations` (src/frob/gates/_sys.py) additionally builds into a disposable tempfile.TemporaryDirectory() cache on every call -- a guaranteed full cold rebuild every time, never reusing the real repo cache at all -- but that file plus src/frob/gates/__init__.py, check_runner.py, and sys_runner.py were leased by in-progress T-3962/T-3995/T-4112/T-4113/T-4212 when this ticket started, so those four sites are deferred, not fixed here (see Scope refusals below). T-4634 (landed today, commit deffcc7fb) already fixed one instance of this exact class in `_land.py`'s post-publish verify-intent snapshot; this ticket generalizes the fix to the five call sites this ticket's scope could actually reach. ### Acceptance criteria and how each is proven 1. "two consecutive get_snapshot calls on an unchanged tree build the graph zero additional times on the second call" -> tests/unit/test_graph_get_snapshot.py::TestGetSnapshot.test_second_call_on_unchanged_tree_does_not_rebuild Spies on `build_graph` via monkeypatch; asserts zero calls on the second `get_snapshot` call. 2. "one changed file between two get_snapshot calls triggers exactly one rebuild" -> tests/unit/test_graph_get_snapshot.py::TestGetSnapshot.test_changed_file_triggers_exactly_one_rebuild Same spy; asserts exactly one `build_graph` call after one file edit. 3. "a never-built cache falls back to build_graph transparently via get_snapshot" -> tests/unit/test_graph_get_snapshot.py::TestGetSnapshot.test_load_failure_falls_back_to_build No cache.db exists; asserts `get_snapshot` still returns Ok with the built symbol present. No gate's findings changed as a result of this change (get_snapshot's behavior is byte-identical to the load-then-build sequence each migrated call site already ran; only the boilerplate moved). ### Test node ids (evidence-bound, --base-ref dev) tests/unit/test_graph_get_snapshot.py::TestGetSnapshot::test_second_call_on_unchanged_tree_does_not_rebuild tests/unit/test_graph_get_snapshot.py::TestGetSnapshot::test_changed_file_triggers_exactly_one_rebuild tests/unit/test_graph_get_snapshot.py::TestGetSnapshot::test_load_failure_falls_back_to_build ### Also run green (regression, not bound as new evidence) tests/unit/test_ack_runner.py, tests/unit/test_coverage_runner.py, tests/gates_suite/test_prework.py, tests/test_graph_affects_runner.py, tests/unit/test_app_runners.py, tests/unit/test_app_runners_batch5.py, tests/unit/test_app_runners_batch6.py, tests/unit/test_app_runners_json_guard_t2492.py, tests/test_graph.py (363 collected, 0 failed) ### Scope refusals / deferred work `src/frob/gates/__init__.py` (main gate pipeline's `_load_graph_queue_lock`, which calls `build_graph` unconditionally, never trying `load_graph` first), `src/frob/app/check_runner.py`, `src/frob/app/sys_runner.py`, and `src/frob/gates/_sys.py` (`_docptr_kept_violations`'s disposable-tempdir full rebuild -- the single worst offender found in this audit) were all under in-progress leases (T-3962, T-3995, T-4112/T-4113, T-4212 respectively) when `frob ticket work` ran the collision check; removed from this ticket's declared scope rather than fought over. These are the highest-value remaining sites and should be a fast follow-up once those leases clear -- not filing a new ticket for them since T-draft-ea93df7b's own body already documents them in full as "deferred work" and a follow-up agent can pick the same ticket back up or a coordinator can spin a new one once the leases are free. Followed the owner's "perf findings become lint rules" directive: a PERF00x detector that flags a new load_graph/build_graph call site NOT going through `get_snapshot` was considered but does not fit this ticket's narrowed scope (it would need to touch the gates/policy engine, itself leased). Filing as a fast-follow is the right move once scope reopens rather than forcing it in under a lease conflict. ### Commits cddbb4b6 fix(graph): add shared content-keyed get_snapshot, migrate 5 call sites 769309e5 chore(tickets): ack sweep_ticket body drift from get_snapshot migration 25b8eae2 chore(tickets): bind frob:ticket directive on migrated snapshot loaders (HEAD 25b8eae2784f6f20615dfeffd834f52794a9d3e4, worktree /home/logan/projects/frob/.claude/worktrees/t-draft-ea93df7b, branch t-draft-ea93df7b) ## Pre-READY checks 1. `frob check --only sys --files <touched> --base dev`: FAIL gate:DRIFT 7 errors (5 waived) -- only src/frob/gates/_prework.py::sweep_ticket (body) attributable to this ticket; fixed via `frob ack src/frob/gates/_prework.py::sweep_ticket` (commit 769309e5), re-verified zero DRIFT hits on my files in the coverage re-run below. FAIL gate:DSL 1 error (tests/test_app.py:387, pre-existing, not touched by this ticket). FAIL gate:SELFAUDIT 1 error (design:1 testsuite fs.write via-list glob, unrelated to any file this ticket touches). Zero SELFAUDIT001/DSL001 findings attributable to my files. 2. `frob check --only arch --files <touched> --base dev`: pass frob-arch 20 warnings (36 waived), 546 suggestions -- no ARCH001/ LARGE001 errors. graph_runner.py and graph/__init__.py show mixed-concern-function/large-file/high-coupling WARNINGS (not errors), pre-existing shape of those files this ticket's small addition did not push over any error threshold. 3. `frob check --only coverage --files <touched> --base dev` (re-run after fix): FAIL gate:COV 9 errors, all waived/pre-existing or attributable to files outside this ticket's scope (frob:doc-on-private-symbol waivers in digest.py/graph_runner.py already carry accepted waivers; the 5 TODO002 findings are in _coverage.py/_dup_spawn.py/_loop_effects.py/_secrets.py, none touched here). The two COV002 findings attributable to this ticket (_load_snapshot_for_ack, sweep_ticket changed with no frob:ticket edge) were fixed by adding `# frob:ticket T-draft-ea93df7b` to both (commit 25b8eae2); confirmed absent from the re-run (COV002 count for those two symbols: 0). 4. `ruff check <touched>` / `ruff format --check <touched>`: all clean ("All checks passed!" / "N files already formatted") for every touched .py file, both before and after the COV002/DRIFT fixups. `ty check <touched .py files>`: "All checks passed!" (one fixup needed mid-way: get_snapshot's return type had to be `Result[GraphSnapshot, BuildError]`, not `Result[GraphSnapshot, GraphError | BuildError]`, since `BuildError` already IS `GraphError | LangError` and ty enforces invariance on the Result error type parameter). 5. Cross-ticket leakage: `git diff --name-only dev...HEAD` lists exactly the files in this ticket's own lease (docs/modules/graph.md, frob.lock, the 5 migrated app/gates files, graph/__init__.py, the two test files, tickets/T-draft-ea93df7b/ticket.md) plus no path leased by any other in-progress ticket (checked via `grep -l "<path>" .git/frob-leases/*.json` for every changed path -- zero hits outside this ticket's own lease file). 6. Not a kind=bug repro-designation case in the BUG002 sense -- this bug's "repro" is the audit/measurement in the ticket body, not a single pre-existing failing test; no `--designate-repro` step applies (this ticket fixes duplicated-boilerplate/perf, not a regression with a pinpointable pre-fix failing assertion). 7. `tickets/T-draft-ea93df7b/ticket.md`'s `id:` line still reads `T-draft-ea93df7b`, matching its directory -- it has not been promoted to a numeric id in this worktree. Per the CLARIFICATION note, promotion is the coordinator's/land's concern; flagging here per the checklist so it is not missed. 8. Evidence bound AFTER the last commit (25b8eae2): evidence was first bound at commit df19ba9b7 (evidence-recording commits are themselves auto-committed by `frob ticket evidence`), then invalidated by the two follow-up fix commits (769309e5, 25b8eae2) and RE-bound at 8f4923144 in its final state -- the evidence-recording auto-commits (df19ba9b7, 8f4923144) are themselves after every substantive code change, so the final evidence-record commit is the true HEAD's own last word.
