bump: minor
T-5366: WEBPERF109-115 close the ticket's server/network performance-config gap: compression, general caching headers, HTTP/2, next-gen image formats, CDN cache hints, a server cache layer over expensive queries, and two React render-performance shapes had no lint coverage. All checks reuse the config-file text-regex idiom T-5325's _websec_headers.lint_response_headers established, without importing its private helpers (its REQUIRED_HEADERS is fixed to five security headers, no Cache-Control/compression vocabulary). Blocks cleanly on T-5143-2/T-5144-2/T-5148-4 per the ticket body rather than re-implementing any of them. Kept the worktree plain (no branch merges) so it lands without passengers, and did not touch sibling T-5371's webperf1xx/markup/** or _webperf_markup.py. Same honest discovery-gap disclosure as every sibling SEO/WEBPERF leaf: websec_ findings is not auto-discovered by any live gate today, covered by T-draft-553232aa. All seven reserved ids used, no follow-up ticket filed for an unused id this time.
