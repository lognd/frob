bump: minor
T-5126: T-5126 -- DOC006 refuses unrelated lands over cli pointers inside ticket bodies merged in from dev ## WHAT changed, per file - src/frob/gates/_docptr.py - Added `_is_ticket_body_doc(doc_path) -> bool`: true for `tickets/<id>/ticket.md` or `tickets/<id>/done-report.md`, REGARDLESS of ticket state (unlike the existing `_is_historical_ticket_doc`, which only exempts an open ticket's `ticket.md` once it is DONE or DROPPED). - In `doc006_gate`, wrapped the `_cli_violations(...)` call with `if not _is_ticket_body_doc(doc_path):` so a ticket body's own narrative CLI-pointer prose (e.g. a Plan section backtick-quoting `frob sys split`) never produces a DOC006 CLI-pointer finding, on any ticket, open or closed. Every OTHER DOC006 kind (file/path, anchor, config, symbol, file::symbol, bare-identifier) is untouched and still runs over ticket bodies exactly as before -- this is a CLI-pointer- only skip, per the ticket's own Plan step 1 wording. - `tickets/archive/**` was already fully exempt via the pre-existing `_is_archival_doc`; no change needed there. - src/frob/tickets/_land_squash.py - Added `_TICKET_LEDGER_FILE_PREFIX = "tickets/"` and `_exclude_dev_merged_ledger_files(worktree, main_branch_name, touched_files) -> frozenset[str]`: narrows a touched-files set to drop `tickets/**` paths whose content at `worktree`'s HEAD is byte-identical to `main_branch_name`'s current tip (proven via `git diff --name-only <main_branch_name> HEAD -- <candidates>`) -- i.e. paths that rode in unchanged via `git merge dev` rather than being genuinely edited by this land. A file the worktree's OWN commits additionally edited after the merge still differs from main and is NOT excluded. A git failure fails CLOSED (no exclusion applied), so this can only ever shrink the set on a proven match. - Wired it in at the one call site in `_land_squash_apply_finish` that feeds `_run_pre_commit_checks`'s T-3324 self-conformance check: `worktree_changeset` itself stays the FULL changeset (still used above, unmodified, for `_assert_land_complete`'s completeness assertion and for `_post_publish_native_rebuild`/`_maybe_rebuild_ natives`), and only the narrowed `self_conformance_touched_files` is passed to `_run_pre_commit_checks`. - tests/gates/test_docptr.py (new file) - `TestDoc006TicketBodyCliPointerSkip.test_open_ticket_planned_cli_pointer_not_flagged` (positive control (a) from the ticket's Plan): an OPEN (queued) ticket's `ticket.md` backtick-quoting a nonexistent planned command (`frob sys split`) produces no DOC006 finding. - `TestDoc006TicketBodyCliPointerSkip.test_real_doc_cli_pointer_still_flagged` (positive control (c)): a real `docs/*.md` file's unresolved CLI pointer (`frob nonexistent-subcommand`) still fires -- the skip does not leak past ticket bodies. - This is a SEPARATE module from the existing tests/test_docptr_gate.py on purpose: that file is under another in-progress ticket's live cross-worktree lease (T-4624), so scope on it was refused (ScopeLeaseConflict) and I did not expand into it. The fixture helpers here are a deliberate, minimal duplication of that file's `_init_repo`/`_write`/`_add_all`/`_snapshot`/`_by_rule` shape. - tests/tickets/test_land_squash.py + tests/tickets/__init__.py (new) - `test_dev_merged_ledger_file_excluded` (positive control (b), the unit-level equivalent of "a land whose merge brings in such a ticket file is not refused"): builds a real git fixture where a `feature` branch merges `main` in after `main` gained an unrelated ticket's `ticket.md`; asserts `_exclude_dev_merged_ledger_files` drops that ledger file from the touched set while keeping the feature branch's own file. - `test_own_ledger_edit_after_merge_still_counted`: the must-NOT- exclude half -- a ticket ledger file the worktree's OWN commits additionally edit after the merge stays counted (the shape a naive blanket `tickets/**` exclusion would silently drop). ## WHY DOC006's CLI-pointer resolution treated a ticket body's own narrative prose (a Plan/repro section legitimately describing a not-yet-built or rejected CLI form) as a live documentation pointer that must resolve. Combined with the T-3324 self-conformance check counting a ledger file that only arrived via `git merge dev` as "this land's own touched files", an UNRELATED land was refused four times (T-4659, T-4416, T-4684, per the ticket's measured repro) over a different ticket's planner prose that rode in through the routine `git merge dev --no-edit` every worktree does before landing. ## Acceptance criteria proof The ticket carries no formal `## Acceptance` list (declared: `frob ticket show` reports 0 acceptance items), so evidence is bound flat against the ticket per the three positive controls its own Plan step 3 names: (a) tests/gates/test_docptr.py::TestDoc006TicketBodyCliPointerSkip::test_open_ticket_planned_cli_pointer_not_flagged (b) tests/tickets/test_land_squash.py::test_dev_merged_ledger_file_excluded tests/tickets/test_land_squash.py::test_own_ledger_edit_after_merge_still_counted (c) tests/gates/test_docptr.py::TestDoc006TicketBodyCliPointerSkip::test_real_doc_cli_pointer_still_flagged All 4 bound via `frob ticket evidence T-5126 <node-id>... --base-ref dev`. ## BUG002 repro This is kind=bug, so a designated repro was required. The repro test (`test_open_ticket_planned_cli_pointer_not_flagged`) was committed ALONE first (commit 0b40df6ea), confirmed to genuinely FAIL against the still-unfixed code at that commit, then the fix landed in a separate commit (0ff330a79). Designated and verified: frob ticket evidence T-5126 --designate-repro "tests/gates/test_docptr.py::TestDoc006TicketBodyCliPointerSkip::test_open_ticket_planned_cli_pointer_not_flagged" --base-ref 0b40df6ea -> FAILED_AT_PARENT (genuine repro, as BUG002 wants) frob ticket evidence T-5126 --check-repro --base-ref 0b40df6ea -> FAILED_AT_PARENT (re-confirmed) ## Filed out-of-scope ticket T-5095: DOC006 also needs a tickets/** skip for the NON-CLI pointer kinds (file/path, config, file::symbol). Found while running tests/test_docptr_gate.py::TestDoc004Doc006ZeroOnFrobsOwnRepo::test_doc004_doc006_zero_against_live_repo, which still fails with 11 pre-existing findings of those other kinds inside OPEN ticket bodies (T-3822, T-3823, T-4668, T-4670, T-4691, T-4693, T-4742, T-4808) -- unrelated to this ticket's CLI-only fix (my diff never touches the file/anchor/config/symbol violation code paths), and out of this ticket's declared scope/Plan (which is explicitly "cli-pointer resolution" only). Left for T-5095 to scope and fix; the underlying tests/test_docptr_gate.py file is also currently under T-4624's live lease, so it could not have been touched here regardless. ## Commits (on branch t-draft-a38af1c4) 9bf89a4a2 (and earlier) chore(tickets): scope T-5126 0b40df6ea test(gates): add DOC006 ticket-body cli-pointer positive controls 0ff330a79 fix(gates): skip DOC006 cli-pointer checks over ticket bodies 3a09b8542 / 7ea58c684 chore(tickets): record evidence for T-5126 (7ea58c684 is HEAD; evidence was bound strictly after the last code commit) HEAD sha: 7ea58c684c57978e01d6f063053390916ed3851d ## Pre-READY checks frob check --only sys --files src/frob/gates/_docptr.py --files src/frob/tickets/_land_squash.py --files tests/gates/test_docptr.py --files tests/tickets/test_land_squash.py --base dev -> FAIL gate:DRIFT (7 errors, 5 waived, all pre-existing/unrelated -- rapid_sweep.py, invariants.py, _evidence.py), FAIL gate:DSL (1 error: tests/test_app.py:387, pre-existing noqa-comment directive, unrelated), FAIL gate:SELFAUDIT (1 error: SYS111 testsuite-glob via-list pending-auto-accept, unrelated to my files) -- ZERO findings attributable to src/frob/gates/_docptr.py, src/frob/tickets/_land_squash.py, tests/gates/test_docptr.py or tests/tickets/test_land_squash.py. frob check --only arch --files src/frob/gates/_docptr.py --files src/frob/tickets/_land_squash.py --files tests/gates/test_docptr.py --files tests/tickets/test_land_squash.py --base dev -> pass gate:frob-arch (21 warnings, 546 suggestions, none naming my 4 files as an ARCH001/LARGE001 offender) frob check --only coverage --files src/frob/gates/_docptr.py --files src/frob/tickets/_land_squash.py --files tests/gates/test_docptr.py --files tests/tickets/test_land_squash.py --base dev -> FAIL gate:COV (10 errors, all pre-existing in unrelated files: _cli_parsers/_check.py, graph/dsl.py, strata/_effects.py, strata/_unity_asmdef.py, tickets/_land_compose.py, _cli_parsers/_core.py, gates/_waive.py, testing/_dotnet_runner.py, plus COV006 findings in tests/gates_suite/test_fix_engine.py, tests/gates_suite/test_sys.py, tests/test_gate_cache.py, tests/test_lang.py), FAIL gate:TODO (5 errors, all in unrelated files: gates/_coverage.py, perf/_dup_spawn.py, perf/_loop_effects.py, strata/_secrets.py) -- ZERO COV001/COV002/COV006/COV007/TODO002 findings attributable to my 4 touched files; my two new private functions carry explicit frob:tests directives. ruff check src/frob/gates/_docptr.py src/frob/tickets/_land_squash.py tests/gates/test_docptr.py tests/tickets/test_land_squash.py -> All checks passed! ty check src/frob/gates/_docptr.py src/frob/tickets/_land_squash.py tests/gates/test_docptr.py tests/tickets/test_land_squash.py -> All checks passed! ## Cross-ticket lease check `git diff --name-only dev...HEAD`: src/frob/gates/_docptr.py, src/frob/tickets/_land_squash.py, tests/gates/test_docptr.py, tests/tickets/__init__.py, tests/tickets/test_land_squash.py, tickets/T-5126/ticket.md -- all 6 are exclusively in this ticket's own lease (.git/frob-leases/T-5126.json); `grep -l <path> .git/frob-leases/*.json` confirms none of the 5 non-ticket-md files is held by any other lease file.
