bump: minor
T-5212: Wires frob.graph.affects.public_caller_dependent_files (T-4560) into _rapid_caller_dependents so the rapid land's --files dependents scope actually picks up callers of a changed PUBLIC symbol, not just private (underscore-named) ones. Before this fix, a caller reaching a changed symbol only through its public name (from target import changed) was invisible to the synchronous pre-land check's scope -- only the private-only CallGraph T-4553 built was consulted. Positive control: test_public_symbol_caller_is_included plants a caller.py that imports target.changed() (no underscore) and asserts frob ticket land's rapid --files scope includes caller.py; it fails without this wiring and passes with it, alongside the existing private-callee case which stays green.
