bump: minor
T-4275: Confirmed the ticket's premise on current main: `_ty_check_files` has no `resolve_root` parameter, unlike its already-fixed sibling `_ruff_check_files` (T-4257), and feeding it empty stdout/stderr (the exact Windows-measured spawn-failure shape T-4257 fixed for ruff) parses as a fabricated CLEAN `ToolResult` rather than `None`, silently undercounting a broken baseline or current-pass identically. Ported both of T-4257's fixes to `_ty_check_files`: split `resolve_root` (defaulting to `worktree`) from `worktree` itself so `_ty_baseline_diagnostic_identities`'s disposable, `.venv`-less snapshot resolves `ty` from the real owning worktree; and added the same bare-PATH `sys.executable -m ty` fallback plus an empty-stdout-and-stderr guard that reports `None` (unmeasurable) instead of a fabricated clean result. Extracted `_spawn_ty` (the shared subprocess-and-except shape) to keep `_ty_check_files` from crossing ARCH001's line threshold now that it carries two spawn attempts. Evidence: two new tests in `tests/test_ticket_land_ty_diff_attribution.py` (the repo's existing dedicated ty-diff-attribution test module, chosen over the giant test_ticket_work_and_land_finish.py specifically to avoid an unrelated SCOPE002 cross-reference cascade from that file's own pre-existing content). Verified fail-then-pass by hand: reverted _land_cmd.py to HEAD and reran both new tests -- one failed with TypeError (no resolve_root kwarg existed yet), the other failed asserting a fabricated clean ToolResult was not None -- then restored the fix and both passed. `pytest tests/test_ticket_land_ty_diff_attribution.py` (6 tests, including the 4 pre-existing ones) is green. Verified with `frob check --ticket T-4275 --json` (unscoped): fixed ARCH001/LANDPARITY002 (the function crossed the 60-line threshold -- resolved by extracting _spawn_ty), FMT001 (two overlong frob:tests directive lines -- wrapped with the standard `\` continuation form) and LANDFMT001 (ruff format). The remaining errors are either pre-existing and unrelated to this ticket's files (ARCH103 in src/frob/graph/cache.py, SELFAUDIT001 on design:1 claude_hooks capabilities, WIRE002 in tests/test_ci_workflow_timeout.py, TODO002 in src/frob/gates/_land_format.py) or SCOPE002 findings inherent to this ticket's own ORIGINALLY DECLARED scope (both files were already listed in tickets/T-4275/ticket.md's scope before I touched anything) -- a consequence of _land_cmd.py's own size and cross-reference density, not of anything added here. Filed: none -- no out-of-scope defect discovered while working this ticket.
