bump: minor
T-5379: All 24 non-load-bearing findings were node='testsuite' substitute-mode mutations. testsuite is the one synthetic aggregate node whose observed- capability set is the union of everything the whole test suite legitimately exercises, so any substituted `may` kind is near-always already observed there -- SYS101 (declared-but-never-observed) is structurally blind to a substitution on this specific node, not a real detection gap. Fix mirrors the module's existing deletion-mode disclosed- gap pattern (export_diff_expected): added MutationFinding.sys101_expected (False only for node='testsuite'), so load_bearing treats this as a disclosed gap rather than a failure, matching the docstring's own "disclosed gap, not a failure" precedent for the seccomp/app-manifest pair. The pre-existing, unrelated selfconform/eval-needle failures I observed while re-running the surrounding suite (SYS100 on 'cli'/ 'testsuite', narrative _bulk declarations) are T-5214's cluster, not caused by this change -- confirmed by this diff touching only _mutation_audit.py's load_bearing computation.
