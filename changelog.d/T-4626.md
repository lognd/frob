bump: minor
T-4626: # Why (T-4626) Auto-filed by the coordinator dispose loop: `COV002:tests/unit/rapid_sweep_ suite/test_dispose.py COV002:tests/unit/rapid_sweep_suite/test_filing.py`. ## Root cause T-4631 and T-4632 (docstring-only DOCARCH001 cleanups) rewrote several test docstrings in these two files. Each rewritten test's only `frob: ticket` edge(s) named a ticket that has since closed (T-2208/T-2352/T-2604/T-3051, all `[done]`) -- COV002 requires an edge to an OPEN ticket, so a changed symbol whose only edges point at closed tickets now flags. ## Fix Declared this ticket's scope as exactly the two named files (it arrived with an empty scope), then added a `# frob:ticket T-4626` line alongside each existing (now-closed) edge at the four flagged symbols -- one in `test_dispose.py` (`test_leaves_quarantine_raised_when_other_findings_ remain_undisposed`), three in `test_filing.py` (`test_filed_ticket_scope_is_relative_end_to_end`, `test_closed_ticket_attribution_still_raises`, `test_duplicate_finding_disposes_to_declaring_ticket_instead_of_ dropping`) -- rather than replacing the closed edges, preserving the historical trail to T-2208/T-2352/T-2604/T-3051. ## Verification - `ruff check`/`ruff format --check` on both files: clean. - `nice -n 10 uv run pytest tests/unit/rapid_sweep_suite/test_dispose.py tests/unit/rapid_sweep_suite/test_filing.py`: 57/57 passed. - `frob check --files <both files> --only ruff --only ty`: PASS, 0 errors, 0 warnings.
