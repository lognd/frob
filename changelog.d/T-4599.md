bump: minor
T-4599: `frob ticket land` had a silent ~111s span between rapid `--files` scoping and the worktree auto-sync onto main with no phase-transition logging, so a stalled land looked identical to a slow-but-healthy one from the log alone. Added phase-marker logging around the auto-rebase/worktree-sync and post-land sweep-dispatch entry points so each transition is now visible. Merged dev (T-4607's already-landed `.git/`-metadata-path fix in `_rapid_sweep.py`/`_fix_engine_sync.py`, identical in substance to this worktree's own earlier T-4599 copy of the same fix) by taking dev's already-landed wording and dropping the duplicate.
