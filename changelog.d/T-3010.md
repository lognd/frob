bump: minor
T-3010: Adds T-3004 section 6's milestone-scoped closure: a sixth, opt-in closure rule (check_milestone_closure) over the existing V-model graph kernel so a configuration can bind a partial architecture as long as every uncovered obligation is a declared, reasoned gap, never silent omission. Mirrors vmodel_check's existing PyO3/gate shape end to end (Rust unit tests, Python PyO3-boundary tests, gate-level tests) and generalises frob.lang._support's FacetState.KNOWN_GAP registry pattern to V-model artifacts (MilestoneGap/MILESTONE_GAP_REGISTRY) per the owner's dispatch instruction. Verified with cargo test (227 passed) and the targeted pytest suite (19 new tests passed) plus a clean vmodel/milestone_closure gate sweep against the touched files.
