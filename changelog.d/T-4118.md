bump: minor
T-4118: T-4118 -- H3-6: a failure-injection test must assert on every response field, not only the field the test-plan row named WHAT changed: - docs/guides/extending/failure-injection-acceptance-criteria.md (new): authoring guidance for writing a failure-injection acceptance-criteria row on a health/probe/status-roll-up component. States the rule (name every response field the test must assert on, or explicitly exclude a field and say why) and a worked BAD/GOOD example matching the H3-6 incident shape (BAD: "reports redis: down" -- names only the injected dependency's own field, silently permits a roll-up `status` field to stay wrong; GOOD: names redis, the roll-up status field, AND the unaffected db field, with an explicit exclusion for a timestamp field). WHY (and why this ships as documentation, not code): F-307 H3-6 SITS APART from the other nine leaves per the parent epic's own classification: it names a PROCESS gap in how a test-plan acceptance row gets WRITTEN, not a code-analysis gap. T-4118's own body says to locate this repo's real ticket-acceptance-criteria authoring guidance (the scope's named starting-guess file, src/frob/tickets/_acceptance_ template.py, was explicitly only a guess) and retarget scope if it lives elsewhere, or ship documentation-only if this repo has no existing lint over acceptance-criteria TEXT to reuse. Confirmed during implementation: src/frob/tickets/_acceptance_template.py does not exist anywhere in this repo -- there is no dedicated ticket- acceptance-template module. There is also no existing lint over ticket/acceptance-criteria prose SHAPE anywhere in this codebase (only gate rules over CODE/doc-anchor text, a structurally different target). Per the ticket's own explicit fallback ("this may ship as documentation- only... do not invent a speculative AST check against ticket prose that the rest of the gate set has no precedent for"), this ships as authoring guidance only. FLAGGED EXPLICITLY per the ticket's own instruction: this is the one leaf of ten with no automated fixture/detector at all -- the "fixture" is the worked BAD/GOOD acceptance-criteria row placed directly in the guidance doc, exactly as the ticket's own Fixture note specifies. Scope retargeting: `frob ticket scope T-4118 --remove src/frob/tickets/ _acceptance_template.py --remove tests/unit/tickets/test_acceptance_ template.py` (neither file exists) and `--add docs/guides/extending/ failure-injection-acceptance-criteria.md` (this repo's real home for this class of authoring guidance -- matches the existing `docs/guides/ extending/litmus-fixtures.md`-style "how to extend/author X" doc pattern, T-0145's own precedent). Acceptance proof: - The doc's own BAD example matches the H3-6 incident shape exactly (only the field under direct test named; roll-up field silently unchecked). - The doc's own GOOD example names every response field (the injected dependency, the roll-up field, the unaffected dependency) or excludes one with a stated reason -- proving the rule is followable, not just stated in the abstract. - "Docs row": since docs/modules/gates.md (this sprint's shared rule- table home) is not the right home for a process/prose-authoring rule in the first place (it is not a gate rule), no fold-in to that file is needed for this ticket -- unlike T-4114/T-4115/T-4221's actual gate rules this sprint, T-4118 has nothing to register there. Evidence: kind=ux is in CMD_EVIDENCE_ALLOWED_KINDS, so evidence is a non-pytest --evidence-cmd (frob.tickets._evidence's documented D-02 route for docs/ux-kind tickets): cmd: grep -n "^\*\*GOOD\*\*\|^\*\*BAD\*\*\|roll-up" docs/guides/extending/failure-injection-acceptance-criteria.md exit=0, sha256=bd63b2c2a141 (recorded on T-4118) This proves the doc file exists and carries both worked examples and the roll-up-field framing the rule turns on -- the strongest evidence shape available for a prose-authoring change with no runnable detector. Commit shas (worktree t-4118, branch t-4118): - bce9f7f0a -- docs(tickets): add failure-injection acceptance-criteria authoring rule - 133f7caa0 -- chore(tickets): record evidence for T-4118 (auto-commit) HEAD: 133f7caa0 Gates run (measured, --only/--files forms): - `frob check --only coverage --files docs/guides/extending/failure-injection-acceptance-criteria.md --base dev`: zero findings attributable to this file (grepped the full output for "failure-injection" -- no hits at all; repo-wide COV/DRIFT/DSL/TODO baseline noise unrelated). - `frob check --only arch --files docs/guides/extending/failure-injection-acceptance-criteria.md --base dev`: `pass frob-arch`. - No ruff/ty applicable (Markdown, not Python). - Cross-ticket lease check: `git diff --name-only dev...HEAD` lists exactly docs/guides/extending/failure-injection-acceptance-criteria.md and tickets/T-4118/ticket.md, both resolving only to T-4118's own lease. Scope refusals: none (the retarget above was T-4118's own explicit correction of a starting-guess scope, not a refusal against another ticket's lease). Filed: none new -- this ticket's own explicit "documentation-only, no fixture" posture means there is no code-side follow-up to hand off (unlike T-4114/T-4115/T-4221, which each filed/extended T-draft-9a4eb7be for gates.md registration). Waivers added: none. Tests skipped: none (none applicable -- no runnable check exists for this leaf, per the ticket's own explicit instruction).
