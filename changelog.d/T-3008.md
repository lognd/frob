bump: minor
T-3008: Adds T-3004 section 7's multi-level invariants: Invariant gains optional level/evidence_levels fields (schema-only, L1) so a declared invariant can name its V-model artifact level and tag individual evidence entries with the level they were verified at, then INVLVL001 (L2, new id per owner review since INV003 already exists) flags a tagged entry whose level does not match the invariant's paired test level. Both additions are strictly optional so no existing invariants/*.md file is affected. Not yet wired into frob check's dispatch table: src/frob/gates/__init__.py and _waive.py were held by a concurrent T-3010 scope lease throughout this ticket's work; filed T-draft-56e0c50e to finish the registration once that lease frees. Verified with the new gate's 5 unit tests plus the full existing INV-family suite (86 tests total, all passing).
