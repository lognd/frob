bump: minor
T-5525: TODO001 (and every other diff-dependent gate) hard-failed with a load failure whenever `git merge-base HEAD main` had no local `main` ref to resolve against -- exactly the shape a shallow CI checkout produces (`actions/checkout` default depth fetches origin/main as a remote-tracking ref, never a local branch). Fixed `_merge_base` to retry against `origin/<base>` when the first attempt fails and `base` has no `/` in it already (so an explicit remote ref is never double-tried). Added two tests: one that reproduces the shallow-checkout shape directly (a remote-tracking ref exists, the local branch does not) and asserts the diff still resolves, and one confirming an already-remote-qualified bad ref still surfaces GitFailed (no silent second spawn).
