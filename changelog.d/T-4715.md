bump: minor
T-4715: T-4715 -- Source narrative C7: perf/ process/ refactor/ release/ scaffold/ security/ serve/ stats/ -- Done report ## Scope actually swept Ticket scope was narrowed at start time (`frob ticket scope T-4715 --remove src/frob/scaffold/_managed.py`, then `--remove src/frob/scaffold/project.py`) because both files were leased by in-progress T-4760 -- collisions recorded in the ticket body, files NOT touched. Final swept scope (19 files): src/frob/perf/_redundancy.py (fully exempt, no edit) src/frob/perf/_rules.py (fully exempt, no edit) src/frob/process/_derived_lock.py (fully exempt, no edit) src/frob/process/_lock.py src/frob/process/_pid_liveness.py src/frob/process/_proc_scan.py src/frob/process/parsers/common.py (fully exempt, no edit) src/frob/process/parsers/ruff.py (fully exempt, no edit) src/frob/process/parsers/ty.py (fully exempt, no edit) src/frob/refactor/_transaction.py src/frob/refactor/_verify.py src/frob/release/__init__.py src/frob/release/_cli.py (fully exempt, no edit) src/frob/security/_redact.py src/frob/serve/_daemon.py src/frob/serve/_events.py (fully exempt, no edit) src/frob/serve/_socketd.py src/frob/serve/_tools.py (fully exempt, no edit) src/frob/stats/_agentic_dispatch.py (fully exempt, no edit) ## What changed, per file (WHAT / WHY) Same method as T-4719/T-4723: `frob narrative move <file> <line> [--keep-file F] --reason "..."` moved each over-12-line non-directive comment run's historical content into the ticket it cited, leaving a `# see T-#### for the history behind this` pointer. `--keep-file` was used at release/__init__.py::_bump_shortfall (kept every frob:doc/ frob:tests/frob:ticket/frob:waive directive, moved only the T-1636 doc-retargeting prose sandwiched between them). Tickets that received moved narrative: T-0918, T-3577 (process/_lock.py), T-3018 (process/_pid_liveness.py), T-2473, T-3072 (process/_proc_scan.py), T-3596 (refactor/_transaction.py), T-1885 (refactor/_verify.py), T-1381/ T-1636 (release/__init__.py), T-0427, T-0219, T-0157 (security/_redact.py), T-4258 (serve/_daemon.py), T-2981 (serve/_socketd.py). No no-citation blocks were found in this cluster's non-exempt runs. ## Collateral fixes (found and fixed within scope, not filed separately) A wholesale comment-run move sometimes carried away a real directive that happened to sit inside the same contiguous comment run as the narrative being migrated. Caught by `git diff dev -- src/frob | grep "^-.*frob:(doc|enforces|tests|waive|invariant|ticket)"` after each narrative-move batch, and fixed before committing: - process/_proc_scan.py::_is_frob_check_process -- a wholesale move at the top of its comment run swallowed the `frob:doc docs/modules/process.md#concurrent-check-advisory-t-2473` anchor and the `frob:waive COV007` that legitimizes documenting this private symbol under a shared section; restored both alongside the surviving `frob:ticket T-2473`/`T-3072` lines. - serve/_daemon.py::IDLE_TERMINATION_S -- a wholesale move of the T-4258 owner-directive narrative also removed the trailing `frob:waive COV001` that legitimizes this constant having no frob:doc anchor of its own; restored it. Both were real suppression/documentation directives, not narrative -- losing them would have surfaced false COV001/COV007 findings on land. `frob check --only coverage` against the full 19-file scope confirmed zero unwaived COV001/COV002 in scope files after these two restorations (every remaining scope-file COV001 hit already carries a pre-existing `[waived: ...]` tag, verified against `git show dev`). ## Acceptance criteria, how each is proven [1] Zero comment runs >12 lines remain in the 19 swept files. Verified with a repo-local awk scan for contiguous `#` runs >12 lines over every file in scope; every remaining >12 run is a frob:/noqa/type:/ruff:/mypy: directive-only run (syntax-exempt), confirmed by hand for each hit. [2] Every block that cited a ticket has its narrative readable in that ticket body, moved not deleted. Every `frob narrative move` call reported "moved N line(s) ... into T-####" with the ticket body's char count growing by the reported amount (session transcript). [3] Every block that cited no ticket has its narrative in T-4715's own body. N/A for this cluster -- every non-exempt run in these 19 files cited a real ticket; none needed the no-citation fallback. [4] `git diff -w` over non-comment lines is empty. `git diff -w -- src/frob | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -vE '^[+-]\s*#'` returns only two blank-line additions/removals inside a comment block in process/_proc_scan.py (a consolidation of two adjacent, originally separate `frob:ticket T-2473`/`T-2473`+`T-3072` directive groups into one, with the connecting blank lines collapsed) -- cosmetic whitespace inside comments, zero code lines touched. [5] `frob ticket list` exits 0 after each batch of ledger writes. Checked as a final check after all narrative-move/evidence batches: exit 0. ## Test node ids (all green, PYTHONPATH=<worktree>/src, serial) tests/unit/test_process_lock.py::TestDerivedStateLock::test_lock_file_created_under_frob_dir tests/test_perf.py::test_perf001_fires_on_list_membership_in_loop tests/test_release_worktree_lease.py::TestStampWorktreeLease::test_no_lease_succeeds Broader confirmation run (not bound as evidence, just a wider smoke check): `pytest tests/unit/test_process_lock.py tests/test_perf.py tests/unit/test_process_pid_liveness.py tests/test_release_worktree_lease.py tests/unit/test_release_stamp_guard.py tests/test_serve_events.py` -> 118 collected, 0 failed. All 5 acceptance criteria have evidence bound (3 node ids each), `frob ticket evidence T-4715 <3 node ids> --accepts N --base-ref dev` for N in 1..5, run AFTER the last code commit (7e2220b9a). ## Commits (in order) 7e2220b9a docs(narrative): condense C7 perf/process/refactor/release/security/serve/stats comment runs bfb07660e..70dd3d590 chore(tickets): record evidence for T-4715 (x5, one per criterion) (plus the earlier self-committing `frob ticket scope`/`start` calls that ran before any code edit) ## Pre-READY checks `frob check --only sys --files <19 files> --base dev`: gate:DOCARCH pass (0 errors, 552 warnings -- all WARN-tier docstring findings, pre-existing narrative-in-docstring debt outside this ticket's scope, unaffected by a comment-run edit); gate:DRIFT FAIL 6 errors/5 waived (identical baseline count, none in scope files); gate:DSL FAIL 1 error at tests/test_app.py:387 (pre-existing, unrelated file); gate:WAIVE pass (0 errors, 8 warnings -- the one scope-file WARN, serve/_events.py's WAIVE010, is pre-existing). `frob check --only arch --files <19 files> --base dev`: pass (21 warnings, 36 waived, 546 suggestions repo-wide, identical to the T-4719/T-4723 baseline) -- 0 errors. `frob check --only coverage --files <19 files> --base dev` (run after the fix-up commit): 10 errors repo-wide, only 4 touching scope files (process/parsers/common.py::tool_no_output_result, release/_cli.py::add_release_status_parser/run_release_status_command, serve/_daemon.py::IDLE_TERMINATION_S), all already carrying a pre-existing `[waived: ...]` tag confirmed against `git show dev`. `ruff check <19 files>`: All checks passed! `ty check <19 files>`: All checks passed! ## Notes for the coordinator - Scope refusals: src/frob/scaffold/_managed.py and src/frob/scaffold/project.py removed from T-4715's declared scope at start time due to live lease collisions with T-4760; not swept, deferred. - No waivers added. - No tests skipped; full green run on the modules touched (118 tests). - Same lesson as T-4723's Done report: `frob narrative move`'s default (no --keep-file) removes the WHOLE contiguous comment run including any frob:doc/frob:waive/frob:ticket directive lines inside it. Always `git diff dev -- <scope> | grep "^-.*frob:(doc|enforces|tests|waive| invariant|ticket)"` after a wholesale-move batch and restore any real directive caught in the sweep.
