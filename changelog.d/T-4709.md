bump: minor
T-4709: # T-4709 -- why-file (READY) Worktree: /home/logan/projects/frob/.claude/worktrees/t-4709 (branch t-4709) HEAD: a484d2a4dd09e347fd8bdbb7aadb74098c6cab24 ## Scope note: _docptr.py handed off mid-task _docptr.py was originally in scope and fully condensed (commit 05a32c717, still present in this branch's history for the DOC006-fix agent to cherry-pick). The coordinator asked me to hand it to a critical land- blocking DOC006 fix (T-draft-a38af1c4). Per the coordinator's exact instructions I: 1. `git checkout dev -- src/frob/gates/_docptr.py` (commit d8e7b5456) to restore its dev content in this worktree. 2. `frob ticket scope T-4709 --remove src/frob/gates/_docptr.py --reason "handed to T-draft-a38af1c4 with its condensation commit 05a32c717"` (commit 3557729dd) -- ticket scope is now the other 20 files. The 7 tickets _docptr.py's narrative moved into (T-3843, T-3979, T-2703, T-0731, T-2131, T-3489, T-2374) already carry that narrative regardless of which agent's commit ships the source-file change -- those ticket writes happened via `frob ticket body` from ROOT and are independent of this handoff. ## What changed (source files, comment-only) 12 files, condensed 20 over-length comment runs down to <=12 lines each, narrative moved into the ticket each block cited: - src/frob/gates/_arch.py (2 runs) -> T-1034, T-1020 - src/frob/gates/_bug_repro.py (11 runs) -> T-1421, T-1748, T-1616, T-2193, T-3104, T-2480, T-2025 - src/frob/gates/_debt_deprecated.py (2 runs) -> T-0576, T-0639 - src/frob/gates/_docblocks.py (1 run) -> T-2231 - src/frob/gates/_fix_engine.py (2 runs) -> T-2935, T-1261, T-1341, T-1870, T-1916, T-1548, T-2400 (SYS100-removal block references T-2922's fuller history in _fix_engine_sync.py rather than repeating it) - src/frob/gates/_fix_engine_scope.py (1 run) -> T-2284 - src/frob/gates/_fix_engine_sync.py (6 runs) -> T-2922, T-1620, T-1886, T-2011, T-2001, T-4607 - src/frob/gates/_fix_engine_text.py (1 run) -> T-1391 - src/frob/gates/_flag_coverage.py (1 run) -> T-4147 - src/frob/gates/_fmt_directives.py (2 runs) -> T-1606, T-0441, T-4473 - src/frob/gates/_lang_conformance.py (4 runs) -> T-2365, T-3541, T-1599, T-2682, T-2698, T-1234 - src/frob/gates/_markdown_scan.py (1 run) -> T-1700 Plus a small follow-up commit (65c4c4ee6): three symbols whose attached comments I condensed (`_BugReproOutcome`, `_apply_capability_ratchet_ bumps`, `_canonical_lines`) tripped COV002 (changed public/private symbol with no frob:ticket edge); added `# frob:ticket T-4709` directives to each, verified COV002 clears. 8 other files in this cluster's original scope (_cve_fingerprint_scan.py, _dead_symbols.py, _decisions_compliance.py, _docblocks_schema.py, _exhaustive_handling.py, _ffi_boundary.py, _gate_cache.py, _opaque.py) were inspected and found to already be under DOCARCH002's 12-line cap once `frob:` directive lines (frob:enforces/frob:tests/frob:doc/ frob:waive, including their `\`-continuations) are correctly excluded from the count -- the raw comment-run length looked high only because a crude line-count doesn't understand directive syntax. No changes needed; verified with the same directive-aware measurement script used throughout (see Evidence below). ## Why each split was made the way it was For every over-length run, the text that stays in code is the load- bearing invariant/gotcha (what a future maintainer needs to not repeat a mistake or misunderstand a non-obvious mechanism); the text that moved is historical framing (which ticket superseded which, a prior incident's narrative, measured-number provenance, cross-reference chains). Full per-block reasoning is in each commit message and in the condensed append text now living in the cited tickets' bodies (each begins "T-4709 follow-up (condensed from ... trimmed for DOCARCH002's 12-line cap)" so it's discoverable by grep). No no-ticket blocks existed in this cluster's files (the two apparent ones in _docblocks.py were pure frob:tests directive runs, not narrative) -- acceptance criterion 3 is proven vacuously true, not skipped. ## Move-never-delete / archived-ticket proof The very first move (src/frob/gates/_arch.py:91 -> T-1034, a `[done]` archived ticket) proves the archived-ticket body-append path works without the DuplicateId ledger hazard NARRATIVE.md constraint 3 warns about. `frob ticket list` was run after every batch throughout the session (dozens of times) and exited 0 every time, including immediately after that first archived-ticket write. ## Idempotency Re-running `frob narrative move src/frob/gates/_arch.py 91 --dry-run` after the first move reported "already migrated -- no-op" (T-2994 constraint 4). ## A real mistake caught and fixed mid-sweep While condensing BugReproOutcome.TIMEOUT's docstring in _bug_repro.py, a line-slice edit accidentally deleted the `TIMEOUT = auto()` enum-member statement along with its docstring. Caught immediately by running `python3 -c "import ast; ast.parse(...)"` after every single edit from that point on (a discipline applied to literally every edit in this session) plus a `git diff -w` non-comment-line check; the missing line was restored before the block was ever committed. No commit in this branch's history ever contained the bug. ## Pre-READY checks `frob check --only sys --files <12 touched files> --base dev`: gate:WAIVE 0 errors, 8 warnings, 0 unresolved, 0 waived (pass) gate:DOCARCH 0 errors, 552 warnings, 0 unresolved, 0 waived (pass) gate:DRIFT/DSL show pre-existing failures on files NOT in this ticket's scope (_land_cmd.py, invariants.py, _evidence.py, _rapid_sweep.py, tests/test_app.py) -- zero SELFAUDIT001 anywhere in the run, zero findings on any of the 12 touched files. `frob check --only arch --files <12 touched files> --base dev`: pass frob-arch: 21 warnings (36 waived), 546 suggestions. No ARCH001, no LARGE001 error on any touched file (large-file/high-coupling/ pattern-recommendation entries against _bug_repro.py, _debt_ deprecated.py, _docblocks.py, _fix_engine.py, _fix_engine_sync.py, _fmt_directives.py, _lang_conformance.py are all pre-existing "suggestion"-tier findings, not new errors -- this pass only removed lines). `frob check --only coverage --files <12 touched files> --base dev` (run twice -- before and after the COV002 fix commit): First run: 13 gate:COV errors, 3 of them COV002 on files I touched (_bug_repro.py::_BugReproOutcome, _fix_engine_sync.py:: _apply_capability_ratchet_bumps, _fmt_directives.py::_canonical_lines). Fixed by adding `# frob:ticket T-4709` directives (commit 65c4c4ee6). Second run: gate:COV 10 errors, zero COV002 remaining anywhere in the output; the remaining 10 are COV006/COV007 on files this ticket does not touch (src/frob/vet/**, tests/gates_suite/test_fix_engine.py), pre-existing. `ruff check <12 touched files>`: All checks passed! (two pre-existing "Invalid # noqa directive" warnings on _fmt_directives.py -- ruff misparsing a quoted `# noqa: E501` example string inside a comment explaining that exact mechanism; present before this change too, not a regression, and only a warning, not an error). `ty check <12 touched files>`: All checks passed! ## Cross-ticket lease check `git diff --name-only dev...HEAD` (source files only): all 12 files are inside T-4709's own lease (`.git/frob-leases/T-4709.json`, after the _docptr.py removal). No file is leased by another in-progress ticket. ## Merge with dev dev had moved substantially (250 files, mostly unrelated) since this worktree branched; merged with `git merge dev --no-edit`. One real conflict: tickets/archive/T-1421/ticket.md, because the initial `frob narrative move` (run before I fully understood the ROOT-vs-worktree rule) committed its ticket-body write inside this worktree, while a later manual `frob ticket body --append-file` (run correctly from ROOT per the brief's own clarification) landed on dev in parallel. Resolved by keeping BOTH body_changes entries and BOTH appended narrative blocks (nothing dropped, no duplication of identical text -- the two appends carry different content). Verified no other conflicts, `frob ticket list` still exits 0 post-merge, and all 12 source files remain comment-only-diff and syntax-clean after the merge. ## Test evidence 538 tests from the ticket's own `## Verify` pytest list (the subset that exists in this tree) passed, 0 failed: `PYTHONPATH=$(pwd)/src python -m pytest tests/gates/test_bug_repro_at_ref_public.py tests/system/test_cli_arch.py tests/system/test_fleet_status_ticket_readiness_arch001.py tests/test_arch_gate.py tests/test_arch_near_duplicate_native.py tests/test_docblocks_gate.py tests/test_gate_cache.py tests/test_gates_fix_engine.py tests/test_gates_fmt_directives.py tests/test_lang_conformance_gate.py tests/unit/gates/test_exhaustive_handling_path_shape.py tests/unit/gates/test_ffi_boundary_path_shape.py tests/unit/gates/test_markdown_scan.py tests/unit/strata/test_cve_fingerprint_scan.py tests/unit/test_app_config_flag_coverage.py tests/unit/test_arch.py tests/unit/test_arch_ocp.py tests/unit/test_arch_python_native.py tests/unit/test_arch_srp.py tests/unit/test_arch_table_schema.py tests/unit/test_docblocks_table_schema.py tests/unit/test_fix_engine_journal.py tests/unit/test_flag_coverage_gate.py tests/unit/test_lang_strata_entity_arch.py tests/vet_suite/test_opaque_indirection.py -p no:cacheprovider -q -o addopts=""` -> `SUITE-RESULT: exitstatus=0 collected=538 failed=0` / `538 passed in 266.28s` ## Acceptance evidence (bound via --evidence-cmd, docs-kind rules) - [1] zero runs >12 lines: verify_t4709_runs.py (re-derives the directive-aware measurement) -- exit=0, CLEAN - [2] ticket-cited narrative readable in cited ticket bodies: verify_t4709_narrative_moved.py checks all 43 cited tickets for a "T-4709" mention in their body -- exit=0, CLEAN (all 43 present) - [3] no-ticket blocks in cluster ticket body: verify_t4709_no_ticket_ blocks.py re-derives the measurement restricted to ticket-free runs -- exit=0, CLEAN (zero existed, proven not assumed) - [4] git diff -w non-comment-empty: verify_t4709_nonbehavior.sh -- exit=0, CLEAN - [5] frob ticket list exits 0: bound directly as the evidence command -- exit=0 All five bound via `frob ticket evidence T-4709 --evidence-cmd ... --accepts N --base-ref dev`, after the last commit (a484d2a4d), per the checklist's "bind evidence AFTER the last commit" rule -- the merge commit and the COV002 fix commit both predate every evidence-cmd binding above. ## Gates: frob check clean per the narrowed --only/--files forms above (see Pre-READY checks) ## Filed: none (no out-of-scope work discovered this session beyond the DOC006 handoff, which was coordinator-directed, not self-discovered)
