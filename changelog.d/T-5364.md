bump: minor
T-5364: Every SEO/WEBPERF rule in the T-5140 web-app epic needs the same three facts about a route's <head> (title, meta/link tags, JSON-LD), and re-querying the DOM per rule would multiply parse cost by the number of rules in the family. This ticket adds the one shared walk: extract_page_metadata normalizes one .html/.jsx route file's <head> (or JSX <Head> wrapper, the Next.js convention) into a frozen PageMetadata, reusing T-5302's framework detection contract and T-5300's html/jsx grammar wiring through frob.lang.raw_tree rather than re-detecting frameworks or standing up a second tree-sitter parser. build_duplicate_ title_index folds a route table's worth of PageMetadata into the cross-route duplicate-title index every downstream SEO rule reuses instead of re-scanning the table itself, matching the ticket's planted duplicate-title fixture pair.
