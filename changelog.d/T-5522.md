bump: minor
T-5522: Measured on T-5477's own land (/tmp/land-T-3010.log ~line 31200): the apply-failed branch of _attempt_ledger_only_rebase retried an IDENTICAL diff-and-apply against an UNCHANGED sibling tip 5 times, deterministic and wasted every retry, then refused -- the promised "falling back to full recompose" never actually happened. Fix adds _recompose_via_merge, a real 3-way git merge in a disposable worktree, tried when the cheap diff-and-apply fails, before falling back to the retryable apply_failed outcome. Only a genuine content conflict in both the diff-apply AND the merge still refuses (and, per the drift-proportional retry limit, may still resolve on a later sibling tip). Positive control: a composed commit and a sibling ledger-only commit touching the SAME tickets/** file at different, non-overlapping lines -- a real case where textual git apply fails (context mismatch) but a 3-way merge succeeds. Manually verified as a genuine repro (fails at dev, passes at the fix) since test and fix share this worktree's own commit history in fix-then-test order.
