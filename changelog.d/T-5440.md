bump: minor
T-5440: Fixes the in-scope, non-webapp/sql subset of this post-land sweep's findings: PERF004 (a per-ticket sorted() call in _reconcile.py, waived as genuinely per-iteration-varying data, not a shared re-sort), SEC110 (FROB_SELF_MATCH_ACK env read in pgrep-self-match-guard.py, waived as a boolean flag not a secret, matching the existing FROB_SUGGEST_ACK precedent), and TEST010 (self_match/main's frob:tests directives moved off the production file onto their covering test, per T-4710's redundant-directive shape). Re-measured (family-scoped checks, not a full gate suite run) rather than fixed on faith: COV002 against sync-claude-config.py/_common.py no longer reproduces. ARCH104:_taint_gate.py is deferred (live lease held by T-5372). Every remaining finding in this ticket's body lives inside src/frob/webapp/** or src/frob/sql/**, out of touch-scope, deferred whole -- not fixed in this pass.
