bump: minor
T-5394: derive_capability_registry()'s 5 capability-status functions were blanket (every supported_languages() member IMPLEMENTED for all 5). Read each thin walker (_walk_css.py/_walk_html.py/_walk_javascript.py/ _walk_vue.py) to decide per (language, capability) cell honestly: - symbol_walk: IMPLEMENTED for all 5 -- every walker genuinely walks >=1 top-level symbol kind. - publicness: IMPLEMENTED only for javascript (real export/export- default signal, mirrors typescript). KNOWN_GAP for css/scss/html/vue (T-5386 css/scss, T-5390 html/vue) -- these languages have no visibility keyword at all (each walker's own docstring says so), so the behavioral check's "observe both public AND private" proof is structurally impossible forever, not merely unimplemented -- matches T-2410's own precedent of treating a hardcoded-True placeholder as a real gap to disclose, not silently exempt. - doc_extract / directive_parse: IMPLEMENTED for css/scss/html/ javascript (real comment extraction verified). KNOWN_GAP for vue (T-5390) -- _walk_vue.py's COMMENT_TYPES is deliberately an empty frozenset(), by design (SFC-shell-only scope, no descent into a block's own inner language where its comments actually live). - call_graph: IMPLEMENTED only for javascript (real function/method calls, same 'name(' token shape frob.graph.callgraph's detector already resolves for python/typescript). KNOWN_GAP for css/scss/html (no calling convention exists) and vue (walker never descends into its <script> block's real body). Found and fixed one real, previously-latent bug while building the html fixture: frob.lang._common._strip_comment_delims had no `<!-- -->` branch at all -- html's doc_extract still passed (RawComment count >=1, delimiters or not) but directive_parse could never match a frob:<verb> directive inside an HTML comment regardless of registry claims, since the leading `<!--` was never stripped before dsl.py saw the text. Added the missing branch (same file already handles `//`, `/* */`, `#`). Also corrected a stale ticket citation: KNOWN_GAP_TRACKING_TICKETS and _PENDING_FACET_WIRING_TICKETS both cited "T-draft-c0e05ddc", which was already promoted to T-5390 by the time this ticket started -- updated both to T-5390 so the checked-in ticket ids resolve to real, currently open tickets. Verified: all 128 tests in tests/test_lang_conformance_gate.py pass (the full 30-param matrix plus test_real_registry_is_behaviorally_clean and test_directive_continuation_folds_correctly_not_just_present, the two tests the coordinator named explicitly), plus 159 in tests/test_lang.py + tests/test_lang_support.py (no regression from the _strip_comment_delims change). css/scss/html needed the SAME single- physical-line directive fixture shape C/C++/cuda already use (their block/<!-- --> comment forms are one grammar-merged node spanning however many physical lines -- confirmed empirically the same way the existing C/C++ comment documents; added css/scss/html to that test's existing exemption set for the continuation-fold acceptance test). Hand-checked the two things the dry-run does not measure (per T-draft- c7aa1ed2): this diff touches no new file-reading module (only existing modules' internal logic/dicts) so there is no new fs.read via-list gap in design/frob.strata to add; this diff touches no docs at all (only src/tests), so there is no illustrative-tracked-file-path risk in touched docs -- both checks are genuine no-ops for this diff, not skipped.
