bump: minor
T-4694: Registered frob narrative move's migrate_block/set_body engine as DOCARCH002 check 2's Tier-A --fix handler so frob check --fix moves a flagged citation run automatically instead of only printing the remedy. The ledger write happens before the file write (T-2994 constraint 1: a half-applied fix never loses narrative from both places), reuses set_body's T-2678 archived-ticket routing (constraint 3) and migrate_block's own marker check for idempotency (constraint 4), and docs/commands/narrative.md now discloses that the fix always moves the whole cited run rather than attempting the load-bearing/ archaeology split (constraint 2). All 5 acceptance criteria bound to tests in tests/narrative/test_docarch002_fix.py.
