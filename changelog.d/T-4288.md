bump: minor
T-4288: Changed: - tests/unit/test_app_runners_batch7.py::_ECHO_VERIFIED_CMD (new module constant) -- replaces the literal string "echo verified" used as --evidence-cmd in TestTicketEvidence.test_evidence_cmd_applied_for_docs_ticket and TestTicketArchive.test_archives_done_ticket with `f"{shlex.quote(sys.executable)} -c \"print('verified')\""`. Investigation (per the ticket's own request to establish whether the fix belongs in the test or the product): `_run_evidence_command` (src/frob/tickets/_evidence.py) has spawned --evidence-cmd as argv only, never through a shell, since T-0805 -- a deliberate, well-reasoned security fix (repo-writable ticket YAML handed to a shell is an injection-adjacent surface). T-0805's own survey of every real `cmd:` evidence entry ever recorded in tickets.md/tickets-archive.md found zero uses of a shell builtin; all real usages are plain argv (`grep ...`, `python3 <script>`, `uv run frob check ...`). So `echo` was never a real product requirement -- the two failing tests used it purely as a cheap "any command that exits 0 and prints something" placeholder, and that placeholder happens to work on posix (a standalone /bin/echo exists there) but has no standalone executable on Windows. Conclusion: this is a TEST-side fix, not a product defect -- the argv-only contract is correct and should not be reopened to accommodate a builtin no real evidence command relies on. The replacement is built from `sys.executable`, not a bare `python3` -- per this session's own earlier corrected premise (a bare `python3` on Windows can resolve to the Store app-execution-alias stub and exit 9009 rather than running anything), `sys.executable` is the actual guaranteed-spawnable cross-platform interpreter path. It is shlex-quoted so `shlex.split` (the evidence-cmd parser) re-splits it back into one token even when the interpreter path itself contains spaces (a real Windows condition, e.g. "C:\Program Files\..."). Per the coordinator's follow-up: measured (not assumed) whether the two sibling tests in the same file using other shell builtins -- test_evidence_cmd_silent_is_refused ("true") and test_evidence_cmd_failure_logs_error ("false") -- also fail on Windows, since they were not among T-4288's or T-4278's named Windows failures. On the real Windows mirror (winrun), both PASS. Root cause of the pass: `true`/`false` have no standalone executable on Windows either (confirmed via `where.exe true`/`where.exe false`, both empty), so the spawn fails with the same WinError2/SpawnFailed as `echo` did -- but both of these two tests only assert that evidence-cmd application was REFUSED with "ticket evidence-cmd failed" logged and a SystemExit, which is exactly what a spawn failure also produces. They pass on Windows, but for an accidental reason (spawn failure looks identical to their intended "silent success refused" / "nonzero exit refused" assertions), not because they exercise the behavior their names describe there. This is a useful negative result, not a defect: no code change needed for these two, and it's now known that this coincidence exists rather than assumed unmeasured. Filed: none. Gates: ran the full ungated-only pass (gates-fast, gates-native, gates-security, lint, static) via the required chunked loop under FROB_AGENT. All findings that are ERROR-tier at the gate:SCOPE/PRE level are the same pre-existing, already-documented noise measured and written up by the immediately-prior ticket touching this exact file (T-4278's Done report, landed as commit 6982c3a3f): PRE001 (pre- work-sweep-recency, cleared here via `frob ticket sweep T-4288`) and SCOPE002 (this shared, large test file's OTHER unrelated tests' frob:tests edges into production modules not in this ticket's declared scope -- widening scope to cover them is out of scope for a two-test Windows fix and was explicitly declined for the same reason by T-4155/T-4234/T-4278). gate:ARCH/SELFAUDIT/WIRE failures are repo-wide, pre-existing findings unrelated to and unaffected by this diff (touched files are tests/unit/test_app_runners_batch7.py only). ruff-check/ruff-format(touched file)/ty/frob-cycle/frob-dup/frob-arch/ frob-exports/claude-config-drift all pass on the touched file. Measured on real Windows (winrun, full mirror sync immediately before): all 7 tests in TestTicketEvidence + TestTicketArchive PASS (previously 2 FAILED). Also measured on Linux (uv run pytest): same 7 tests PASS.
