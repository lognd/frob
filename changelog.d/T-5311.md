bump: minor
T-5311: Adds WEBSEC123-125 (resource-exhaustion input-bounds) to close out the T-5140 WEBSEC epic's reserved rule-id block: unbounded schema field length (pydantic/Zod/class-validator), XML entity-bomb/XXE and JSON-bomb parser configs, and unbounded recursion on user-controlled input, all static-AST/regex-detectable resource-exhaustion gaps the corpus names. Follows T-5307's own precedent exactly -- one framework-gated scan module folded into the existing frob.gates._taint_gate.taint_gate call site, no second gate registration -- so it wires automatically wherever that gate already runs, with one positive and one negative fixture per rule id proving each fires and each does not false-positive on the bounded/guarded shape. Item 28 (business-logic step-skipping) is DYNAMIC-only per the corpus; filed as a frob:todo obligation in tests/unit/test_websec_bounds.py and documented in docs/modules/webapp-websec-bounds.md rather than shipped as a static rule, since no AST/regex shape distinguishes a correctly- ordered multi-step handler from one drivable out of order.
