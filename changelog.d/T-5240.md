bump: minor
T-5240: Added the missing inventory row (docs/guides/extending/registry_of_registries.json), a frob:doc anchor + fragment pointing at it from the nearest natural code anchor (src/frob/tickets/_models.py::AcceptanceCriterion -- this guide is authoring guidance for HOW to write a failure-injection acceptance criterion, and AcceptanceCriterion is the concrete model those criteria are stored in), and the matching _REGISTRY_PROBES entry tests/unit/test_extending_guides_complete.py::test_probe_table_and_inventory_agree requires in lockstep (missed on first filing -- the guide's own registry needs all three legs, not just the inventory row). Note: `ruff format` corrupted the JSON registry file (rewrote 2-space indent to 4-space WITH trailing commas, which is invalid strict JSON -- confirmed by json.load failing on the reformatted output). Reverted that and hand-edited the JSON in the original style instead; did not run ruff format on this file. Measured: tests/unit/test_extending_guides_complete.py (6 tests) pass clean; docs/guides/extending/registry_of_registries.json parses as valid JSON.
