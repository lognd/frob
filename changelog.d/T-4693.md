bump: minor
T-4693: T-4693: DOCARCH002 structural comment lint WHAT changed ------------ - src/frob/gates/_docarch_structural.py (new): DOCARCH002 family. - `_docs_config(root)`: reads `[gates.docs] comment_run_max`/`docstring_max` from frob.toml (defaults 12/20), missing-is-default posture matching `frob.gates._dup._dup_config`. - `_iter_comment_runs`, `_is_directive_run`, `_is_leading_header_run`: the syntax-only exemption machinery for check 1. `_is_directive_run` uses a MAJORITY (>= half) of a run's lines matching the `# frob:`/`# noqa`/`# type:`/`# ruff:`/`# mypy:`/`# pyright:`/`#!` marker regex -- matched to T-4691's own measurement script (scratchpad/struct.py) rather than re-derived, so the shipped baseline count is the number this repo was actually measured at. - `_docstring_spans`: docstring line-count spans via the stdlib `ast` module directly on raw source -- NOT `frob.lang`'s `doc_text`, which `frob/lang/_common.py` deliberately whitespace-collapses ("so reflow never changes doc_text"), the wrong substrate for a line-count cap. - `scan_comment_length`: check 1 (comment-run + docstring length cap). - `scan_citation_shape`: check 2 (T-#### citation must be a directive or a single-line "# see T-####" pointer; 2+ comment lines under a bare citation is a finding). - `docarch002_violations`: repo-wide scan, ratchet-severity-adjusted via `frob.gates._ratchet.resolve_ratchet_severity` when "DOCARCH002" is in `[gates.ratchet] rules`. - `docarch_structural_gate`: thin alias, the wiring point for `frob.gates.__init__`'s `GATE_RUNNERS` (see DEFERRED below). - tests/gates/test_docarch_structural.py (new): 10 cases covering both checks' must-fire/must-stay-quiet fixtures, the config-override control, and the ratchet-severity integration end to end. - frob.toml: added `[gates.docs] comment_run_max = 12` / `docstring_max = 20`, and `"DOCARCH002"` to `[gates.ratchet] rules`. - frob-ratchet.lock.json: baselined today's 5,660 DOCARCH002 finding keys (repo-wide scan, both checks) via `snapshot_ratchet` -- added to this ticket's scope (`frob ticket scope T-4693 --add frob-ratchet.lock.json`, unleased). - docs/modules/gates.md: added the DOCARCH001-sibling rule-catalog row, plus a full `## DOCARCH002` section (checks, exemptions, ratchet posture, and the registration-deferral note below). WHY --- Neither existing narrative rule can host a content-blind length cap without changing what it means: DOCARCH001 is docstring-only and keyed on WORDING; NARR001 only opens a block on a `# T-####:` LEAD line, so 82% of the measured comment bloat (376/447 runs, T-4691's own count) is invisible to it. DOCARCH002 is the new, syntax-only sibling the owner's directive asked for. Acceptance criteria (T-4693's ticket.md, all six) -------------------------------------------------- 1. 20-line pure-algorithm run, no ticket -> flagged. test_flags_long_pure_algorithm_run 2. 5-line run / 20-line frob: directive block / 20-line license header -> none flagged. test_short_run_is_quiet (not separately bound; same fixture family), test_directive_run_is_exempt, test_leading_license_header_is_exempt 3. 25-line docstring flagged, 15-line not, default docstring_max=20. test_long_docstring_flagged_short_is_quiet 4. `[gates.docs] comment_run_max = 30` silences the 20-line fixture. test_config_override_silences_default_fixture 5. frob:ticket / "# see T-1234" quiet; "# T-1234:" + 3 prose lines flagged, remedy names `frob narrative move`. test_directive_and_pointer_are_quiet, test_bare_citation_with_prose_is_flagged 6. WARN + ratchet baseline; a brand-new over-cap run reports ERROR. test_baselined_finding_stays_warn_new_one_errors NARRATIVE.md's extra positive controls --------------------------------------- - Config-path control: same as (4) above -- proven live, not just the default. - 97-exempt-directive-runs / baseline-507 control: `_is_directive_run`'s majority rule was calibrated directly against scratchpad/struct.py (the planner's own measurement script) rather than re-derived from the ticket text. Re-running an equivalent algorithm over src/frob/**/*.py measured 98 directive-exempt runs and 493 non-directive over-cap runs (506 total before header exemption, vs. the narrative's 507/97) -- within 1, attributable to this repo having moved by a commit or two since the planner's snapshot, not to a different algorithm. The committed frob-ratchet.lock.json baseline is the actual live measurement at land time, which is the number that matters operationally (today's count stays WARN; the NEXT one errors). DEFERRED (leased files, per BRIEF's cross-ticket-lease protocol) ------------------------------------------------------------------ Two registration hunks are NOT applied, because both target files were leased by other in-progress tickets at land time: 1. `src/frob/gates/__init__.py` (leased T-3962): wire the gate into `GATE_RUNNERS`. The exact hunk (mirrors NARR001's own wiring site, `narrative_blocks_gate`'s entry): ```python from frob.gates._docarch_structural import docarch_structural_gate ... GATE_RUNNERS["docarch_structural"] = lambda: docarch_structural_gate(st.repo_root) ``` 2. `src/frob/gates/_waive.py` (leased T-4212): add `"DOCARCH002"` to `_KNOWN_GATE_RULES`, and update docs/modules/gates.md's own top-of-file `frob:enumerates ... members="..."` directive (line 13) to include it -- the two must move together since the directive is verified against the live set. Land with the cross-ticket-lease flag noted; T-4693's own scope (docarch_structural.py itself, plus tests/frob.toml/docs/ratchet-lock) is otherwise self-contained and runnable standalone via direct import even before wiring lands (as the tests demonstrate). Filed: none (no out-of-scope work found beyond the two deferred hunks above, which are provenance-tracked in this report, not new tickets -- they are this ticket's own remaining scope, blocked only by two leases). Evidence bound -------------- - tests/gates/test_docarch_structural.py::TestScanCommentLength::test_flags_long_pure_algorithm_run - tests/gates/test_docarch_structural.py::TestScanCommentLength::test_directive_run_is_exempt - tests/gates/test_docarch_structural.py::TestScanCommentLength::test_leading_license_header_is_exempt - tests/gates/test_docarch_structural.py::TestScanCommentLength::test_long_docstring_flagged_short_is_quiet - tests/gates/test_docarch_structural.py::TestScanCommentLength::test_config_override_silences_default_fixture - tests/gates/test_docarch_structural.py::TestScanCitationShape::test_directive_and_pointer_are_quiet - tests/gates/test_docarch_structural.py::TestScanCitationShape::test_bare_citation_with_prose_is_flagged - tests/gates/test_docarch_structural.py::TestDocarch002RatchetSeverity::test_baselined_finding_stays_warn_new_one_errors Commits ------- - d2970d2c4 feat(gates): add DOCARCH002 structural comment/docstring lint - 2fb5e18e7 fix(gates): drop invalid frob:doc anchor, tighten ast typing in DOCARCH002 - dacc9d193 chore(tickets): scope T-4693 (frob-ratchet.lock.json) - 8d92bda3f chore(tickets): record evidence for T-4693 (HEAD) ## Pre-READY checks `frob check --only sys --files src/frob/gates/_docarch_structural.py --files tests/gates/test_docarch_structural.py --files frob.toml --files frob-ratchet.lock.json --files docs/modules/gates.md --base dev`: FAIL overall (gate:DRIFT 6 err/5 waived, gate:DSL 1 err, gate:GATERULE 1 err, gate:SELFAUDIT 2 err -- all pre-existing repo-wide baseline findings; NONE named any file this ticket touches; grepped the full output for `_docarch_structural` and found zero hits in the error sections). gate:DOCARCH (DOCARCH001, unrelated to this ticket's own DOCARCH002) 0 errors/552 warnings, pass. gate:PROFILE pass, gate:WAIVE pass. `frob check --only arch --files src/frob/gates/_docarch_structural.py --base dev`: WARN, 0 errors, 21 warnings -- all pre-existing god-module warnings on unrelated files (_coverage.py, _waive.py, gitio.py, etc.); none on _docarch_structural.py. `frob check --only coverage --files src/frob/gates/_docarch_structural.py --base dev` (run twice: first pass found COV007 on `_docs_config`'s frob:doc anchor -- fixed by removing the anchor from that private helper; second pass below is post-fix): First pass: FAIL, 11 errors total repo-wide, exactly 1 attributable to this file (`_docarch_structural.py:80 COV007`) -- fixed in commit 2fb5e18e7. gate:TODO (5 err) and remaining gate:COV/DRIFT errors are pre-existing, unrelated files (_cli_parsers/_check.py, graph/dsl.py, strata/_effects.py, etc.) -- grepped, zero hits for `_docarch_structural` after the fix commit. `ruff check src/frob/gates/_docarch_structural.py tests/gates/test_docarch_structural.py`: All checks passed! `ruff format --check src/frob/gates/_docarch_structural.py tests/gates/test_docarch_structural.py`: 2 files already formatted `ty check src/frob/gates/_docarch_structural.py`: All checks passed! (two type errors found and fixed in commit 2fb5e18e7: `ast.get_docstring`'s narrower parameter type, and `ast.expr` lacking `.value`) pytest: `PYTHONPATH=$(pwd)/src .venv/bin/python -m pytest tests/gates/test_docarch_structural.py -p no:cacheprovider -q` -> 10 passed, 0 failed.
