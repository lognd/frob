bump: minor
T-5477: parse_pytest_log's vanilla-pytest regexes never matched this repo's own CI logs: -n auto --dist=loadgroup makes tests/conftest.py write its own SUITE-RESULT/SUITE-RESULT-FAILED xdist summary instead of trusting the positional dot-stream, and gh api .../logs additionally prefixes every line with an ISO timestamp that defeats the ^-anchored vanilla regexes even where literal pytest text does appear. Net effect measured on real CI run 35951365410: build_job_report returned not_recoverable for every completed job, silently -- the exact false-negative the module exists to prevent; all 19 ubuntu failures were extracted by hand. Fix adds a SUITE-RESULT-aware parse path, tried first and authoritative whenever present (so a nested subprocess's own vanilla pytest output, e.g. test_scaffold_dx's generated-project run, can never be mistaken for the outer result), with the optional gh-logs timestamp prefix. Verified against a trimmed real capture of run 35951365410: recovers all 19 real failing node ids.
