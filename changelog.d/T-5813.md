bump: minor
T-5813: Fixes the land pipeline's pre-land ty check running before the worktree's own stale natives get rebuilt. T-5518 added a stale-natives rebuild call (_rebuild_stale_worktree_natives) but only at its own POST-merge call site (_reverify_evidence_post_merge). The PRE-merge ty check (_assert_touched_files_type_check_pre_land, called from _land_core_prepare right after Tier-A absorption) never got the same treatment, so a ticket whose worktree branch adds/changes a Rust export -- or that lands after such a change is already on the target branch -- gets ty-checked against an extension built before that export existed. Measured twice even with T-5518 already on dev: T-3010's land (/tmp/land-T-5464.log) and T-5366's land (/tmp/land-T-5366.log) both saw "Module strata_core has no member X", cleared by a manual frob natives build with no source change. Fix: _land_core_prepare now calls _rebuild_stale_worktree_natives right before the ty check. Verified with 2 new tests asserting call order via a shared call_order list (the rebuild call always precedes the ty-check call, whether the check refuses or stays quiet) plus the full pre-existing 15-test test_ticket_land_dry_run.py suite (no regression).
