bump: minor
T-5341: Root cause (found via direct graph probe, not guessed): frob.graph.dsl. _reorient_test_edge (landed under T-4710/T-4758) canonicalizes every `frob:tests` TESTS edge to src=implementation-symbol, target=test -- but src/frob/gates/__init__.py's whole COV006 rescue chain (_cov006_edge_violation plus its three T-0528 rescue helpers: _cov006_public_wrapper_reachable, _cov006_implicit_dispatch_reachable, _cov006_third_file_reachable) was never updated and still assumed the OLD orientation (src=test, target=private symbol). Under the new canonical direction, `target_record = snapshot.symbols.get(edge.target)` resolved to the TEST's own symbol record (public by convention), so `target_record.public` short-circuited every single edge to "not a finding" -- COV006 went silently inert across the whole repo, which is exactly what test_cov006_flags_test_with_no_call_graph_reachability (and 3 siblings) caught. Fix: build one swapped `test_edge` at the entry point (_cov006_edge_violation) and thread it through the whole rescue chain unchanged, rather than rewriting all four functions' internals. Fixing the direction bug exposed a second, previously-unreachable latent bug: a whole-FILE `frob:tests` binding (no specific test function named, so the test-side symref has no "::" qualname) crashed two of the rescue helpers with an IndexError on `.split("::", 1)[1]` once COV006 actually started evaluating real edges again. Guarded both sites to treat a missing qualname as "cannot locate the test symbol" (their own docstrings' stated contract), matching the pattern already used elsewhere in this same file (lines 2379/2459/2532). Verified: all 150 tests in tests/gates_suite/test_coverage.py and all 78 in tests/gates_suite/test_sys.py pass (228 total, includes the 6 bound evidence ids plus the rule-fixability/known-rule-id pair T-5350's land already fixed independently). A full unscoped self-scan of _cov006 against this repo's own ~33k symbols now takes noticeably longer than before (it is doing real work across many more edges than the silently-broken version ever did) -- did not complete within a 10-minute ad-hoc probe on this host; the scoped land-time check and the bound unit-test evidence are what this ticket's acceptance rests on, not that ad-hoc full self-scan.
