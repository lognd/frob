bump: minor
T-5350: T-5274's land wired DSTACK001 into gates dispatch and T-4758's directive-ergonomics sweep touched FMT002/DOCARCH002 handlers, but none of those lands updated the checked-in _KNOWN_RULE_FIXABILITY literal in src/frob/gates/__init__.py in lockstep. A fresh generated_fixability() scan reported all three rules as "auto" while the literal had none of them, tripping test_checked_in_literal_matches_a_fresh_scan on frob's own repo. Fix: add the three missing "auto" entries. No behavior change to the gates themselves -- this only keeps the drift-lock literal honest.
