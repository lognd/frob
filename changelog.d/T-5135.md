bump: minor
T-5135: H2: doable-time re-verification now writes a negative cache entry on the UNMEASURABLE branch instead of only on success. Before: every `frob ticket doable` call re-spawned the identical doomed ~20s budgeted re-check (measured 76s end to end); after: a second call within the TTL reuses the cached unmeasurable outcome with 0 spawns. H3: COV002's `_ledger_states_at_base_v2` replaced one `git show <base>:<path>` per ticket file with one `git grep -n '^state:' <base> -- tickets/` spawn. Measured at HEAD: 4646 tickets/T-*/ticket.md blobs, so this cuts up to 4646 spawns per distinct (root, base) pair down to 1. H4/H5: `_sibling_branch_ref` now accepts the already-hoisted `leases` sequence (T-4492) instead of re-scanning `read_all_leases` per candidate; `_sibling_branch_touched_path`'s two-spawn-per-hit-path loop is preceded by one batched `git diff --name-only HEAD..<branch>` spawn, short-circuiting every path the batch already proves differs. M7: `_directive_anchored_ticket_ids` now runs one `git grep -l frob:ticket <branch> -- <paths>` before paying a per-file `git show`, skipping files the grep proves cannot contain a directive at all. Descoped: explore_runner.py's xref/map artifact-cache fix (lease collision with live T-4690) and _close_cmd.py/_lifecycle.py's repeated load_queue calls (M4/M5, stayed out to keep the H2-H5/M7 fix order) -- filed as T-5201 and T-5199.
