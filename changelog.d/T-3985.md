bump: minor
T-3985: Design (acceptance[1]): ENFORCING is defined as effective severity == error after `[gates.severity]` overrides are applied (`frob.gates. _waive._severity_overrides`), evaluated per rule id, never as "every gate with subject_count == 0" -- a legitimate zero-subject case (a rule whose subject universe does not exist in this repo/run, e.g. PROFILE001 in a non-frob checkout) is distinguished by requiring the caller to supply this per-rule/per-repo "enforcing" determination explicitly (`enforcing_zero_subject_diagnostic(tool, subject_count, *, enforcing)` in src/frob/process/parsers/common.py never guesses it). The wired PROFILE001 probe additionally gates `enforcing` on `frob.repo_meta. is_frob_own_repo(root)`, since PROFILE001 is self-referential to frob's own `src/frob/tickets/_profile.py` module and has no legitimate subject universe in any other repo -- without this carve-out every non-frob checkout would trip SUBJECT001 permanently, exactly the "matches the normal case, disables the guard" failure the ticket's own design constraint (and WAIVE004's precedent) warns against. Full design writeup, including the honest limit against T-4056's node-identity bug and the explicit boundary against the (separately motivated) reachability-from-entrypoint shape (T-4025/T-4071/H3-6, raised mid-task by the coordinator), lives in docs/modules/process.md's new "Subject-count primitive" section. Design (acceptance[2]): answered honestly in docs/modules/process.md -- no retroactive subject_count exists for T-3844's 308 promoted-to-error rules (out of this ticket's declared proof-of-concept scope, which wires exactly one rule, PROFILE001). T-3844's own `RuleCensusEntry. corpus_wide`/`_WAIVE004_STRUCTURALLY_UNVERIFIABLE_RULES` census already narrows which zero-finding rules are diff-scoped-and-expected-zero vs. corpus-wide-should-be-clean; the natural candidate set for a future subject-count audit among the 308 is any rule whose subject universe is narrow, platform-conditional, or artifact-dependent -- the same shape class PROFILE001 itself is. A full per-rule retrofit is named as explicit follow-up, not silently skipped. Design (acceptance[3]): `tests/unit/gates/test_profile_boundary_subject_ count.py::TestProfileBoundarySubjectCount.test_zero_reproduces_t3941_ windows_shape` reproduces T-3941's own root cause directly (monkeypatches `_symbol_usages` to return `()`, the exact shape xref's backslash/ forward-slash path mismatch produced on Windows) and shows `profile_ boundary_gate` alone reports zero violations (indistinguishable from clean) while `profile_boundary_subject_count` reports the missing `0` signal. `tests/unit/test_check.py::TestSubjectCountPrimitive. test_frob_own_repo_with_zero_usages_trips_subject001` proves the SAME shape wired all the way through the real `frob check` pipeline (`_run_ gates` -> `gate:PROFILE` `ToolResult`): `subject_count == 0`, `exit_code == 1`, a `SUBJECT001` diagnostic present, and the overall `gate-summary` exit code also fails -- not just the per-family stage. `test_foreign_repo_never_trips_subject001` is the negative control this design constraint requires: the same zero subject_count on a non-frob repo produces NO finding. Changed: - src/frob/process/parsers/common.py::ToolResult.subject_count (new field) - src/frob/process/parsers/common.py::enforcing_zero_subject_diagnostic (new function) - src/frob/gates/_profile_boundary.py::profile_boundary_subject_count (new function, PROFILE001 POC probe) - src/frob/check/_python.py::_subject_count_probes, _family_subject_count, _gates_family_result, _gates_family_results, _gates_success_result (wiring) - src/frob/gates/_waive.py::_KNOWN_GATE_RULES (SUBJECT001 registered, T-1002 zone) - docs/modules/process.md (new "Subject-count primitive" section: design, T-3844 answer, honest limit, explicit reachability boundary) - docs/modules/gates.md (rule-catalog: SUBJECT001 row + enumerates member) - docs/design/registry/check-coverage.yaml (CHK-GATE-SUBJECT001 entry, via `frob registry audit --sync-gate-rules`) - design/frob.strata (testsuite node: new test file's fs.write capability) - docs/design/registry/capability-via-ratchet.lock.json (testsuite::fs.write ceiling 507 -> 508) - tests/unit/test_process.py, tests/unit/test_check.py, tests/unit/gates/test_profile_boundary_subject_count.py (new tests) Filed (out-of-scope discoveries, not fixed silently): - T-4087: WIRE001 follow_up anchor (T-1856 anchor=True, stays open forever by design) -- `profile_boundary_subject_count` is genuinely wired but only via a function-scoped deferred import (`frob.check._python` cannot import `frob.gates._profile_boundary` at module scope without a real `frob.gates`<->`frob.check` circular import), which the best-effort callgraph cannot trace through. - T-4098: SCOPE002 was promoted to `error` by T-3844's blanket ratchet, but its own waive-matching path (`_match_waiver`'s file-scoped branch, `violation.file == "tickets.md"`) requires a `frob:waive` comment physically inside a file literally named `tickets.md` -- a file that no longer exists after the ledger-v2 cutover (T-2356). SCOPE002's own docstring calls it "a nudge, not a hard block" that "must never block a ticket that legitimately intends a narrower slice than its own doc/call graph suggests" -- promoting it to error made it an unwaivable hard block for exactly the disproportionate-closure-breadth case T-3914/T-4019 already disclosed as a known, accepted cost when SCOPE002 was still WARN. This ticket's own scope touches several heavily cross-referenced files (docs/modules/gates.md, design/ frob.strata, src/frob/gates/_waive.py) purely because SUBJECT001 becoming a real registered gate rule id requires it -- `frob ticket scope-ack --reason ...` was tried first and does not suppress SCOPE002 (it appears to gate a different rule, TICK009). `gate:SCOPE` is left with SCOPE002 findings genuinely NOT waivable through any existing mechanism; T-4098 tracks the fix (either restore a waivable-target file, or add a `scope_breadth_ack`-honoring exemption to `_scope002_violations` the way TICK009 has one). Gates: `frob check --ticket T-3985` -- every family this ticket's own diff plausibly touches is clean (gate:PROFILE 0/0/0/0, gate:FMT clean for touched lines, gate:GATERULE clean, gate:DOCENUM clean, gate:REG clean, gate:WIRE clean via the T-4087 anchor waiver, gate:SELFAUDIT clean via the capability-via-ratchet bump) EXCEPT gate:SCOPE, which fails on SCOPE002 findings that are structurally unwaivable per T-4098 above -- a genuine pre-existing gate defect this ticket's own (required) scope breadth exposed, not a defect in this ticket's own change. Every OTHER family shown as FAIL in a full run (COV/PERF/PRE/TODO) is pre-existing repo-wide baseline noise unrelated to this diff (verified via `git blame` on each flagged line before/after this diff; PRE001 is this ticket's own stale-sweep nag, cleared by re-running `frob ticket sweep T-3985` each time scope changed).
