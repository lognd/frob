bump: minor
T-5353: WEBSEC218-224 close the ticket's password-policy/storage gap: no lint covered missing email-verification/breach-password checks, weak MD5/SHA1 password hashing, AES-ECB, hardcoded IVs, default seed-data accounts, or case-folded password comparisons. Reusing T-5308's pkgutil discovery hook (websec_findings(root, frameworks)) keeps taint_gate's own wiring untouched, matching every sibling WEBSEC leaf. T-5352 was still queued, not landed, so its token helpers were not imported from; no helper was needed here regardless (file-scope regex checks only). Fixtures live under their own websec2xx/password/ subdir, separate from sibling T-5351's csrf_session/ and T-5352's jwt_oauth/ subdirs under the same parent. WEBSEC225 is left unimplemented (the ticket body's seven-item corpus fits seven of the eight reserved ids) and filed as T-5500 rather than expanding this ticket's scope.
