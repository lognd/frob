bump: minor
T-5474: CI run 35951365410 (dev) failed test_every_ops_leaf_matches_its_flat_twin [natives]: the flat `frob natives` group parser has a `--path` option (mirrored there by T-4522) that its `frob ops natives` twin lacked. Added the same set_defaults(natives_command="build") + --path mirror to the ops group parser so the two option-string sets match again. Verified against the full parametrized parity test (10/10 pass) and the whole cli_group_ parity file (29/29 pass).
