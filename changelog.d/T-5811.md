bump: minor
T-5811: CI run 36086669322 failed "Typecheck" on all three platforms (ubuntu/ macos/windows) with identical unresolved-import errors for sqlfluff. Root-caused from the raw job logs: CI's initial "Sync deps" step runs uv sync --all-extras --all-groups (installs sqlfluff, confirmed by the log's own + sqlfluff line), but a later "make core-wheels" step reaches $(STAMP): pyproject.toml's own uv sync --extra serve recipe via the core-wheels -> core -> $(STAMP) dependency chain -- this re-narrows the sync, uninstalling sqlfluff (confirmed by the log's own - sqlfluff line immediately after "uv sync --extra serve") before the Typecheck step ever runs. Because Typecheck fails, every downstream Test step is skipped on all three platforms. Added --extra sql to the stamp's recipe (excluding --extra smt deliberately, per the comment above the rule: z3-solver can fail to build on some hosts, sqlfluff carries no such risk). Verified directly: re-ran the exact CI sequence (uv sync --all-extras --all-groups, then make core) locally -- sqlfluff stays installed, ty check on src/frob/sql/_sqlfluff_plugin.py passes. Added a regression test (matching T-4479's own precedent shape: parse the Makefile recipe text and assert its extras) that genuinely fails at dev and passes at the fix.
