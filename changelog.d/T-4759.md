bump: minor
T-4759: T-4759 -- frob.toml [commands] table plus a run verb WHAT changed, per file: - src/frob/policy/_models.py Added CommandStep (type alias: str | tuple[str, ...]), CommandEntry, CommandsConfig, CommandsError -- the pydantic shapes for a parsed [commands] table, model_config = {} per instruction. CommandsError (typani ErrorSet) covers MalformedEntry/CycleDetected. - src/frob/app/run_runner.py (new) load_commands(root): parses frob.toml's [commands] table with tomllib, normalizes each raw TOML value into a CommandEntry (a flat string array that matches only declared entry names is a composed reference sequence; any other array is one literal shell-free argv; a nested array is always unambiguous), then DFS-detects reference cycles (self and transitive) and refuses at load time, naming the full reference path in the log. _resolve_sequence(): flattens a name into a concrete ordered tuple of argv tuples, expanding references (cycles already refused at load) and falling back to _NATIVE_DEFAULTS for test/lint/format/check when undeclared (build has no native default). _execute_sequence(): runs steps in order, shell-free (subprocess.run, no shell), logs each step's index/command/duration, stops at the first non-zero exit naming that step's index and command. run(argv)/run_build(argv): the frob run <name> [--dry-run] and frob build [--dry-run] entry points, dispatched directly (bind/ agent/worktree's own pattern) rather than through AppConfig/ Subcommand/app.py -- see WHY below. - src/frob/_cli_parsers/_run.py (new) _add_run_parser/_add_build_parser: --help-only parser builders, mirroring bind/agent/worktree's precedent. Carry frob:debt DEAD001 (ticket=T-4811) because they are not yet reachable from _root.py's registration tree -- see WHY. - src/frob/_cli_parsers/__init__.py Re-exports _add_run_parser/_add_build_parser (added import + __all__ entries), same convention every other parser-builder module uses. - src/frob/__main__.py Re-imports _add_build_parser/_add_run_parser (noqa: F401, matching every other re-exported builder here). Added _dispatch_run/ _dispatch_build (mirroring _dispatch_bind/_dispatch_agent/ _dispatch_worktree) and wired them into _dispatch()'s routing table ahead of "bind". Added "# frob:ticket T-4759" to _dispatch's existing ticket-list comment block (COV002). - docs/commands/run.md (new) Usage, the [commands] table's semantics (single command vs. composed sequence, the name-matching disambiguation rule, nested-array steps), native defaults, execution semantics, cycle refusal, error types, and the derived-wrappers relationship (frob run <name> is the one thing a generated Makefile/make.bat, T-4760, ever calls). Headings match the frob:doc anchors used in run_runner.py/policy/_models.py exactly (#public-api, #commands-table, #native-defaults, #error-types, etc). - tests/unit/test_run_commands.py (new) TestLoadCommands (no-file empty-Ok, three-step composition, single- command-is-literal-argv, self-reference cycle, transitive A->B->A cycle), TestRun (middle-step failure names its index via a subprocess.run monkeypatch, dry-run never calls subprocess.run, unknown command is Err, native default used when undeclared), TestBuild (delegates to the build entry, no native fallback -> Err). 11 tests, all green. WHY (design decisions): - frob run/frob build are dispatched directly from frob.__main__._dispatch (own argparse, bypassing AppConfig/Subcommand), the same shape bind/ agent/worktree already use -- because wiring them through frob.app.app's _SUBCOMMAND_RUNNER_NAMES dict would require editing src/frob/app/app.py, which is leased by T-4689 (in progress at land time). This keeps frob run/frob build fully functional without touching that file. - The coordinator's scope correction removed src/frob/_cli_parsers/ _core.py from this ticket's scope (T-4546/T-4690 lease) and added _run.py instead. The actual --help registration hook for every _add_*_parser builder is src/frob/_cli_parsers/_root.py (_add_analysis_subparsers), which turned out to ALSO be leased by T-4546 at land time -- so _add_run_parser/_add_build_parser exist and are re-exported (same convention as every other builder) but are not yet called from that tree. Filed T-4811 (queued, blocked_by T-4546, scope src/frob/_cli_parsers/_root.py) to wire them in once that lease clears, and marked the gap with frob:debt DEAD001 (ticket=T-4811) on both builders rather than a permanent frob:waive, since it genuinely resolves when T-4811 lands. frob run/frob build work correctly today; only their appearance in top-level `frob --help` is deferred. - The [commands] disambiguation rule (a flat string array is a composed reference sequence only when every element matches a declared entry name, otherwise one literal argv) is what makes the ticket's own worked example (`check = ["fmt", "lint", "test"]`) unambiguous with a literal command like `fmt = ["ruff", "format", "."]` without a second TOML shape to teach -- documented explicitly in run.md and in _normalize_raw_entry's own docstring. - docs/guides/frob-toml.md (mentioned in the coordinator's brief) is NOT touched: T-4761 (SCAFFOLD-TREE.md's dispatch table) already owns that file for the scaffold's own frob.toml templates; adding a section there now would collide with that ticket's land. run.md's own content is self-sufficient in the meantime. ACCEPTANCE CRITERIA, how each is proven: [1] three-step entry, middle step fails, error names the index: tests/unit/test_run_commands.py::TestRun::test_middle_step_failure_names_index [2] self-reference (and transitive A->B->A) refused at load, path named: tests/unit/test_run_commands.py::TestLoadCommands::test_self_reference_refused_with_path (transitive case also covered by test_transitive_reference_cycle_refused, not separately bound as evidence since criterion 2 needs one node) [3] no [commands] table -> test/lint/format/check resolve to native verbs: tests/unit/test_run_commands.py::TestRun::test_native_default_used_when_undeclared [4] --dry-run prints the resolved sequence, spawns nothing (subprocess spy): tests/unit/test_run_commands.py::TestRun::test_dry_run_prints_without_spawning Evidence bound (after the last commit, per the close-dance ordering): frob ticket evidence T-4759 <node> --accepts N --base-ref dev for all four node ids above. Commit: cebb83f5d "feat(run): add frob.toml [commands] table plus frob run/build (T-4759)" Filed: T-4811 (queued, blocked_by T-4546) -- wire frob run/build --help into _build_parser's subcommand tree once T-4546's lease on src/frob/_cli_parsers/_root.py clears. Cross-ticket lease note: src/frob/__main__.py is also leased by T-4546 (in progress) -- it is part of this ticket's implicit_scope grant (FEATURE-kind CLI-wiring, T-0446/T-1848), touched only to add the two _dispatch_run/_dispatch_build helpers and their routing-table entries plus one frob:ticket line, no other edits nearby. Coordinator may need --allow-cross-ticket or to sequence this land against T-4546's. ## Pre-READY checks frob check --only sys --files src/frob/policy/_models.py --files src/frob/app/run_runner.py --files src/frob/_cli_parsers/_run.py --files src/frob/_cli_parsers/__init__.py --files src/frob/__main__.py --files docs/commands/run.md --files tests/unit/test_run_commands.py --base dev -> gate:WAIVE pass (0 errors, 8 warnings, 0 unresolved, 0 waived -- all pre-existing/unrelated); no SELFAUDIT/DSL findings attributable to touched files (re-verified narrowly on src/frob/_cli_parsers/_run.py alone after the WAIVE010 fix: gate:WAIVE pass, 0 errors, 8 warnings) frob check --only arch --files src/frob/policy/_models.py --files src/frob/app/run_runner.py --files src/frob/_cli_parsers/_run.py --files src/frob/_cli_parsers/__init__.py --files src/frob/__main__.py --base dev -> pass frob-arch 21 warnings (36 waived), 546 suggestions -- no ARCH001/LARGE001, only whole-repo pattern-recommendation noise (Ok(...)/Err(...) constructed directly in 180-190 files etc, not attributable to these files) frob check --only coverage --files src/frob/policy/_models.py --files src/frob/app/run_runner.py --files src/frob/_cli_parsers/_run.py --files src/frob/_cli_parsers/__init__.py --files src/frob/__main__.py --base dev -> after fixing COV001 (added frob:doc anchors matching real run.md headings on every public symbol), COV007 (removed frob:doc from the private _NATIVE_DEFAULTS), COV002 (added "# frob:ticket T-4759" to _dispatch's ticket-list), and the frob:debt malformed-directive/WAIVE010 syntax fix: gate:COV 10 errors / gate:TODO 5 errors remain, ALL pre-existing and attributable to other files (src/frob/_cli_parsers/_check.py, src/frob/graph/dsl.py, src/frob/strata/_effects.py, src/frob/strata/_unity_asmdef.py, src/frob/tickets/_land_compose.py, src/frob/gates/_coverage.py, src/frob/perf/_dup_spawn.py, src/frob/perf/_loop_effects.py, src/frob/strata/_secrets.py) -- verified by grep, none reference run_runner.py/_run.py/_models.py/__main__.py. gate:DRIFT (6 errors, pre-existing waived elsewhere) and gate:DSL (1 error) likewise not attributable to touched files. ruff check src/frob/policy/_models.py src/frob/app/run_runner.py src/frob/_cli_parsers/_run.py src/frob/_cli_parsers/__init__.py src/frob/__main__.py tests/unit/test_run_commands.py -> All checks passed! ty check src/frob/policy/_models.py src/frob/app/run_runner.py src/frob/_cli_parsers/_run.py src/frob/_cli_parsers/__init__.py src/frob/__main__.py -> All checks passed! (fixed two invalid-argument-type diagnostics: explicit isinstance(element, list) branch in _normalize_raw_entry, and str(k) coercion building known_names) pytest tests/unit/test_run_commands.py (PYTHONPATH=worktree/src, -p no:cacheprovider -q) -> SUITE-RESULT: exitstatus=0 collected=11 failed=0
