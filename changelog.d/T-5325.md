bump: minor
T-5325: WEBSEC needs a response-header lint engine before any header-family gate rule can fire: it turns Django SECURE_* settings, Express helmet(), and nginx/Caddy config directives into per-header PRESENT/MISSING findings. A repo with no in-repo security surface at all (headers set purely at a CDN/edge dashboard) reports ADVISORY rather than a false ERROR -- the documented CDN-layer gap the owner directive called out. Built strictly on T-5302's detect_frameworks (never re-detects) and gated the app-code Django scan on it; helmet detection uses a text regex rather than frob.lang.iter_identifiers because that walker has no JS/TS identifier types yet (T-3232 follow-up), documented inline and in the module doc. Positive and negative fixtures cover all four evidence sources plus the no-evidence advisory case.
