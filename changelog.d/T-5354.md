bump: minor
T-5354: Randomness and TLS verification had no static coverage in the WEBSEC family: session tokens could be seeded from a predictable PRNG, outbound API calls could skip certificate verification or run over plaintext http://, TLS contexts could be created with no minimum protocol version, mobile clients could ship with no certificate pinning, and login handlers could have no rate limiting against credential stuffing. This closes the gap with five WARN-tier rules (WEBSEC226-230), auto-discovered by taint_gate's existing pkgutil hook so no gate wiring was needed. HSTS stays T-5143-2's header-lint scope, cross-referenced not duplicated.
