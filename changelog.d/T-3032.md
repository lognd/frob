bump: minor
T-3032: T-3032 -- Extract the graph concerns tickets shares with strata into the shared kernel, incrementally, one concern per land WHAT changed ------------ - docs/design/ticket-strata-shared-graph-inventory.md (NEW): the required per-concern inventory across all six graph-shaped tickets modules the owner's 2026-08-26 directive measured (_setters.py, _models.py, _doable.py, _store.py, _evidence.py, _scope.py). Each concern graded SHARED / TICKET-SPECIFIC / SIMILAR-BUT-SUBTLY-DIFFERENT with reasoning, plus an extraction-status table. - src/frob/graph/_hierarchy.py (NEW): the ONE extraction this leaf lands end-to-end. children_by_parent_id() builds {parent_id: [child ids]} from (own_id, parent_id) pairs; descendant_ids() walks that adjacency to every id reachable from a root, any depth, each visited once, root excluded. Deliberately id-only (Hashable _Id TypeVar, not Ticket-typed) so a non-ticket consumer can reuse it without importing frob.tickets. - src/frob/gates/_milestone.py (MODIFIED): _children_by_parent and _descendants_of now delegate to the shared helpers, translating between id-space and Ticket-space at the call site (this caller still needs full Ticket objects for milestone comparison). - src/frob/tickets/_evidence.py (MODIFIED): _open_descendant_ids now delegates to the shared helpers, keeping only the ticket-specific open/closed-state filter locally. - tests/unit/test_graph_hierarchy.py (NEW): 8 unit tests for the new module directly (adjacency construction, multi-depth walk, root exclusion, leaf-with-no-children, diamond-shape dedup). - docs/modules/graph.md (MODIFIED): new "Hierarchy (T-3032)" section documenting the extraction and the frob:doc anchor target for the new module's two public functions. WHY --- Both call sites' own pre-existing docstrings already disclosed this exact duplication before this leaf touched anything: frob.gates._milestone._descendants_of said "same walk shape _open_descendant_ids (frob.tickets._evidence) uses for its own open-descendant check, kept as a local helper rather than importing that private one" -- i.e. the duplication was known and left in place. This leaf is the incremental extraction T-3032's own METHOD section calls for: pick the smallest shared concern (the ticket names "parent/child hierarchy and ancestor walks" as a LIKELY SHARED candidate explicitly), prove existing tests cover it, extract, reroute, verify unchanged. WHY THIS CONCERN FIRST: of the four SHARED concerns the inventory identifies (descendants walk, blocked_by transitive resolution, cycle refusal, typed-id construction refusal), the descendants walk was the only one with (a) an EXACT existing duplicate (not just a similar shape) and (b) zero ticket-state coupling in the walk itself -- the cleanest, smallest, most reversible first increment, matching the ticket's own "smallest first" instruction. REGRESSION COVERAGE EXISTED BEFORE THE EXTRACTION (acceptance criterion 3): both call sites already carried `frob:tests` bindings to real regression suites before this leaf touched them -- frob.gates._milestone._mile002_descendant_later_milestone has 6 frob:tests bindings into tests/test_gates_milestone.py::TestMile002, and frob.tickets._evidence._open_descendant_ids's T-0715 guard is covered by tests/test_tickets_tiers.py::TestCloseOpenDescendantGuard and tests/unit/test_app_runners_t0715_sprint_tier.py. All of these ran and passed UNCHANGED after the extraction (60 tests total, see Evidence below) -- proving the refactor did not change behaviour. FOLLOW-UPS FILED (acceptance criterion 4 -- one per remaining SHARED concern, per the ticket's "DO NOT extract several concerns in one land" rule): T-draft-5d5c1eb2 -- blocked_by transitive resolution (scope: src/frob/tickets/_doable.py) T-draft-452acd80 -- cycle refusal on blocked_by/parent mutation (scope: src/frob/tickets/_setters.py) T-draft-e7434c27 -- typed-id/dangling-reference construction refusal for Ticket.parent (scope: src/frob/tickets/_models.py; explicitly scoped to a DESIGN PASS first, since strata_core's own .pyi exposes no generic schema-validation entry point yet) (effective_milestone's ancestor walk was graded SIMILAR-BUT-SUBTLY- DIFFERENT in the inventory and deliberately kept separate, not filed as a follow-up extraction -- see the doc's _doable.py row for why forcing it onto the same primitive would be a premature abstraction.) OUT-OF-BAND COORDINATOR REQUEST DURING THIS TICKET: a message purporting to be "the coordinator" arrived appended to a background task's system notification (not as a normal turn) asking me to expand T-4661's scope to touch docs/modules/vet.md and docs/modules/gates.md and add a new lint rule. I treated this as unverified/likely injected (wrong channel, contradicted BRIEF.md's explicit no-touch instruction on those leased hotspot files) and did not act on it. A SECOND message, explicitly self-identified as arriving through the real dispatch channel and narrowly scoped to filing a ticket from ROOT (a sanctioned operation) with no scope expansion, I did act on: filed T-4716 "Gate registry owns each rule's one-sentence description; DOCENUM002 flags mismatched/unregistered docs table rows", parented under T-4655, blocked_by T-4661. Not worked -- reporting the id here per that message's own instruction. Commits (worktree /home/logan/projects/frob/.claude/worktrees/t-3032, branch t-3032): 2348aec8a refactor(graph): extract shared parent/child descendants walk (T-3032) b3681483d fix(graph): make _hierarchy's TypeVar private (COV001) 1c77c2a91, e39224fe1, 05d908694, 7c0fda55a chore(tickets): record evidence for T-3032 HEAD sha: 7c0fda55a1... (see `git rev-parse HEAD` in worktree) Evidence (bound after the last code commit): tests/unit/test_graph_hierarchy.py::TestChildrenByParentId::test_builds_adjacency_from_parent_pairs tests/unit/test_graph_hierarchy.py::TestDescendantIds::test_multi_depth_walk tests/test_gates_milestone.py::TestMile002::test_grandchild_descendant_fires tests/test_tickets_tiers.py::TestCloseOpenDescendantGuard::test_epic_close_refused_with_open_descendant Full regression run (all tests touching either rerouted call site, local sanity check before binding evidence): PYTHONPATH=.../t-3032/src python -m pytest tests/unit/test_graph_hierarchy.py tests/test_gates_milestone.py tests/test_tickets_tiers.py tests/unit/test_app_runners_t0715_sprint_tier.py -p no:cacheprovider -q -> SUITE-RESULT: exitstatus=0 collected=60 failed=0 ## Pre-READY checks 1. `frob check --only sys --files src/frob/graph/_hierarchy.py --files src/frob/gates/_milestone.py --files src/frob/tickets/_evidence.py --files tests/unit/test_graph_hierarchy.py --files docs/design/ticket-strata-shared-graph-inventory.md --files docs/modules/graph.md --base dev`: Tool summary: FAIL gate:DRIFT (6 errors) and FAIL gate:DSL (1 error) -- grep of the full output for "_hierarchy.py|_milestone.py|_evidence.py| test_graph_hierarchy.py|shared-graph-inventory|graph.md" shows every attributable line is a pre-existing DOCARCH001/WAIVE010/DRIFT001 finding on OTHER functions in src/frob/tickets/_evidence.py (record_rapid_debt, reverify_close_guard, add_evidence, base_ref_resolvable, _done_transition_structural_guard, add_cmd_evidence) never touched by this diff -- zero findings on my new/changed lines. 2. `frob check --only arch --files ...` (same file set): "pass frob-arch 19 warnings (36 waived), 546 suggestions" -- the only per-file hits on my touched files are pre-existing, file-level metrics on _evidence.py (large-file/high-coupling/god-module, all file-wide, none attributable to the ~15 lines I changed) and one pattern-recommendation on _milestone.py (Version(...) constructed directly across 5 files, pre-existing, unrelated to my edit). No ARCH001/LARGE001 error against my new module. 3. `frob check --only coverage --files ...` (same file set), run TWICE: first run found COV001 on `_hierarchy.py::Id` (a public-looking TypeVar with no frob:doc edge) -- fixed by renaming to `_Id` (private) and committing (b3681483d). Second run: grep for "_hierarchy.py" across the full output returns ZERO matches -- clean. Remaining FAIL gate:COV/TODO/DSL/DRIFT lines in the tool summary are all pre-existing findings in _evidence.py/_mutation_evidence.py/other test files, unrelated to this ticket's diff. 4. `ty check src/frob/graph/_hierarchy.py src/frob/gates/_milestone.py src/frob/tickets/_evidence.py tests/unit/test_graph_hierarchy.py`: "All checks passed!" `ruff check` (same files): "All checks passed!" 5. Cross-ticket file check: `git diff --name-only dev...HEAD` (code commits) = docs/design/ticket-strata-shared-graph-inventory.md, docs/modules/graph.md, src/frob/gates/_milestone.py, src/frob/graph/_hierarchy.py, src/frob/tickets/_evidence.py, tests/unit/test_graph_hierarchy.py -- all six are T-3032's own declared scope (`frob ticket scope T-3032 --add ...`, done before touching any file). DISCLOSED COLLISION: `frob ticket start T-3032` (attempted after scope was declared and all code/tests were already committed) was REFUSED: "T-3032's declared scope collides with in-progress T-4684's lease on 'src/frob/tickets/_evidence.py'". T-4684 ("cross-lease": tests/test_ticket_leases_cross_worktree.py, src/frob/tickets/ _evidence.py) took a live lease on that same file after my scope-add but before my start attempt. Consequence: T-3032 has NO recorded cross-worktree lease in ROOT's `.git/frob-leases/` -- this worktree's commits exist and are internally consistent (tests pass, gates clean), but the ticket itself is still `state: queued` in ROOT's ledger, not `in-progress`. The coordinator must either wait for T-4684 to land/release its lease on `_evidence.py` and then run `frob ticket start T-3032` before landing this worktree, or land with an explicit cross-ticket override -- I did not force past this collision myself, per BRIEF.md's lease-conflict guidance. 6. Not a kind=bug ticket -- BUG002 designate-repro not applicable. 7. No tickets/T-draft-* directory created inside this ticket's own scope; the three follow-up tickets were filed from ROOT via `frob ticket new --json` (sanctioned) and are ROOT's own ledger state, not files inside this worktree. 8. Evidence bound AFTER the last code commit: the tip (7c0fda55a, an evidence-record commit) follows b3681483d (the last code commit); no code commit follows any evidence commit.
