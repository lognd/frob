bump: minor
T-4669: T-4669 -- SF-04+SF-20: digest-keyed cache for capability_via_site_counts + load_design_ids Worktree: /home/logan/projects/frob/.claude/worktrees/t-4669 (branch t-4669, base dev) HEAD sha: eaaa30a71 (code commits: 947bade55, 1e8a83f68; evidence commits: 8ce68b232, 4dbcfa476, f1c531425, eaaa30a71) ## WHAT changed src/frob/strata/_effects.py - New per-process cache `_CAPABILITY_SITE_COUNT_CACHE: dict[(str(root), digest), dict[str,int]]` for `capability_via_site_counts`. `digest` (`_capability_scan_digest`) is a SHA-256 over `model`'s own grant signature (every node.id/atom/via triple -- needed because two calls sharing one root but a different via-list SHAPE on a non-glob grant produce different `counts` from an identical candidate file set; a real regression test caught this, see below) plus `(rel, content-hash)` for every file `_capability_scan_candidates` says the scan will actually read (the `testsuite`-bare-glob candidate set `_glob_via_observed_site_count` visits). - `_capability_scan_candidates(model, binding)`: the exact file set the scan reads, factored out so the digest and the real scan can never drift onto two different predicates. - `_cached_file_sha256(path)` + `_FILE_CONTENT_HASH_CACHE`: a per-file content-hash cache gated on `(size, mtime_ns)` -- a stat match reuses the cached hash without re-reading the file; a stat MISMATCH always re-reads and re-hashes (mtime is a fast-path invalidation hint here, never a source of truth, so it cannot produce a false content match). Needed because re-reading hundreds of candidate files' bytes on every call (even a cache HIT on the outer dict) cost ~1.3s by itself. - `_bind_code_or_none` / `_capability_site_count_cache_lookup`: `capability_via_ site_counts`'s cache-lookup logic factored into two helpers so the function stays under the arch gate's long-function threshold (60 lines) now that it also does cache bookkeeping. - `capability_via_site_counts` itself: computes `binding` once (unchanged), then checks the cache before running the O(files) scan; a HIT returns a fresh `dict` copy of the cached result, a MISS rescans and stores the result before returning. `root is None` (the pre-T-4495 call shape) and a `bind_code` failure both skip caching entirely, matching prior behavior exactly (that path was already cheap). src/frob/strata/_design_load.py - New per-process cache `_DESIGN_IDS_CACHE: dict[(str(root), design_dir, digest), DesignIds]`. `digest` (`_design_load_digest`) is a SHA-256 over `(rel, content)` for every `.strata` file `_strata_files` finds under `root/design_dir`. - `load_design_ids`: computes `paths`+`digest` first, returns the cached `DesignIds` instance unchanged on a HIT, otherwise parses+elaborates as before and stores the result before returning. tests/unit/strata/test_strata_scan_cache.py (new) - `TestCapabilityViaSiteCountsCache`: `test_second_call_in_process_is_a_cache_hit_ under_one_second` -- the ticket's own positive control. Builds a `testsuite` bare-glob-via model, writes one real tracked file, spies on `_glob_via_observed_site_count` (monkeypatch), asserts the first call scans once, the second call in the SAME process scans ZERO more times and returns in under 1.0s. `test_changed_tracked_file_invalidates_the_cache` -- edits the SAME tracked file's content between calls and asserts the spy fires again (a rescan, not a stale hit) and the new count is correct. - `TestLoadDesignIdsCache`: same shape for `load_design_ids`, spying on `_load_all_design_files`; the cache-hit test additionally asserts the second call returns the IDENTICAL `DesignIds` object (`is`), not just an equal one. ## WHY SF-04 measured `capability_via_site_counts` at 23.03s cold / 17.83s again on a second call in the SAME process at HEAD c8f56ef10 -- no memoization existed at all, and this quantity is recomputed by SYS111, the land pre-commit check, the composed-tree check and the detached post-land sweep, TWICE per call in `_fix_engine_sync.py:1313` (current_counts + before_counts). SF-20: `load_design_ids` had no lru_cache/mtime/ digest check, called 6+ times per run at separate call sites, and is why SF-11's `require_analyzable` WARNING fires ~12.7 times per land instead of once. Cache key is a CONTENT digest, not mtime, because a worktree checkout / rebase rewrites mtimes with no content change -- caching on mtime alone would defeat the cache exactly when it matters most (right after a land). The per-file content-hash cache (`_FILE_CONTENT_HASH_CACHE`) uses `(size, mtime_ns)` only as a fast-path invalidation HINT to skip re-reading a file whose stat is unchanged since it was last hashed in this process -- any mismatch always falls back to a real read+hash, so it can never produce a stale/false content match; it only ever affects how much work an UNCHANGED file costs to re-verify. ## Real-repo timing (measured in-process against this repo's own tree, root = worktree root, under fleet CPU load -- see note below) load_design_ids(root, "design") cold: 0.027s load_design_ids(root, "design") warm (2 calls): 0.005s, 0.006s capability_via_site_counts(model, root) cold: 26.5s (71 keys, 1635 sites) capability_via_site_counts(model, root) warm (2): 1.19s, 1.21s The remaining ~1.2s on a warm real-repo call is NOT this ticket's cache: it is `bind_code`'s own file-tree walk (`_code_binding.py::_sorted_py_files`, measured at ~1.0-1.3s standalone, unaffected by this change either way) which `capability_via_ site_counts` still runs unconditionally before checking the cache, since re-walking the tree is the only way to detect a file's ADDITION/REMOVAL (a pure content-hash digest of a fixed candidate list cannot see a file that doesn't exist yet). Caching `bind_code`'s own walk is `_code_binding.py`'s surface, outside this ticket's declared scope (`_effects.py`, `_design_load.py`); it is a natural T-4669 follow-up. This measurement was taken while the shared box is under the fleet's normal load (per BRIEF's CPU-BUDGET note), so the walk's absolute cost may vary run to run; the elimination of the 17.8s `_line_effects` re-scan (the actual measured bottleneck named in the ticket) is the part this cache change is responsible for and is reproduced below with a zero-re-scan positive control that does not depend on wall-clock timing at all. ## Acceptance criteria [1] (ticket's [0]) "a test calling it twice in one process asserts the second call completes in under 1 second": `TestCapabilityViaSiteCountsCache:: test_second_call_in_process_is_a_cache_hit_under_one_second` -- passes; asserts BOTH `elapsed < 1.0` AND zero additional `_glob_via_observed_site_count` calls (stronger than a wall-clock budget alone, per memory/positive-control-or-it- proves-nothing.md). [2] (ticket's [1]) "a tracked source file changes -> next lookup MISSES and rescans, logs digest and hit/miss at every call": `test_changed_tracked_file_invalidates_ the_cache` -- passes; asserts the spy fires again after editing the SAME file's content. `capability_via_site_counts`/`load_design_ids` both log at DEBUG on every lookup (`... cache HIT (root=... digest=...)` / `... cache MISS (root=... digest= ...) -- rescanning`), matching the acceptance text's logging requirement. ## Test node ids bound as evidence (frob ticket evidence T-4669 ..., --base-ref dev) --accepts 1: tests/unit/strata/test_strata_scan_cache.py::TestCapabilityViaSiteCountsCache::test_second_call_in_process_is_a_cache_hit_under_one_second --accepts 2: tests/unit/strata/test_strata_scan_cache.py::TestCapabilityViaSiteCountsCache::test_changed_tracked_file_invalidates_the_cache (additional, same shape for SF-20's load_design_ids, not separately numbered acceptance clauses but the same positive-control discipline) --accepts 1: tests/unit/strata/test_strata_scan_cache.py::TestLoadDesignIdsCache::test_second_call_in_process_is_a_cache_hit --accepts 2: tests/unit/strata/test_strata_scan_cache.py::TestLoadDesignIdsCache::test_changed_design_file_invalidates_the_cache Full local run (all pre-existing + new strata-cache tests in scope): PYTHONPATH=$(pwd)/src <venv python> -m pytest tests/unit/strata/test_strata_scan_cache.py \ tests/unit/strata/test_effects.py tests/unit/strata/test_design_load.py -q -> SUITE-RESULT: exitstatus=0 collected=82 failed=0 Also re-ran the full strata self-conformance suite (real design/frob.strata against this repo, the slow real-tree path) to confirm no regression: ... tests/unit/strata/test_selfconform.py added -> SUITE-RESULT: exitstatus=0 collected=163 failed=0 ## Scope note Two commits: 947bade55 (the cache itself) and 1e8a83f68 (a follow-up fixing COV002 -- every new private symbol needed a `frob:ticket T-4669` edge, a prose T-4669 mention in a docstring does not count -- and extracting `capability_via_site_counts`'s cache lookup into two helpers to clear the ARCH long-function threshold, both caught by running the pre-READY checklist below, not by land refusal). Did not touch design/frob.strata or docs/design/registry/capability-via-ratchet.lock.json per the ticket's explicit instruction, and did not touch src/frob/strata/_code_binding.py (bind_code's own walk cost, noted above, is a natural follow-up, out of this ticket's scope). ## Known, expected, non-blocking finding at land time `frob check --only sys` on the touched files reports one SELFAUDIT001: the new test file's own `.write_text(...)` call sites match `testsuite`'s bare-glob `via "tests/**"` grant, growing the REAL observed `testsuite::fs.write` count. The finding's own text says this is a pending AUTO-ACCEPT ("only a land's own composed-tree check run may write ... a bare check outside a land never writes it", T-4563/T-4495) -- this is the documented, working ratchet-growth path for `testsuite`-node bare-glob vias, not a gate failure requiring a design/frob.strata edit (which the ticket explicitly forbids anyway). No action taken; flagging for the coordinator per the checklist item 5/6 spirit even though it is neither a cross-ticket lease conflict nor a kind=bug repro. ## Pre-READY checks `frob check --only sys --files src/frob/strata/_effects.py --files src/frob/strata/_design_load.py --files tests/unit/strata/test_strata_scan_cache.py --base dev` -> FAIL gate:DRIFT (6, all pre-existing/unrelated files), FAIL gate:DSL (1, tests/test_app.py:387, unrelated file), FAIL gate:SELFAUDIT (1, testsuite fs.write auto-accept, see above -- expected/non-blocking); zero findings attributable to _effects.py/_design_load.py content beyond the expected SELFAUDIT001. `frob check --only arch --files src/frob/strata/_effects.py --files src/frob/strata/_design_load.py --files tests/unit/strata/test_strata_scan_cache.py --base dev` -> pass frob-arch (19 warnings, 36 waived, 546 suggestions) -- only pre-existing large-file/high-coupling warnings on _effects.py (2019 lines, 9 local imports, both pre-existing before this ticket); zero ARCH001 long-function findings after the helper-extraction follow-up commit. `frob check --only coverage --files src/frob/strata/_effects.py --files src/frob/strata/_design_load.py --files tests/unit/strata/test_strata_scan_cache.py --base dev` -> FAIL gate:COV (9 errors total) -- both remaining hits on _effects.py are pre-existing on dev (COV001 on FROB_LAND_LOCK_ROOT_ENV, COV007-waived on _declared_kinds_for_file); zero COV002 findings on this ticket's new symbols after the frob:ticket-directive follow-up commit. FAIL gate:DRIFT/TODO also present but 100% pre-existing/unrelated files (verified by path). `ruff check src/frob/strata/_effects.py src/frob/strata/_design_load.py tests/unit/strata/test_strata_scan_cache.py` -> All checks passed! `ty check src/frob/strata/_effects.py src/frob/strata/_design_load.py tests/unit/strata/test_strata_scan_cache.py` -> All checks passed!
