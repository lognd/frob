bump: minor
T-5286: My own T-5034 fix (already landed) inlined a call to frob.graph.build_graph directly in _required_release_bump instead of the mockable _ticket_runner._graph_snapshot(root) helper, to fix a genuine stale- cache bug. This broke tests/unit/test_ticket_runner_land_release.py:: TestApplyReleaseBumpForLand::test_unreadable_graph_fails, which monkeypatches _graph_snapshot to simulate a graph-build failure -- the monkeypatch had no effect once the call was inlined. Fix: extracted the fresh-build logic into a new, separately-testable seam, src/frob/app/ticket_runner/__init__.py::_fresh_graph_snapshot (always incrementally rebuilds, never trusts a load_graph cache hit -- the same T-5034 correctness fix, now in one reusable place). _land_cmd.py calls through this seam instead of inlining build_graph. Updated the test to monkeypatch _fresh_graph_snapshot instead of _graph_snapshot. Measured: tests/unit/test_ticket_runner_land_release.py (21 tests) and tests/ticket_land_suite/test_release.py (36 tests, including T-5034's own original regression test) all pass clean.
