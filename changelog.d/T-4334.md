bump: minor
T-4334: Anchored INV003/INV004's genuine append-only/never-null claim in docs/modules/verify-rapid-debt-visibility.md with a new invariant (invariants/INV-052.md), code-anchored at tests/unit/test_rapid_debt.py::TestRecordRapidDebt.test_appends_one_json_line_per_call (same-file trust with its own evidence; deliberately NOT anchored in src/frob/app/verify_runner.py or src/frob/tickets/_evidence.py, both large multi-purpose files whose pre-existing frob:doc/frob:tests webs would have pulled 100+ unrelated symbols into this ticket's scope-closure obligations -- exactly the anti-pattern T-4324's own doc explains it split this file out to avoid). Reworded the one loose "never cleared or promoted" historical-measurement sentence instead of anchoring it, since it describes a past incident, not an enforced behavior. Fixed REF002 by linking the doc from docs/index.md (the same module-index surface the other T-1780 subject-split docs are already linked from) rather than folding it back into tickets-verify-sweep.md, which would have reopened the exact scope-closure blowup (137 warnings) T-4324 built this split-out file to avoid. Recorded a T-4310 scope-breadth-ack (not a frob:waive -- none of the three target rules were waived) for the residual SCOPE002 gap this narrow, deliberate scope leaves against verify_runner.py/_evidence.py's own pre-existing edges. Verified: `frob check --ticket T-4334` is 0 errors. A bare unscoped `frob check --json` on this still-uncommitted worktree diff shows PRE001/SCOPE001 ("no active ticket is derivable") -- a pre-existing branch-naming artifact (`frob ticket work` creates lowercase `t-####` branches; `_BRANCH_TICKET_RE` expects `T-####-`) unrelated to this ticket's content, which clears once this lands on main. INV002, INV003, INV004, and REF002 are confirmed gone from both the `--ticket`-scoped and unscoped counts.
