bump: minor
T-5166: T-4658's _refuse_renumber_inside_worktree refused ANY renumber under .claude/worktrees/, including frob ticket land's own root-orchestrated finalize_draft_for_land calls -- so every land whose worktree carried an unpromoted draft (its own id or a sibling's) died with WorktreeLeaseViolation. That guard exists to stop an AGENT renumbering from inside its own worktree, not land's legitimate call against the worktree it is landing. renumber_one/renumber_one_v2 now take a _land_internal bypass, used only by finalize_draft_for_land, so land succeeds while the agent-renumber refusal (default False) is unchanged.
