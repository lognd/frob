"""SYNTHETIC FIXTURE (T-4113): the config field `configured.py`'s stub call
site reads its outbound host from -- not real frob code, never imported by
the package."""

MEDIA_HOST_URL = "https://media.example.com/asset"
