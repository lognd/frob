// contention_port_clean.strata -- T-0699 SYS200 litmus: the clean twin of
// contention_port_vuln.strata -- two distinct nodes each declare their
// OWN, non-overlapping port. SYS200 must not fire.
//
// tested end to end by tests/unit/strata/test_contention.py.

module contention_port_clean

node api_a : trusted {
    clearance Internal;
    listens 8080;
}

node api_b : trusted {
    clearance Internal;
    listens 8081;
}

// frob:waive REF002 reason="litmus fixture read by exactly one dedicated test module by design (T-0145/T-0255/T-0699 litmus suites); a second consumer would not be genuine"
