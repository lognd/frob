# frob:waive PARSE002 reason="intentionally-malformed parser test fixture \
# (tests/test_lang.py, tests/test_gates.py::TestParseFailureGate); \
# the syntax error below is the point of the file"
def good_one(x: int) -> int:
    """A valid function before the syntax error."""
    return x + 1


def broken_one(x, y
    return x
