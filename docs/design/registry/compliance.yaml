# frob:used-by src/frob/gates/_registry_exhaustiveness.py
# compliance.yaml -- compliance-framework registry
# source doc: docs/design/compliance-corpus.md
# NOTE: source manifest is UNIT-granular (27 named units, e.g. GDPR-ARTICLES count=99),
# not leaf-control-granular -- individual controls/articles within a unit have no
# separate canonical id in the source doc. See RECONCILIATION.md.
# T-0388/T-0607: the 17 "mixed"/"config"/"static" entries below were
# previously deferred:T-0388, then deferred:T-0607 (T-0388's own
# re-pointing pass) -- both factually wrong the moment their named ticket
# closed, since a `deferred:` disposition is only ever a promise that
# SOME OTHER standing ticket will do the work, and neither T-0388 nor
# T-0607 was that ticket -- each WAS the reconciliation/enforcement
# ticket itself. T-0607 closed this out: each of the 17 now carries a
# reasoned out_of_scope disposition (leaf-level primary-source text is
# partial/paywalled per docs/design/compliance-corpus.md's own research
# note, so per-control static enforcement is not buildable without
# fabricating unverified control text) naming COMPLIANCE005
# (`check_cmpl_registry_unit_dispositions`, src/frob/strata/
# _compliance.py) as the standing structural compensating control: it
# refuses any of these 17 ids ever reverting to deferred/undispositioned
# again. The 10 "process"/"advisory" entries stay out_of_scope per their
# own checkability tag, unchanged.
schema_version: 1
total: 27
entries:
  - id: "CMPL-SOC2-CATEGORIES"
    framework: "SOC2"
    leaf_count: 5
    checkability: "mixed"
    source_doc: "docs/design/compliance-corpus.md"
    # out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1245 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string
    disposition: "out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1245 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string"
    cross_refs: []
  - id: "CMPL-SOC2-CC-FAMILIES"
    framework: "SOC2"
    leaf_count: 9
    checkability: "mixed"
    source_doc: "docs/design/compliance-corpus.md"
    # out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1245 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string
    disposition: "out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1245 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string"
    cross_refs: []
  - id: "CMPL-PCIDSS-REQUIREMENTS"
    framework: "PCI-DSS-v4.0.1"
    leaf_count: 12
    checkability: "mixed"
    source_doc: "docs/design/compliance-corpus.md"
    # out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1245 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string
    disposition: "out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1245 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string"
    cross_refs: []
  - id: "CMPL-HIPAA-ADMIN-STANDARDS"
    framework: "HIPAA-Security-Rule"
    leaf_count: 8
    checkability: "process"
    source_doc: "docs/design/compliance-corpus.md"
    disposition: "out_of_scope:none -- this entry's own checkability tag is 'process': it names an organizational/procedural control (a documented policy or workflow a human/org must follow), not a static code property; frob's checks operate on source code and cannot verify organizational process compliance -- a compliance audit or organizational policy review is the layer that could verify this"
    cross_refs: []
  - id: "CMPL-HIPAA-PHYSICAL-STANDARDS"
    framework: "HIPAA-Security-Rule"
    leaf_count: 4
    checkability: "advisory"
    source_doc: "docs/design/compliance-corpus.md"
    disposition: "out_of_scope:none -- this entry's own checkability tag is 'advisory': it is a best-practice recommendation rather than a testable rule, and names no specific catching control; frob's checks target testable rule violations -- a compliance/design review is the layer that could evaluate adherence to this advisory guidance"
    cross_refs: []
  - id: "CMPL-HIPAA-TECHNICAL-STANDARDS"
    framework: "HIPAA-Security-Rule"
    leaf_count: 5
    checkability: "config"
    source_doc: "docs/design/compliance-corpus.md"
    # out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1245 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string
    disposition: "out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1245 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string"
    cross_refs: []
  - id: "CMPL-GDPR-CHAPTERS"
    framework: "GDPR"
    leaf_count: 11
    checkability: "process"
    source_doc: "docs/design/compliance-corpus.md"
    disposition: "out_of_scope:none -- this entry's own checkability tag is 'process': it names an organizational/procedural control (a documented policy or workflow a human/org must follow), not a static code property; frob's checks operate on source code and cannot verify organizational process compliance -- a compliance audit or organizational policy review is the layer that could verify this"
    cross_refs: []
  - id: "CMPL-GDPR-ARTICLES"
    framework: "GDPR"
    leaf_count: 99
    checkability: "mixed"
    source_doc: "docs/design/compliance-corpus.md"
    # out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1246 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string
    disposition: "out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1246 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string"
    cross_refs: []
  - id: "CMPL-CCPA-CORE-RIGHTS"
    framework: "CCPA"
    leaf_count: 4
    checkability: "process"
    source_doc: "docs/design/compliance-corpus.md"
    disposition: "out_of_scope:none -- this entry's own checkability tag is 'process': it names an organizational/procedural control (a documented policy or workflow a human/org must follow), not a static code property; frob's checks operate on source code and cannot verify organizational process compliance -- a compliance audit or organizational policy review is the layer that could verify this"
    cross_refs: []
  - id: "CMPL-CPRA-ADDED-RIGHTS"
    framework: "CPRA"
    leaf_count: 3
    checkability: "process"
    source_doc: "docs/design/compliance-corpus.md"
    disposition: "out_of_scope:none -- this entry's own checkability tag is 'process': it names an organizational/procedural control (a documented policy or workflow a human/org must follow), not a static code property; frob's checks operate on source code and cannot verify organizational process compliance -- a compliance audit or organizational policy review is the layer that could verify this"
    cross_refs: []
  - id: "CMPL-NIST80053-FAMILIES"
    framework: "NIST-SP-800-53-Rev5"
    leaf_count: 20
    checkability: "mixed"
    source_doc: "docs/design/compliance-corpus.md"
    # out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1247 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string
    disposition: "out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1247 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string"
    cross_refs: []
  - id: "CMPL-NISTCSF-FUNCTIONS"
    framework: "NIST-CSF-2.0"
    leaf_count: 6
    checkability: "process"
    source_doc: "docs/design/compliance-corpus.md"
    disposition: "out_of_scope:none -- this entry's own checkability tag is 'process': it names an organizational/procedural control (a documented policy or workflow a human/org must follow), not a static code property; frob's checks operate on source code and cannot verify organizational process compliance -- a compliance audit or organizational policy review is the layer that could verify this"
    cross_refs: []
  - id: "CMPL-NIST80263-VOLUMES"
    framework: "NIST-SP-800-63"
    leaf_count: 3
    checkability: "mixed"
    source_doc: "docs/design/compliance-corpus.md"
    # out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1247 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string
    disposition: "out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1247 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string"
    cross_refs: []
  - id: "CMPL-SSDF-PRACTICE-GROUPS"
    framework: "NIST-SP-800-218"
    leaf_count: 4
    checkability: "static"
    source_doc: "docs/design/compliance-corpus.md"
    # out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1247 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string
    disposition: "out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1247 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string"
    cross_refs: []
  - id: "CMPL-ISO27002-THEMES"
    framework: "ISO-IEC-27001-27002-2022"
    leaf_count: 4
    checkability: "mixed"
    source_doc: "docs/design/compliance-corpus.md"
    # out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1248 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string
    disposition: "out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1248 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string"
    cross_refs: []
  - id: "CMPL-ISO27002-CONTROLS"
    framework: "ISO-IEC-27001-27002-2022"
    leaf_count: 93
    checkability: "mixed"
    source_doc: "docs/design/compliance-corpus.md"
    # out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1248 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string
    disposition: "out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1248 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string"
    cross_refs: []
  - id: "CMPL-CIS-CONTROLS"
    framework: "CIS-Controls-v8"
    leaf_count: 18
    checkability: "mixed"
    source_doc: "docs/design/compliance-corpus.md"
    # out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1248 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string
    disposition: "out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1248 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string"
    cross_refs: []
  - id: "CMPL-CIS-SAFEGUARDS"
    framework: "CIS-Controls-v8"
    leaf_count: 153
    checkability: "config"
    source_doc: "docs/design/compliance-corpus.md"
    # out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1248 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string
    disposition: "out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1248 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string"
    cross_refs: []
  - id: "CMPL-CIS-IMPLEMENTATION-GROUPS"
    framework: "CIS-Controls-v8"
    leaf_count: 3
    checkability: "advisory"
    source_doc: "docs/design/compliance-corpus.md"
    disposition: "out_of_scope:none -- this entry's own checkability tag is 'advisory': it is a best-practice recommendation rather than a testable rule, and names no specific catching control; frob's checks target testable rule violations -- a compliance/design review is the layer that could evaluate adherence to this advisory guidance"
    cross_refs: []
  - id: "CMPL-ASVS-CHAPTERS"
    framework: "OWASP-ASVS-4.0.3"
    leaf_count: 14
    checkability: "static"
    source_doc: "docs/design/compliance-corpus.md"
    # out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1249 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string
    disposition: "out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1249 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string"
    cross_refs: []
  - id: "CMPL-ASVS-REQUIREMENTS"
    framework: "OWASP-ASVS-4.0.3"
    leaf_count: 286
    checkability: "static"
    source_doc: "docs/design/compliance-corpus.md"
    # out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1249 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string
    disposition: "out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1249 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string"
    cross_refs: []
  - id: "CMPL-ASVS-LEVELS"
    framework: "OWASP-ASVS-4.0.3"
    leaf_count: 3
    checkability: "advisory"
    source_doc: "docs/design/compliance-corpus.md"
    disposition: "out_of_scope:none -- this entry's own checkability tag is 'advisory': it is a best-practice recommendation rather than a testable rule, and names no specific catching control; frob's checks target testable rule violations -- a compliance/design review is the layer that could evaluate adherence to this advisory guidance"
    cross_refs: []
  - id: "CMPL-SAMM-FUNCTIONS"
    framework: "OWASP-SAMM-v2"
    leaf_count: 5
    checkability: "process"
    source_doc: "docs/design/compliance-corpus.md"
    disposition: "out_of_scope:none -- this entry's own checkability tag is 'process': it names an organizational/procedural control (a documented policy or workflow a human/org must follow), not a static code property; frob's checks operate on source code and cannot verify organizational process compliance -- a compliance audit or organizational policy review is the layer that could verify this"
    cross_refs: []
  - id: "CMPL-SAMM-PRACTICES"
    framework: "OWASP-SAMM-v2"
    leaf_count: 15
    checkability: "process"
    source_doc: "docs/design/compliance-corpus.md"
    disposition: "out_of_scope:none -- this entry's own checkability tag is 'process': it names an organizational/procedural control (a documented policy or workflow a human/org must follow), not a static code property; frob's checks operate on source code and cannot verify organizational process compliance -- a compliance audit or organizational policy review is the layer that could verify this"
    cross_refs: []
  - id: "CMPL-FEDRAMP-IMPACT-TIERS"
    framework: "FedRAMP"
    leaf_count: 3
    checkability: "mixed"
    source_doc: "docs/design/compliance-corpus.md"
    # out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1249 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string
    disposition: "out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1249 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string"
    cross_refs: []
  - id: "CMPL-SLSA-BUILD-LEVELS"
    framework: "SLSA-v1.0"
    leaf_count: 4
    checkability: "static"
    source_doc: "docs/design/compliance-corpus.md"
    # out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1249 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string
    disposition: "out_of_scope:none -- leaf-level control text for this unit is partial/paywalled/unverified at the primary source per docs/design/compliance-corpus.md's own research-method note, so per-control static enforcement cannot be built without fabricating unverified control text; T-1249 triage confirms no (a)/(b)/(c) path exists without primary-source access -- classified (d) out of scope, not merely riding a generic handled_by:COMPLIANCE005 string"
    cross_refs: []
  - id: "CMPL-FROB-CATALOG-ENTRIES"
    framework: "frob-std.compliance"
    leaf_count: 7
    checkability: "mixed"
    source_doc: "docs/design/compliance-corpus.md"
    # T-1250 confirms this row is NOT the same vacuous self-reference shape
    # as the 16 other CMPL_REGISTRY_UNIT_IDS rows (T-1245-T-1249): it is a
    # meta-row counting COMPLIANCE_CATALOG's own real RegulationEntry
    # units -- each has a real RegulationEntry+mitigation wired into
    # check_regulation_catalog_completeness/check_regulation_discharge
    # (COMPLIANCE001-003), independently of this registry row's own
    # disposition string. handled_by:COMPLIANCE005 here is verified via
    # those real catalog entries, not merely a non-deferred disposition
    # string riding COMPLIANCE005's structural check -- explicitly stated
    # so this row is not silently swept into the same re-triage bucket as
    # the 16 vacuous ones. leaf_count corrected 6 -> 7 (T-1250): COMPLIANCE_
    # CATALOG grew to 7 entries when T-1314 added PRIVACY-NOTICE
    # (COPPA, GDPR-ERASURE/RETENTION/BASIS, HIPAA-BAA, MINIMIZATION,
    # PRIVACY-NOTICE); this row is this ticket's own scope
    # (docs/design/registry/compliance.yaml) and was left stale after
    # T-1314 landed. docs/design/compliance-corpus.md's own manifest
    # (count: 6, TOTAL_LEAF_CONTROLS_ENUMERATED: 599) is the upstream
    # source doc this row derives from and is now ALSO stale by the same
    # +1 -- that file is outside this ticket's scope (not in T-1250's
    # `scope` globs), so a follow-on ticket is filed to correct it and
    # TOTAL_LEAF_CONTROLS_ENUMERATED (599 -> 600) together, rather than
    # silently editing an out-of-scope file here.
    disposition: "handled_by:COMPLIANCE005"
    cross_refs: []
# total_leaf_controls_enumerated corrected 599 -> 600 (T-1250) alongside
# CMPL-FROB-CATALOG-ENTRIES's leaf_count 6 -> 7 -- same PRIVACY-NOTICE
# (T-1314) drift; docs/design/compliance-corpus.md's own manifest total is
# tracked in the follow-on ticket noted on that row above.
total_units: 27
total_leaf_controls_enumerated: 600
