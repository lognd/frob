# frob:used-by src/frob/gates/_registry_exhaustiveness.py
# secrets.yaml -- secret-detection registry (section-granular, see compliance.yaml note)
# source doc: docs/design/secrets-pii-corpus.md
schema_version: 1
total: 3
entries:
  - id: "SEC-SECRETS-SECRETS-DETECTOR_PROJECTS"
    total_items: 5
    sourcing: "verified"
    source_doc: "docs/design/secrets-pii-corpus.md"
    disposition: "out_of_scope:bibliographic census of external tools (gitleaks, trufflehog, detect-secrets, git-secrets, GitHub secret scanning) that frob does not vendor or wrap -- informational sourcing table only, not a frob-enforceable claim"
    cross_refs: []
  - id: "SEC-SECRETS-SECRETS-DETECT_SECRETS_PLUGINS"
    total_items: 26
    sourcing: "verified"
    source_doc: "docs/design/secrets-pii-corpus.md"
    # T-draft-adc19cf8: was handled_by:SEC002, which is factually wrong --
    # SEC002 is the tracked-.env-file check, unrelated to plugin/token-
    # format detection. The `_PATTERNS` regex table (frob.gates._secrets)
    # is what actually encodes this corpus, and it fires as SEC001 (with
    # SEC003 carved out for the unwaivable Stripe-live case).
    disposition: "handled_by:SEC001"
    cross_refs: []
  - id: "SEC-SECRETS-SECRETS-PROVIDER_TOKEN_FORMATS"
    total_items: 30
    sourcing: "{'verified': 28, 'partial': 2}"
    source_doc: "docs/design/secrets-pii-corpus.md"
    # T-draft-adc19cf8: same fix as DETECT_SECRETS_PLUGINS above -- see
    # that entry's comment.
    disposition: "handled_by:SEC001"
    cross_refs: []
total_sections: 3
