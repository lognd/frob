# frob vet

dependency-vetting: lockfile allow conformance, quarantine, typosquat, lifecycle scripts, osv advisories

## Usage

```
usage: frob vet [-h] [--hook COMMAND] [--json] [--cve-mirror DIR]
                [--timeout SECONDS] [--jobs N]
                [path]
```

*Generated from the live argparse tree (`frob docs --sync-command-pages`) -- a stub; enrich with worked examples as this verb's real usage patterns become clear.*
