# frob process

process/forkserver maintenance (T-3106): reap orphaned forkservers on demand -- also available as `frob ops process` (T-4520)

## Usage

```
usage: frob process [-h] {reap} ...
```

*Generated from the live argparse tree (T-4702, `frob docs --sync-command-pages`) -- a stub; enrich with worked examples as this verb's real usage patterns become clear.*
