# frob test

select and run tests for the touched set (or --all)

## Usage

```
usage: frob test [-h] [--all] [--fuzz] [--collect] [--wait-coverage]
                 [--base REF] [--lang L] [--fallback {package,suite,warn}]
                 [--json]
                 [path]
```

*Generated from the live argparse tree (`frob docs --sync-command-pages`) -- a stub; enrich with worked examples as this verb's real usage patterns become clear.*
