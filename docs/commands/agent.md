# frob agent

print/export the dispatched-agent guard env (T-0574) -- 'env' is implied: bare `frob agent` runs it (T-4546); the two-word `frob agent env` spelling still works as an alias

## Usage

```
usage: frob agent [-h] {env} ...
```

*Generated from the live argparse tree (T-4702, `frob docs --sync-command-pages`) -- a stub; enrich with worked examples as this verb's real usage patterns become clear.*
