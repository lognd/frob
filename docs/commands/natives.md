# frob natives

build declared [[native]] crates (T-0864: frob-owned maturin develop, shared CARGO_TARGET_DIR) -- 'build' is implied: bare `frob natives` runs it (T-4522); the two-word `frob natives build` spelling still works as an alias

## Usage

```
usage: frob natives [-h] [--path DIR] {build} ...
```

*Generated from the live argparse tree (T-4702, `frob docs --sync-command-pages`) -- a stub; enrich with worked examples as this verb's real usage patterns become clear.*
