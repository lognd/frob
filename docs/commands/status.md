# frob status

delta-first movement summary: findings burned/introduced since the last baseline, verification lag, ticket landing velocity -- reuses frob check --stamp-baseline/frob verify status/frob ticket flow's own data, invents no new counter

## Usage

```
usage: frob status [-h] [--path DIR] [--json] [--only GATE] [--tickets]
                   [--no-tickets]
```

*Generated from the live argparse tree (T-4702, `frob docs --sync-command-pages`) -- a stub; enrich with worked examples as this verb's real usage patterns become clear.*
