# frob claude

sync this repo's tracked Claude config to ~/.claude/ (T-1808) -- 'sync' is implied: bare `frob claude` runs it (T-4522); the two-word `frob claude sync` spelling still works as an alias

## Usage

```
usage: frob claude [-h] [--check] {sync} ...
```

*Generated from the live argparse tree (T-4702, `frob docs --sync-command-pages`) -- a stub; enrich with worked examples as this verb's real usage patterns become clear.*
