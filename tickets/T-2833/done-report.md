## Done report

Changed:
- src/frob/tickets/_worktree_sweep.py (new) -- sweep_worktrees, remove_worktree, _WorktreeSweepError, _WorktreeVerdict, _is_agent_worktree_path, _list_agent_worktrees, _worktree_is_clean, _worktree_head_age_seconds, _kept_live_verdict_if_process_present, _kept_unlanded_verdict_if_present, _kept_lease_or_age_verdict, _sweep_verdict_for_worktree -- verbatim move out of _leases.py, directives intact; added a DUP001 waiver (pre-existing cross-file similarity to frob.app.ticket_runner._lifecycle._root_is_itself_a_nested_worktree, surfaced by the move, never flagged on main because the clone gate only scans files in the diff) and two AFFECT001 waivers (digest moved only because the file path changed, T-1162's precedent shape)
- src/frob/tickets/_leases.py -- worktree-sweep family removed; re-imports/re-exports sweep_worktrees/remove_worktree from _worktree_sweep so its two existing external importers (frob.app.worktree_runner, frob.app.ticket_runner._rapid_sweep) need no change; also re-imports _list_agent_worktrees (a private helper _should_skip_root_lease, T-2007, still needs -- not part of the moved public surface's own external contract, but an internal cross-reference); dropped the LARGE001 waiver header (no longer applicable, replaced with a short pointer to the new module)
- tests/test_ticket_leases.py -- retargeted plain imports and frob:tests directives for _WorktreeSweepError/_WorktreeVerdict/remove_worktree to _worktree_sweep.py; added frob:ticket T-2833 to every test/class COV002 flagged as touched-with-no-open-ticket-edge
- tests/unit/test_rapid_sweep.py -- retargeted a plain import of _WorktreeSweepError to _worktree_sweep; added frob:ticket T-2833 to the COV002-flagged class/test (its monkeypatch.setattr("frob.tickets._leases.sweep_worktrees", ...) calls needed NO change -- sweep_stale_worktrees_after_land does a function-local `from frob.tickets._leases import sweep_worktrees` at call time, and _leases.py still carries that name via the re-export, so the patch target still resolves correctly)

Capability-grant premise verified and found NOT to hold: measured `frob check --ticket T-2833` against the new file and found ZERO SYS003/SYS100/SEC capability findings -- the moved code's only I/O is `gitio.run_argv(...)` (git subprocess calls), and the effect-detection needle table attributes "exec" effects to the file containing the RAW subprocess call (frob.gitio itself, which already carries its own exec grant), not to callers of a wrapper function. `_leases.py` itself was never in design/frob.strata's `may "exec"` via-list for exactly this reason -- git calls through `gitio.run_argv` were never flagged there either. The moved file also does no direct fs.read/fs.write (no `open`/`.read_text`/`.write_text`/`.unlink`/`os.stat` -- confirmed via grep, zero hits) and no env reads. Conclusion: NO new capability grant was needed in design/frob.strata; the identifying ticket's premise (T-2822's own investigation) was based on a plausible-sounding but unmeasured assumption. design/frob.strata was left untouched -- the correct minimal action per this ticket's own "verify the premise" instruction, not "add the narrowest grant" (there was nothing to grant).

Seam verification: confirmed on inspection -- the worktree-sweep family (~440 lines) is reachable only from frob.app.worktree_runner's CLI (`frob worktree sweep`/`remove`) and frob.app.ticket_runner._rapid_sweep's rapid-profile auto-sweep, a genuinely distinct consumer set from the lease-CRUD/ledger-commit machinery the rest of _leases.py serves. Its only real dependency back on _leases.py is lease-liveness data (_LeaseRecord, read_all_leases, is_lease_ttl_expired [transitively, via _live_lease_for_worktree], lease_age_seconds, _live_lease_for_worktree, scan_for_live_worktree_process) -- inherent to a worktree-removal-safety decision, not leftover coupling. One direction of coupling runs the other way too: _leases.py's own _should_skip_root_lease (T-2007) needs _list_agent_worktrees, now re-imported back from _worktree_sweep.

Refactor hazards checked: no mock.patch call site targets a moved symbol by its old _leases.py-qualified path (git grep, zero hits) -- the one CLI test that DOES patch `frob.app.worktree_runner.sweep_worktrees` still resolves correctly because worktree_runner.py's own `from frob.tickets._leases import sweep_worktrees` import is unaffected by the split (re-export). Every plain (non-patch) import of a moved private symbol (_WorktreeSweepError, _WorktreeVerdict) was retargeted to the new module. Re-measured UNSCOPED `frob check` (no --ticket): only the expected PRE001/SCOPE001 "no --ticket passed" notices on the touched files, zero new defects.

Evidence: tests/test_ticket_leases.py::TestSweepWorktrees::test_clean_no_lease_removed, tests/test_ticket_leases.py::TestRemoveWorktree::test_removes_a_clean_unleased_worktree, tests/test_ticket_leases.py::TestWorktreeSweepCli::test_sweep_cli_prints_verdicts_and_summary, tests/unit/test_rapid_sweep.py::TestSweepStaleWorktreesAfterLand::test_never_uses_force, tests/test_ticket_leases_cross_worktree.py::TestCrossWorktreeLeaseVisibility::test_lease_written_in_one_worktree_seen_in_another

Filed: none

Gates: `frob check --ticket T-2833` -- every finding this split itself introduced (ruff import-sort/line-length, DUP001, AFFECT001 x2, COV002 missing frob:ticket edges, SCOPE001 on the two touched test files) is fixed. Final scoped run shows zero remaining errors touching _leases.py/_worktree_sweep.py/the two retargeted test files -- the only remaining errors are pre-existing repo baseline noise (an unrelated import-cycle report, SYS003 on src/frob/check/__init__.py, ruff issues in src/frob/strata/_selfconform*.py, unrelated DRIFT/DOC/TICK/SEC/PERF findings). `uv run pytest -q tests/test_ticket_leases.py tests/test_worktree_guard.py tests/unit/test_rapid_sweep.py tests/unit/test_land_finish_guard.py tests/test_ticket_leases_cross_worktree.py` -> SUITE-RESULT: exitstatus=1 collected=370 failed=6, all 6 failures confirmed pre-existing on main (reproduced byte-for-byte with none of this ticket's changes applied: a `frob ticket priority/tier/... --reason` dispatch-table gap, unrelated to this split).

### Changed
```
 tickets/T-2833/ticket.md | 37 ++++++++++++++++++++++++++++++++++++-
 1 file changed, 36 insertions(+), 1 deletion(-)
```

### Evidence
- `tests/test_ticket_leases.py::TestSweepWorktrees::test_clean_no_lease_removed` (pytest node id, verified passing when recorded)
- `tests/test_ticket_leases.py::TestRemoveWorktree::test_removes_a_clean_unleased_worktree` (pytest node id, verified passing when recorded)
- `tests/test_ticket_leases.py::TestWorktreeSweepCli::test_sweep_cli_prints_verdicts_and_summary` (pytest node id, verified passing when recorded)
- `tests/unit/test_rapid_sweep.py::TestSweepStaleWorktreesAfterLand::test_never_uses_force` (pytest node id, verified passing when recorded)
- `tests/test_ticket_leases_cross_worktree.py::TestCrossWorktreeLeaseVisibility::test_lease_written_in_one_worktree_seen_in_another` (pytest node id, verified passing when recorded)

### Captured claims
- tests: 5 passed (from 5 evidence id(s))
- gates: 21 error(s), 959 warning(s), 750 waived
- error-findings: CLAUDE001@.claude/hooks/sync-claude-config.py, COV001@src/frob/graph/callgraph.py, CYCLE001@src/frob/__init__.py, DOC006@docs/audits/test005-zero-classification-t1418.md, DOC011@docs/investigations/T-2796-backlog-reproduction.md, DRIFT001@src/frob/app/ticket_runner/_verify.py, DRIFT001@src/frob/tickets/__init__.py, DRIFT002@docs/modules/tickets-data-storage.md, PERF004@src/frob/strata/_selfconform_binding_rules.py, PERF004@src/frob/strata/_selfconform_surface_rules.py, PERF004@src/frob/tickets/_evidence.py, REG002@docs/design/registry/check-coverage.yaml, SEC110@src/frob/app/ticket_runner/_verify.py, SEC110@src/frob/app/verify_runner.py, SEC110@tests/test_release.py, SELFAUDIT001@design, SYS003@src/frob/check/__init__.py, TEST001@src/frob/strata/_multifile.py, TICK003@tickets.md, TICK004@tickets.md, TICK006@tickets.md
