## Done report

ANSWER: (a), attrs on edges (and on nodes) is INTENDED and genuinely consumed -- cited with the diff, not inferred. git show 5ee140434^:strata-core/src/parse/grammar_core.rs (the pre-T-3260-split file) shows the identical ast.vmodel_edges.push({..., attrs: attrs}) call site and the identical T-3044 H3 comment already present verbatim BEFORE T-3260 touched the file at all (git log -S confirms T-3044's commit 51bc8c6dd first introduced this exact line; T-3260's commit 5ee140434 only relocated it unchanged into the new strata-core/src/parse/grammar_vmodel.rs). So T-3260 introduced NOTHING here -- it moved code that was already stale-relative-to-its-own-test since T-3044 landed, and the test (written at T-3042, before T-3044 existed) was never updated when T-3044 added attrs. T-3260 is not at fault; the finding's own title attributing this to T-3260 is really 'T-3260 made a pre-existing gap newly visible', same shape as T-3423's 'newly visible not newly introduced' pattern this session. CONSUMER NAMED: src/frob/gates/_vmodel.py's _collect_nodes_edges (lines ~160-163) reads e.get('attrs', {}) for every vmodel_edge and n.get('attrs', {}) for every vmodel_node, feeding Graph.add_node_with_attrs/add_edge_with_attrs in strata_core.vmodel_check -- a real, load-bearing consumer, not unreferenced surface. FIX: updated test_vmodel_node_and_edge_round_trip_through_python to assert the FULL node/edge payload shape (name/kind/level/attrs for nodes, kind/src/dst/attrs for edges) instead of a subset (edges previously omitted attrs entirely; nodes were only checked by name), and added a runnable-node/reason-edge case so attrs' actual consumed content has real coverage, not just an empty-dict shape that would pass whether or not the grammar wired the field at all. FFI PAYLOAD-SHAPE COVERAGE ENUMERATION (the wider check): (1) vmodel_nodes JSON array (grammar_vmodel.rs::parse_vmodel_node) -- now fully covered by the exact-shape assertion above. (2) vmodel_edges JSON array (grammar_vmodel.rs::parse_vmodel_edge) -- same, now fully covered. (3) strata_core.vmodel_check's PyO3 return, (Vec<String>, Vec<(String,String)>) -- NOT part of T-3260's split (lives in strata-core/src/lib.rs, untouched by that ticket) and structurally cannot silently gain a field the way a serde_json::Value dict can: PyO3 enforces the Rust tuple/Vec signature at compile time, so a shape change there is a compile error, not a silent runtime drift; already has dedicated end-to-end Python coverage in tests/unit/strata/test_vmodel_check.py (T-3043) exercising real (nodes, edges) tuples through the same boundary. (4) graph::vmodel::{Graph, ClosureViolation, GraphSchema} internal to strata-core/src/graph/vmodel/{mod.rs,closure.rs} -- consumed internally by vmodel_check before conversion to the typed tuple above; never independently serialized across the FFI boundary, so no separate shape-coverage gap. No other type crossing the boundary was found uncovered in the T-3260 split files.

### Changed
```
 tickets/T-3424/ticket.md | 7 ++++++-
 1 file changed, 6 insertions(+), 1 deletion(-)
```

### Evidence
- `tests/unit/strata/test_vmodel_authoring.py::TestVmodelAuthoringFormat::test_vmodel_node_and_edge_round_trip_through_python` (pytest node id, verified passing when recorded)
- `tests/unit/strata/test_vmodel_authoring.py::TestVmodelAuthoringFormat::test_duplicate_vmodel_node_name_is_a_parse_error` (pytest node id, verified passing when recorded)
- `tests/unit/strata/test_vmodel_authoring.py::TestVmodelAuthoringFormat::test_existing_bare_module_files_parse_unchanged` (pytest node id, verified passing when recorded)
- `tests/unit/strata/test_vmodel_authoring.py::TestVmodelAuthoringFormat::test_designs_own_frob_strata_still_parses` (pytest node id, verified passing when recorded)

### Captured claims
- tests: 4 passed (from 4 evidence id(s))
- gates: 13 error(s), 3966 warning(s), 856 waived
- error-findings: COV003@tickets/T-3410, DEPR006@frob-deprecated-baseline.lock.json, DOC006@docs/design/windows-portability.md, DOC006@tickets/T-3411/ticket.md, DOC006@tickets/T-3424/ticket.md, DRIFT001@src/frob/app/ticket_runner/_rapid_sweep.py, OPAQUE001@src/frob/_cli_parsers/_ticket/_metadata.py, PRE001@tickets/T-3424, REL001@src/frob/__init__.py, SELFAUDIT001@design, TICK004@tickets.md, WAIVE011@frob-ratchet.lock.json, unresolved-attribute@tests/system/test_coverage_sigterm.py
